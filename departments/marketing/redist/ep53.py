# -*- coding: utf-8 -*-
r"""ep53 재유통 보충 선언 (2026-09-12 신설).

ep53 은 `_facts.py` 가 **있는데 `EP` 선언이 없다** — 주장 값(76줄)만 담겨 있다. 그래서 워커가
편 번호를 못 읽어 `epNone` 으로 발행로그를 뒤지다 섰다. 편 폴더는 `01_발행완료` 라 못 고치므로
(정관 §2) 빠진 칸만 여기서 채운다. **편 폴더 선언이 언제나 이기고 여기 것은 «없는 칸만» 채운다.**

값의 출처는 편 폴더의 `assemble_reel.py`(소스 파일 42행 · `KIT = None` 57행)다.
"""

EP = 53

#: Threads 에 붙일 공식 원본 — 릴스 재료가 된 오픈에이아이 소개 필름.
ATTACH_OFFICIAL = ("_official/openai_images25_2097394956457623964.mp4",)

#: 🔴 **킷 없는 편이다** (`assemble_reel.py` 57행 `KIT = None` · 발행로그 킷 칸 «없음»).
#:    마지막 포스트는 원류 안내로 닫는다 — 이 편은 인스타에 이미 나가 있어
#:    발행로그가 그 URL 의 정본이다(게이트 `[4-2b]`).
