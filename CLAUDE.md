# JJ Company OS — 회사 정관

> 이 레포는 JJ의 개인 자동화 본사다. 모든 스케줄 작업과 부서 에이전트는 이 디렉토리를 CWD로 실행된다.
> 이 파일은 500줄을 넘길 수 없다. 넘기면 분리하지 말고 줄여라.

## 0. 대원칙 (대표실)

- 방향/목표/전략/최종 결정은 JJ가 내린다. 에이전트는 위임받은 범위만 실행한다.
- 자동화 등급 3단계를 절대 넘지 않는다:
  - **A등급 (완전 자동)**: read-only 감사, 리포트 생성. 파일 수정 없음.
  - **B등급 (제안까지 자동)**: 콘텐츠 초안, 카드 제안, 커밋 준비. 산출물은 reports/ 로만. 발행/승격은 JJ 승인.
  - **C등급 (사람 필수)**: 발행, 배포, 외부 발송(DM/이메일), 결제. 에이전트 직접 실행 금지.
    - **되돌리기 어려운 git 동작도 C등급이다** — force push, 브랜치·PR 삭제, 원격 이력 변경(`reset --hard` 후 push 등). 가르는 기준은 파일 종류가 아니라 **되돌림 비용**이다. `revert` 한 번으로 안 돌아오는 순간 등급이 올라간다.
  - **머지는 C등급에서 뺐다 (2026-08-15 개정).** 에이전트가 PR 생성·리베이스·충돌 해소·검증까지 다 하고 마지막 버튼만 사람이 누르는 구조는 실익 없이 병목만 만든다. 대신 **넷을 다 충족해야** 머지한다:
    1. **JJ의 명시적 승인.** "좋다"·"진행해" 같은 포괄 승인이 아니라 **그 PR을 가리킨** 승인이어야 한다.
    2. **승인 이후 내용이 바뀌지 않았을 것.** 승인은 «그 내용»에 대한 것이지 «그 PR 번호»에 대한 것이 아니다. head 커밋이 승인 시점과 다르면 다시 받는다.
    3. **`MERGEABLE`·`CLEAN` 이고 검증(verify·역검증)이 통과**했을 것.
    4. **스택이면 base와 순서를 확인**했을 것. 순서가 틀리면 나중 머지가 앞의 내용을 **되돌린다.** 근거: 2026-08-15 `skill-scan-domain-rule` 은 base가 v3.6 이전이라 그대로 머지했으면 로스터 2종·§7-8·`reels.py` 수정이 사라질 뻔했다. 같은 날 `#31`·`#32` 도 base가 `OJJ` 라 순서를 뒤집으면 훅이 되돌아가는 구조였다. **`MERGEABLE` 은 «충돌 없음»이지 «되돌리지 않음»이 아니다.**
  - **정본(CLAUDE.md·SKILL.md) 변경도 머지 대상에서 빼지 않는다.** "사람이 읽어야 한다"는 목적은 **승인 시점에 이미 달성된다** — 승인했다는 건 그 조항을 읽었다는 뜻이고 머지는 기계적 동작이다. 파일 종류로 가르면 오늘 기준 8건 중 6건이 걸려 병목이 그대로 남는데, 정작 막아야 할 «JJ가 읽지 않은 내용»은 파일 종류로 갈리지 않는다(조건 2·4가 그것을 본다). 대신 **머지 후 바뀐 조항 전문을 보고에 붙인다** — 사후 읽기를 보장하고, 정본은 `revert` 한 번으로 되돌아간다.
  - **머지 후 실물을 확인하고 보고한다.** `state=MERGED` 와 **대상 브랜치의 실물**을 같이 본다. 근거: 2026-08-15 낡은 PR 목록만 보고 "아직 안 머지됐다"고 보고한 일이 있다 — 실제로는 이미 머지돼 있었다.
  - **발행은 매체별로 가른다 (2026-08-28 확정).** 「발행」을 한 덩어리로 두면 **되돌림 비용이 전혀 다른 것들이 같은 칸에** 앉는다 — 가르는 기준은 §0 그대로 되돌림 비용이고, 매체마다 그 값이 다르다.
    - **인스타그램 게시는 계속 C등급이다 — 이 조항은 그것을 개정하지 않는다.** 자동화하려면 Content Publishing API 가 필요하고 그것이 **덱 규격 변경 4건을 전제**하는데, 그중 둘이 조문·비공개 원칙과 정면으로 부딪힌다. 보류 사유와 실측은 `docs\infra-backlog.md` **13번**.
    - **Threads 텍스트 스레드는 에이전트 발행 대상으로 연다.** 첨부 0건이라 **형식·URL·오디오 제약이 전부 해당 없고**, 남는 장벽은 자격(API 권한)뿐이다. 되돌림 비용도 낮다 — 텍스트 포스트는 삭제로 되돌아가고 원고는 `reports\` 에 남는다.
    - 🔴 **연다 ≠ 지금 발행한다.** 아래 승인 장치가 서기 전까지 에이전트는 **어느 매체로도 발행하지 않는다.** 설계 초안은 `docs\workers\publish-threads.md` 이고 **구현은 미착수**다.
    - **승인은 채팅 문장이 아니라 파일이다.** «좋다»·«올려»는 승인이 아니다 — 그 말이 **어느 판본**을 가리키는지 나중에 아무도 증명할 수 없기 때문이다(§0 머지 조건 2와 같은 뿌리). 승인은 `publish_approval\<ep>.json` 이고, **본문 해시와 체인 순서**를 박아 JJ 서명으로 만든다.
    - **에이전트는 발행 직전 셋을 확인하고, 하나라도 어긋나면 발행을 거부한다** — ① 승인 파일의 본문 해시 = 지금 발행할 원고 파일의 해시 ② 승인 파일이 **에이전트 손으로 만들어지지 않았을 것** ③ 대상 편이 일치할 것.
    - 🔴 **승인 파일의 자기 서명·위조는 금지 자리다** — 에이전트가 `publish_approval\` 을 만들거나 고치거나 서명하는 것은 §6 금지 사항이다. 이것을 열면 위 3확인 전체가 **에이전트가 자기에게 내주는 허가**로 무너진다.
    - **②는 «쓰기 차단 + 매 회차 실증»으로 잰다 (2026-08-28 확정).** 에이전트 권한에서 `publish_approval\` 을 빼고(§2 대로 `Edit(경로)` 로 적는다 — `Write(경로)` 는 안 걸린다), **워커가 뜨기 전에 그 폴더에 실제로 한 번 써 보고 거부되는지 확인**한다. 거부되지 않으면 그 회차는 중단이다. §4 vault-health 프로브와 **같은 장치·같은 양방향**이다 — 목록 안(`reports\`) 쓰기가 성공하는 것까지 같이 봐야 «전부 거부하는 상태»와 구별된다. 🔴 이것이 증명하는 것은 «에이전트가 만들 수 없다»가 아니라 **«이 회차에 만들지 못했다»** 다(§0 4층 ④).
    - **부분 발행은 보고하고 멈춘다 (2026-08-28 확정).** 체인 중간에서 끊기면 올라간 포스트를 **자동으로 이어 붙이지도, 자동으로 지우지도 않는다.** 끊긴 원인이 «일시 오류»인지 «원고가 갈렸는지»를 워커는 모르고, 모르는 채 이어 붙이면 바깥에 어긋난 스레드가 **완성된다** — 반쪽이 남아 눈에 띄는 편이 낫다. 삭제도 되돌림 비용이 0이 아니다(읽은 사람·인용·알림은 되돌아오지 않는다). 워커는 «어디까지 갔는가»를 리포트에 다 적고 `STATUS: FAIL partial-chain n/N` 을 찍는다. **이어 올릴지 지울지는 사람이 정하고, 재실행은 새 승인 파일을 요구한다.**
    - **고정 댓글(핀 고정)은 사람 자리로 확정한다.** 「고정됐는가」를 API 로 되읽을 수 없어 **성공을 확인할 방법이 없다** — §0 «감지 장치가 값을 담는지 검증한다» 를 만족시킬 수 없는 동작이라 자동화 대상에서 뺀다.
    - **적용은 ep39 부터다. ep35~38 은 소급하지 않는다** — 현행대로 사람이 발행한다.
- 추측 금지. 로그/데이터 기반으로 진단한다. 확인 안 되면 "확인 불가"로 보고한다.
- **감지 장치를 만들면 그 장치가 실제로 값을 담는지 검증한다.** 형식만 맞고 값이 비면 "기록되고 있다"로 보여서 장치가 없는 것보다 위험하다. 검사(verify)에는 **역검증**을 같이 둔다 — 일부러 걸려야 하는 입력을 넣어 검사가 헛돌지 않는지 확인한다.
  - 근거: 2026-08-15 스케줄 로그에 판본 기록을 넣었으나 PowerShell 쉼표 우선순위 탓에 스탬프가 한 줄로 뭉쳐 `skill live:  (deployed )` 로 **빈 값**이 찍혔다. 대응책 자체가 조용한 실패를 일으킨 사례다.
  - 같은 계열: 이모지 검사에 실제 이모지를 넣어 잡히는지 확인, 기능 귀속 검사에 "레이어는 다루는가"를 같이 확인 (토망치랩 힉스필드 편에서 적용).
  - **역검증 케이스는 다른 검사가 같이 걸리지 않게 분리한다.** 분리하지 않으면 **그 검사가 없었어도 잡혔을 입력**인지 알 수 없다. 위 조항이 "검사가 헛도는지"를 본다면 이건 한 겹 안쪽 — **"그 검사가 제 몫을 하는지"**를 본다.
    - 근거: 2026-08-15 `cardcheck.py` 에 격자 국소 대비를 이식하며 만든 `local_blob` 케이스. 처음엔 텍스트 존의 한 열 전체를 어둡게 칠했더니 `local`(국소 대비)과 `stdev`(배경 산만함)가 **같이** 걸렸다. 그 상태로는 격자를 빼도 stdev 가 잡으므로 **새로 넣은 검사가 필요했는지 증명되지 않는다.** 얼룩을 한 칸 크기(존의 5%)로 줄이자 전역 대비 197.5·stdev 48.3 으로 **다른 검사는 전부 통과**하고 최악 칸 1.06 으로 국소만 걸렸다 — 그제야 격자의 값어치가 증명됐다.
- **조용히 실패하는 코드를 남기지 않는다.** 실패했는데 예외도 경고도 없이 넘어가는 경로가 가장 위험하다 — 로그에 아무 일도 없으니 "동작했다"로 읽히고, 그 상태로 다음 작업이 얹힌다. 저장·기록·색인처럼 **나중에 조회될 것을 남기는 코드**가 특히 그렇다.
  - 판정 기준은 하나다: 그 경로가 실패했을 때 **누군가 알아차릴 수 있는가.** 알아차릴 방법이 없으면 예외를 던지거나 최소한 경고를 찍는다. "조건에 안 맞으면 그냥 넘어감"은 그 자체로 결함이다.
  - **검사는 쓰기 전에 한다.** 파일을 먼저 만들고 색인 단계에서 실패하면 반쪽 상태가 남아, 다음 사람이 무엇이 끝났고 무엇이 안 끝났는지 알 수 없다.
  - 근거: 2026-08-15 씬 라이브러리에서 같은 계열이 **두 번** 났다. ① 개념어가 목록에 없으면 저장은 되는데 조회가 영원히 안 됐다 — 파일명이 색인 정규식 밖이라 다음 편이 같은 그림을 다시 생성했고 크레딧이 조용히 샜다. ② 사람이 읽는 색인 문서에 특정 제목이 없으면 표 한 줄 추가가 예외 없이 건너뛰어졌다. 둘 다 기계 판독본은 갱신돼 "등록됐다"로 보였다.
- **결함을 잡는 순서는 4층이다 (2026-08-25 신설).** ① **기본값·구조**로 못 나게 할 수 있나 → ② 안 되면 **생성 단계**에서 막을 수 있나 → ③ 안 되면 **검사**로 잡는다 → ④ 검사도 못 하면 **사람이 보되 «못 잡는다»고 적는다.** **③으로 먼저 가지 않는다** — 검사는 만들기 쉽고 «했다»가 눈에 보이지만, 역검증·거짓 경보·검사 헛돎 점검을 계속 지고 간다. ①로 닫을 수 있는 것을 ③에 두는 것이 가장 비싼 선택이다. **④가 조문의 절반이다** — 못 잡는 것을 못 잡는다고 적지 않으면 ③이 완전한 척하게 되고 위 «감지 장치» 조항에 걸린다.
  - 근거: 2026-08-22 하루에 셋이 각각 다른 층에 앉았다 — 발행 후 뒷정리·PS1 인코딩은 ③(사람이 인스타에서 발행하고 파일마다 지켜야 해서 기본값으로 옮길 수 없다), `webshot` 주소 바는 `bar=False` 한 줄(①)로 **검사 자체가 필요 없어졌다.** 같은 계열: `logo_reference()` 디코드 가드(참조를 얻는 유일한 경로라 ①), §3 pre-commit 훅(조항 → 장치).
  - **검사가 틀린 것을 요구하고 있으면 산출물보다 검사부터 고친다.** 근거: 2026-08-23 ep28 게이트 `REQUIRED` 가 셀프 언급 문장을 강제해 **문장만 지우면 FAIL** 나는 구조였다 — 순서는 게이트 개정 → 문장 제거.
- **실물을 조회할 수 있는 것은 실물이 정본이고 문서는 조회 결과다 (2026-08-25 신설).** 문서가 실물과 다르면 **문서가 틀린 것**이고, 문서를 읽고 실물을 판정하지 않는다. 조회 절차가 있는 항목은 조문이 그 절차를 가리킨다 — 스케줄 트리거는 `Get-ScheduledTask` (`docs\schedule-task-registration.md`), 인계 필드 목록은 `departments\marketing\handoff_schema.py`, 배포·머지 상태는 `git fetch` 뒤의 `origin/main`.
  - 근거 — 나흘에 네 번, 한 뿌리: ① §4 현황판이 실값(12:30)과 사흘 어긋나 8/23 ops-auditor 가 «미실행»으로 오독 ② 백로그 «WakeToRun 적용 완료» vs 실물 4/4 `False`(8/25) ③ 로컬 main 이 뒤처져 **머지된 킷 PR 을 «미배포»로 오보**(8/24) ④ content-scout 정의의 필드 목록과 실제 인계 스키마가 갈려 정의가 그것을 몰랐다(8/25).
- **«공식 이미지 그대로 인용»의 조작적 정의 (2026-08-28 신설).** 「그대로」는 서술이 아니라 **원본 대비 화소 손실 0** 이다. 판정 기준은 «자르지 않았다»고 적었는가가 아니라 **원본 화소가 한 점도 빠지거나 가려지지 않았는가**다.
  - **허용** — ① 판 비율을 맞추는 **무손실 여백**(원본 화소는 전부 남고 바깥에 여백만 붙는다) ② 포맷·컨테이너 변환 ③ 원본 **2장 이상을 각각 통째로** 병치(각 장이 온전할 것 · 라벨은 판 사이 흰 띠에만).
  - **불허** — ① 크롭 ② 확대 재배치 ③ **원본 위 라벨 덧씌우기.** 셋 다 원본 화소를 지우거나 가린다.
  - **적용 범위는 «공식 에셋을 인용하는 자리»뿐이다.** 우리가 직접 찍은 캡처의 크롭 규칙(원본 파일 보존·의미 불변·반대 맥락 삭제 금지·«어디를 왜 잘랐는지» 보고)은 **그대로 살아 있고 이 조항이 대체하지 않는다.** 자회사 정본(SKILL §6 ⓑ «공식 사이트·X 의 이미지·영상 에셋은 그대로 인용한다») 문안 정합은 §1.5 대로 자회사 트랙 안건이다.
  - 🔴 **지금 이 조항은 §0 «4층» 의 ④다 — 검사 장치가 없다.** 게이트는 덱에 실린 이미지를 `_official\` 원본과 대조하지 않는다(2026-08-28 실측: `epcheck.py`·`cardcheck.py` 에 해시·화소 대조 0건이고 `official` 은 `OFFICIAL_VIDEO` 선언 검사뿐이다). **그래서 «못 잡는다»고 여기에 적는다.** 장치화 안건은 `docs\clause-backlog.md` **C-8-1**.
  - 근거 실측 (2026-08-28 · ep38): 판 처리 수치가 **발행팩 산문에만** 있었다 — «원본을 자르지 않고 흰 여백으로 판 비율(1.392:1)에 맞췄다» · «그대로 넘기면 «덮기»가 좌우 139 화소씩 깎는다» · `1280x920` · `836폭`. 그 좌표를 만든 `_plate.py`·`_beforeafter.py` 는 **`workshop\` 전체가 `.gitignore`** 라 커밋된 코드에 없다. 「그대로」를 산문으로 주장하고 그 주장을 잴 자리는 어디에도 없는 상태였다.
- **«다음에 정한다»로 미룬 것은 목록 파일에 적는다 (2026-08-27 신설).** 인프라 정비는 `docs\infra-backlog.md`, **조문 안건은 `docs\clause-backlog.md`** 가 정본이다. 발행팩·PR 본문·메모리에만 적힌 «다음 회차 대상»은 **그 작업이 끝나는 순간 아무도 다시 열지 않는다** — §3 «조건·트리거를 적으면 감시처도 같이 적는다» 가 가리키는 그 결함이고, 미루는 쪽이 감시처를 함께 적지 않으면 미룬 것이 아니라 버린 것이다. **조문 회차를 여는 사람은 그 파일부터 읽는다.**

## 1. 조직도

| 부서 | 에이전트 | 등급 | 담당 |
|---|---|---|---|
| 운영팀 | ops-auditor | A | JJ-Brain vault 건강 감사, 백업 검증 |
| 마케팅팀 | content-scout | B | 토망치랩 소재 조사 → 카드 제안 |
| 영업팀 | job-scout | B | AX/AI 엔지니어 채용 공고 발굴 → 요약 |
| 개발팀 | implementer / verifier (글로벌 상속) | B | 구현/검증 분리, adversarial cross-validation |
| 감리 | 교차검증 codex | A | 타모델 감리. 리포트 vs 에이전트 정의 대조 — read-only, 본문 미수정 append 전용 |

- 개발팀 에이전트는 글로벌 정의를 상속한다. 이 레포에 중복 정의하지 않는다.
- 부서별 상세 규약은 departments/<부서>/ 에 둔다. 에이전트는 자기 부서 규약을 먼저 읽고 시작한다.

## 1.5 자회사

자회사는 본사와 정본이 분리된다. 본사 에이전트는 자회사 정본을 **읽되 수정하지 않는다.**

| 자회사 | 정본 | 본사 관할 | 상태 |
|---|---|---|---|
| 토망치랩 | `~\.claude\skills\tomangchi\SKILL.md` | §5.5 아침 스캔 **1~3-1단계**(수집·검증·판정·스캔로그·제안) | 가동 |

- **정본 수정 금지.** 충돌·모순을 발견하면 고치지 말고 리포트에 "확인 필요"로 보고한다.
- 컨펌·제작·발행은 본사 관할 밖 — 자회사 트랙에서 JJ가 진행한다.

## 2. 작업 디렉토리 (출장지)

| 대상 | 경로 | 권한 |
|---|---|---|
| 작업장 | C:\Users\ojaej\orca\jj-company | 읽기/쓰기. JJ가 개발하는 곳. **본 트리는 main 고정 · 커밋 금지** — 작업은 worktree에서 (§3) |
| 운영 서버 | C:\Users\ojaej\jj-company | 스케줄러 CWD. **main 전용 · 사람 직접 수정 금지** (git pull 로만 갱신) |
| 세컨드브레인 | C:\Obsidian.JJ\JJ-Brain | **읽기 전용** — 읽기는 해당 스케줄 스크립트의 `--add-dir` 로만 부여 (전역 허용 금지) |
| 토망치랩 작업 폴더 | C:\Users\ojaej\orca\tomangchi-lab.github.io\workshop | 읽기 전용 — **단, `workshop\스캔로그\` 는 content-scout 쓰기 허용** (아래 예외 조항) |
| 블로그 | OhjaejunO.github.io 로컬 클론 | 읽기 전용 (초안은 reports/에) |

- 출장지 파일을 직접 수정하는 에이전트는 없다. 모든 산출물은 운영 서버 reports/ 에 쓴다.
- **출장지 쓰기 예외 1건 (content-scout 한정)**: `...\tomangchi-lab.github.io\workshop\스캔로그\` 에만 쓴다. 토망치랩 SKILL.md §5.5-3-1 아침 스캔 로그가 그 폴더 정본이라 우회 산출이 불가능하기 때문이다. 조건 — ① 해당 스케줄 스크립트의 `--add-dir` 로만 부여(전역 허용 금지) ② 기존 내용 수정·삭제 금지, 맨 아래 append 만 ③ 그 외 workshop 하위 폴더와 다른 출장지 쓰기는 계속 금지.
- **전달 드라이브의 «옛 판본 덱 파일» 삭제는 에이전트가 한다 (2026-08-28 JJ 상설 승인).** 종전에는 드라이브 삭제를 전부 사람 자리로 뒀는데, **덱 번호가 밀릴 때마다 옛 번호 파일이 남는 구조**라 사람이 매 회차 같은 일을 하게 됐다(2026-08-28 한 세션에 두 번·잔존 16개). 도구는 `scripts\stale_delivery.py --delete`.
  - **승인 범위는 좁다** — 전달 대상 폴더 안의 **덱 파일(번호로 시작하는 `.png`·`.mp4`) 중 이번 덱에 없는 것**뿐이다. 다른 파일·다른 폴더·다른 드라이브 동작은 **계속 C등급(사람 자리)**이다.
  - **안전 조건 셋을 코드가 건다** — ① 대상 확장자·이름 꼴 제한 ② 그 편의 **새 파일 누락이 0일 때만** 지운다(누락이 있으면 «전달이 덜 된 것»이라 지우면 덱에 구멍이 난다) ③ 원본이 편 폴더에 있어 **재전달로 복구된다.**
  - 🔴 **목록을 재사용하지 않는다.** 덱이 바뀌면 지울 목록도 바뀐다 — **지우기 직전에 다시 훑는다.**
  - **이건 «되돌림 비용»이 낮아서 내린 판정이다** — §0 의 가르는 기준 그대로다. 원본이 남아 있고 재전달 한 번으로 복구되므로 C등급의 이유가 성립하지 않는다. **근본 해결은 파일명을 내용 기반으로 바꾸는 것**이고(`docs\infra-backlog.md` 10번 ①), 그때 이 조항도 같이 폐기한다.
- **등급 경계 (content-scout)**: 토망치랩 SKILL.md §5.5의 1~3-1단계(수집·검증·판정·스캔로그·제안)까지만 자동화한다. 컨펌·캡처·카드 제작·발행은 사람이 한다. SKILL.md 는 에이전트가 수정하지 않는다 (개정은 토망치랩 트랙에서 JJ가 진행).
- 코드/정관 변경은 작업장에서만 한다. 운영 서버에서 직접 편집하면 다음 pull 에서 충돌로 스케줄이 멈춘다.
- vault 경로는 settings.json deny rules로 쓰기 차단되어 있다. 우회 금지.
  - **(Edit 규칙만 유효 — Write 규칙은 파일 권한 검사에 안 걸림)** 경로 차단은 반드시 `Edit(경로)` 로 적는다. `Write(경로)` 는 무시된다.
  - settings.json 은 BOM 없는 UTF-8 로 저장한다. BOM 이 있으면 파일 전체가 파싱 실패해 deny rule 이 전부 무력화된다. (`.claude/agents/*.md` 프론트매터도 동일)
- **보안 알림의 판정은 `docs\security-review-log.md` 원장에 남긴다 (2026-08-25 신설).** 자동 보안 리뷰·deny 발동·스캐너 경고는 **먼저 원장을 보고**, 없으면 조사한 뒤 «일자·출처·판정(오탐/정탐/확인 불가)·근거·조치» 한 줄을 더한다. 근거: 8/25 자동 리뷰가 `permissions.deny` 12건을 allow 로 오독했고, 기록 자리가 없어 **두 세션이 같은 조사를 두 번**(13:24·13:35) 했다.

## 3. Git 규칙

- main 직접 커밋 금지. **worktree에서 작업 브랜치를 파고** → PR → `--assignee OhjaejunO --merge --admin`
  - `OJJ` 상설 브랜치 방식은 **폐기(2026-08-15)**. 본 트리를 OJJ에 두고 쓰면 병렬 세션이 HEAD를 옮기는 순간 아래와 똑같은 사고가 난다 — 다른 레포에서 이미 두 번 겪은 그것이다. **예외를 두면 규칙이 둘이 되고, 둘이면 지켜지지 않는다.**
- **여러 세션이 공유하는 레포는 `git worktree` 로만 분기한다.** 공유 작업 트리에서 `git checkout -b` 를 하지 않는다. 대상: `tomangchi-skill`, `tomangchi-lab.github.io`, `jj-company`, `content-ops`.
  - `content-ops` 추가 근거 — 2026-08-15 실제 발생. 한 세션이 미커밋 상태로 작업 중일 때 다른 세션이 `checkout -b` 를 했고, **그 미커밋 변경이 새 브랜치로 딸려가 남의 브랜치 이름으로 커밋됐다.** 되돌리는 과정에서 작업이 사라진 것처럼 보이기까지 했다(reflog 로 추적해 fast-forward 로 복구, 잃은 커밋은 없었다). 조항에 없었을 뿐 위험은 같았다.
  - 이유는 **HEAD 가 공유 자원**이기 때문이다. 한 세션이 브랜치를 옮기면 그 순간 다른 세션의 커밋이 엉뚱한 브랜치에 얹힌다 — 2026-08-15 실제 발생(병렬 세션의 v3.5.1 커밋이 의도한 브랜치가 아닌 곳으로 갈 뻔했다).
  - 작업이 끝나면 `git worktree remove` 로 정리하고 **`git fetch origin` 으로 원격을 당겨 둔다.** 로컬 `main` 이 뒤처진 채 남으면 다음 세션이 낡은 판본을 정본으로 읽는다.
    - **조항이 있었는데 두 번 났다 (2026-08-25 장치화).** 8/22 3커밋·8/24 2커밋 뒤처진 채 **머지된 킷 PR 을 «미배포»로 여러 번 오보**했고, 그 혼란 속의 `git stash` 가 당일 배포본을 옛 판으로 되돌렸다(라이브 전 발견). 그래서 `check-repo-guard.ps1` 이 **fetch 한 뒤 로컬 main 이 `origin/main` 뒤에 있으면 FAIL** 한다(역검증: `main~1` 이 뒤처짐으로 읽히는지). **세션은 착수 시 이 검사기를 돌린다** — 뒤처져 있으면 `git pull --ff-only origin main` 부터.
  - **같은 목표로 두 세션이 돌지 않게 착수 시 클레임을 건다 (2026-08-25 신설).** worktree 는 **HEAD 공유**만 막고 «같은 일 중복»은 못 막는다. `powershell -File scripts\claim.ps1 -Topic <주제> -Goal "<한 줄>"` 이 `logs\claims\<주제>.claim` 을 만들고, **이미 있으면 거부하며 누가 언제 잡았는지 보여준다.** 끝나면 `-Release`, 현황은 `-List`, 역검증은 `-SelfTest`(중복 거부·해제·재획득 양쪽). `logs\` 라 커밋되지 않는다 — 세션은 한 기계에서 돌므로 로컬로 족하다.
    - 근거: 8/25 본 트리에 **다른 세션의 `handoff_schema.py` 판본**이 untracked 로 남아 PR #70 판본과 갈렸고(두 세션이 같은 목표로 돌았다), 같은 날 두 세션이 `verify.py` 를 각자 고치게 돼 사람이 «이번 세션은 pre-flight 절만»으로 피했다. 8/15 content-ops 사고도 같은 유형이다.
  - **한쪽만 지켜서는 소용없다.** 두 세션이 같은 레포를 동시에 만진 것이 2026-08-15 사고의 원인이었고, **그 사고를 고치는 작업에서 같은 일이 재현됐다**(두 세션이 같은 목적의 스크립트를 동시에 신규 생성). 규율이 아니라 이 조항으로 막는다.
  - **조항만으로는 또 규율이다 — 훅으로 막는다.** `scripts\githooks\pre-commit` 이 **본 트리에서의 커밋을 거부**하고 worktree는 통과시킨다. 판별은 `git rev-parse --git-dir` — 링크된 worktree는 `.git/worktrees/…` 를 돌려준다. `post-checkout` 은 본 트리가 main을 벗어나면 경고한다(git에 `pre-checkout` 훅이 없어 차단은 못 한다).
  - **거부 메시지에 대안을 같이 준다.** 막기만 하고 길을 안 알려주면 세션이 우회로(`--no-verify`)를 찾는다. 훅 출력에 worktree 생성·정리 명령 전체를 넣어 뒀고, 어떤 클라이언트로 git을 부르든 읽히도록 **ASCII 전용**으로 쓴다.
  - 훅은 `core.hooksPath` 가 가리켜야 돌아간다. **이 설정은 git이 옮겨주지 않는 클론별 설정이라, 없으면 가드가 조용히 사라진다.** 그래서 `scripts\check-repo-guard.ps1` 이 설정·훅 존재·본 트리 브랜치를 확인하고 **§0 역검증**까지 한다 — 본 트리에서 훅을 실제로 실행해 거부되는지, 링크된 worktree에서는 통과하는지 **양쪽**을 본다(한쪽만 보면 전부 거부하는 훅도 정상으로 보인다). **링크 worktree가 없으면 검사기가 임시로 만들어 검증하고 지운다** — `미검증`으로 남기면 다음 사람이 STATUS 줄만 보고 통과로 읽는다. 설치: `powershell -File scripts\check-repo-guard.ps1 -Fix`
- 커밋 메시지: conventional prefix(영어) + 한국어 설명. 100바이트 이내.
- 리포트(reports/)는 로컬 전용 — 민감 정보 포함 가능성으로 커밋 금지 (.gitignore 유지).
- logs/ 는 커밋하지 않는다 (.gitignore).

## 4. 스케줄링 규약

- 헤드리스 실행: `claude -p "<프롬프트>"`. claude 바이너리는 전체 경로로 지정 (스케줄러는 PATH 최소 상태).
- CWD는 항상 `C:\Users\ojaej\jj-company`.
- stdout/stderr를 `logs\scheduled\<작업명>_<yyyyMMdd>.log` 로 리다이렉트.
- 리포트는 `reports\<yyyy-MM-dd>_<작업명>.md`.
- 실행 결과 마지막 줄에 `STATUS: OK` 또는 `STATUS: FAIL <사유>`.
  - **예외**: 교차검증(codex) 섹션은 `STATUS` 줄 **뒤에 append** 된다. STATUS 판정은 감리 섹션을 제외한 본문의 마지막 `^STATUS:` 줄을 기준으로 한다. (스케줄 스크립트의 판정 로직이 이미 파일의 마지막 `^STATUS:` 줄을 읽으므로 감리 섹션이 뒤에 붙어도 판정은 정상 동작한다 — 실증 완료)
- **`STATUS` 의 뜻은 «작업을 끝까지 수행했는가»다 — 발견의 심각도가 아니다 (2026-08-25 신설, 전 스케줄 작업 공통).** 🔴 가 몇 개든 리포트를 다 썼으면 `OK`. `FAIL` 은 데이터를 못 읽었거나 리포트를 못 쓴 것처럼 **작업 자체가 성립하지 않았을 때**다. 반대 방향도 같다 — **절차를 건너뛰고 `OK` 를 찍지 않는다.** 승인 거부·소스 미조회로 단계가 빠졌으면 `STATUS: OK (부분: <빠진 단계>)` 로 적어 판정 스크립트는 통과시키되 사람이 읽게 한다. 근거: 8/25 ops-auditor 가 합성 RED 발견에 `FAIL` 을 찍었고, 8/24 tomangchi-scout 는 승인 거부 9건으로 절차를 건너뛰고도 `OK` 였다 — 한쪽은 너무 엄하고 한쪽은 너무 관대했다.
- **`--allowed-tools` 는 게이트가 아니다 (2026-08-25 실측).** `--permission-mode acceptEdits` 에서 Edit·Write·Read·Glob·Grep 은 목록과 무관하게 통과한다 — 목록이 가르는 것은 **Bash 접두 패턴과 Web 도구뿐**이다. 근거: 8/22~25 트랜스크립트 18파일 전수에서 목록 밖 `Edit` 9·`Write` 16·`rm`·`date`·`grep`·`ls`·`wc` 가 실행됐다. 진짜 게이트가 필요하면 `--permission-mode default` + `Edit(경로/**)` 허용으로 간다.
  - **에이전트를 쓰는 스케줄 셋 전부 `default` 로 옮겼다 (2026-08-28).** vault-health 시범은 8/26·8/28 두 회차가 거부로 인한 실패 없이 끝났다(8/27 은 `skill-sync` 로 에이전트까지 가지 못해 증거가 아니다). 확대 대상은 tomangchi-scout·job-scout 이고, **Bash 목록은 종전 그대로 둔다** — 두 작업이 확인용으로 쓰던 `tail`·`ls`·`git status` 는 `acceptEdits` 에서 **이미 거부되고 있었는데도 회차가 정상 종료**했다. 넓히지 않는 근거는 추측이 아니라 그 기록이다.
  - 🔴 **깃발은 주장이고, 증거는 매 회차의 프로브다.** `scripts\permission_probe.py` 가 에이전트보다 먼저 **실제로 한 번 막혀 본다** — 목록 밖(`docs\_probe_should_fail.md`) 쓰기는 거부되어야 하고 목록 안(`reports\`) 쓰기는 성공해야 한다. **판정은 파일의 존재이지 봇의 말이 아니다.** 한쪽만 보면 «전부 거부하는 상태»가 정상으로 보이므로 **양방향이다**(정관 §0 역검증, `check-repo-guard.ps1` 과 같은 꼴). 실패하면 그 회차는 `STATUS: FAIL probe …` 로 **중단한다** — 게이트가 열린 채로 에이전트를 띄우는 것이 막으려던 바로 그것이다. 프로브 자신의 역검증은 `--self-test`(느슨한 목록에서 FAIL 이 나는지).
- A/B등급만 스케줄 가능. C등급 스케줄 등록 금지.
- lock 파일 `logs\<작업명>.lock` 존재 시 즉시 종료.
- 모든 스케줄 작업은 실행 시작 시 **두 가지를 최신 main과 동기화**한 뒤 진행한다. 어느 쪽이든 실패하면 즉시 종료한다.
  1. **운영 서버** — `git pull origin main`. 실패 시 `STATUS: FAIL git-sync`.
  2. **스킬 라이브 정본** — `scripts\deploy-skill.ps1` (라이브 ← `origin/main`). 실패 시 `STATUS: FAIL skill-sync`.
  - `~\.claude\skills\tomangchi` 는 **레포로 향하는 링크가 아니라 `origin/main` 의 사본**이다 (2026-08-15 개편). 링크였을 때는 체크아웃된 브랜치가 곧 라이브 정본이라, 한 세션이 feature 브랜치에 두고 나오면 다른 세션·스케줄이 **미승인 규칙으로 조용히 돌았다.** 사본이므로 **아무도 밀어 넣지 않으면 갱신되지 않는다** — 그래서 동기화를 스케줄 선행 단계로 못박는다.
  - 실행에 쓰인 리비전은 `scripts\skill-version.ps1` 이 로그에 남긴다. 근거는 라이브의 `.deployed` 스탬프이며 소스 레포 HEAD가 아니다.

- **고친 것이 언제부터 도는지 확인하고 검증한다 (2026-08-18 신설).** 고쳤다고 그 순간부터 도는 것이 아니다. **이미 돌고 있던 것은 옛 판본으로 끝난다.**
  - **스케줄 스크립트를 고친 날은 그날 실행분이 옛 판본으로 돌 수 있다.** PowerShell 은 시작할 때 파일을 통째로 읽으므로, 실행 중에 `git pull` 로 새 판본을 받아도 **그 회차는 옛 코드로 끝까지 간다.**
    → 고친 뒤 검증하려면 **프로세스를 새로 띄우거나 다음 실행분을 본다.** 돌고 있는 것을 보고 «반영됐다»고 적지 마라.
  - **서브에이전트 정의(`.claude/agents/*.md`) 변경은 반드시 «새 세션 기동»으로 검증한다.** 정의는 **세션 시작 시점에 고정**되므로, 머지 직후 같은 세션에서 그 에이전트를 띄우면 **구정의로 돈다.** 도구를 뺐는데도 그대로 쓰이는 것처럼 보인다.
  - 근거: 2026-08-18 ops-auditor 에서 `Bash` 를 뺀 개편을 검증하다 **두 번 연속** 이 함정에 빠졌다. ① 같은 세션에서 띄우니 구정의(`Bash` 보유)로 돌았고 ② 래퍼를 수동 실행하니 `git pull` 로 새 판본을 받고도 **데이터 수집 단계를 건너뛴 옛 코드**가 끝까지 갔다. 새 세션·새 프로세스로 다시 돌린 3회차에서야 `도구 부재`와 `STATUS: OK` 가 같이 확인됐다.
  - **이것은 §0 역검증 조항의 실행 조건이다.** 무엇을 재는지 이전에 **언제 재는지**가 틀리면, 고쳐 놓고도 «안 고쳐졌다»로 읽거나 그 반대로 읽는다.

- **동시 기동은 사고다 — 시작 지연을 분산하고 pull 은 재시도한다 (2026-08-19 신설).** 네 작업 모두 «놓친 실행은 가능해지면 즉시»(StartWhenAvailable)라, 절전·재부팅 뒤엔 **같은 초에 넷이 함께 뜬다.** 그러면 한 작업 트리에서 `git pull` 셋이 경쟁해 하나만 이기고 나머지는 `incorrect old value provided` 로 죽는다.
  - 시간 트리거엔 결정적 지연이 없어(`RandomDelay` 뿐) **지연은 래퍼 안의 상수**다 — drift 0 / vault 2 / scout 4 / job 6분. 매 실행에 적용되지만 예정 시각이 30분 간격이라 무해하다. `git pull` 은 `scripts\git-sync.ps1` 로 **최대 3회(5초 간격)** 시도하고 모든 시도를 로그에 남긴다 — 재시도로 살아난 실행도 «살아났다»가 보이게.
  - 근거: 2026-08-19. 02:12 절전 → 11:40 기상 → 11:46:15 넷 동시 기동, ops-auditor·job-scout 가 `FAIL git-sync` 로 리포트 없이 끝났다. 스크래치 클론에서 재현하니 재시도 없이 12회 중 7회 같은 오류, 재시도 2회로 12/12 성공.
- **네이티브 exe 에 넘기는 프롬프트는 `ConvertTo-NativeArg` 로 감싼다 (2026-08-19 신설).** PS 5.1 은 인자 속 `"` 를 이스케이프하지 않아 **`claude -p` 프롬프트가 첫 따옴표에서 잘리고 뒤는 버려진다.** 세 프롬프트 파일 모두 앞줄에 `"확인 불가"` 류가 있어 스케줄 에이전트 셋이 **잘린 프롬프트로 돌고 있었고 아무 로그도 없었다** — 에이전트 정의 파일 덕에 겉으로는 동작해 보였다. `scripts\native-arg.ps1`. 실측: `alpha "beta gamma" delta` 의 마지막 단어를 물으니 `beta`, 감싸니 `delta`.
- **인증·마운트는 아침 회차가 점검한다 (2026-08-27 신설).** `scripts\auth_check.py` 가 morning-vault-health 래퍼에서 **에이전트보다 먼저** 돌아 `gh auth status` 와 **구글 드라이브 마운트(«내 드라이브» 경로 ∧ `GoogleDriveFS` 프로세스)** 를 조회해 `logs\audit-data\auth_<날짜>.txt` 에 쓴다. `AUTH_ALL_OK=no` 면 리포트에 **🔴** 로 오르고 «JJ가 할 일» 에 복구 명령이 붙는다. **둘 다 보는 이유** — 프로세스가 죽어도 경로가 잠깐 남고, 프로세스만 살아 있고 마운트가 아직 안 붙은 순간도 있다. 한쪽만 보면 둘 다 놓친다.
  - 근거: 2026-08-27 **하루에 둘이 같이 만료됐다.** `gh` 토큰 만료로 push·PR 이 막혀 킷 배포가 멈췄고, 구글 드라이브 데스크톱 미실행로 `deliver.py` 가 «'내 드라이브'를 찾지 못했다» 로 멈춰 폰 전달이 안 됐다(마운트가 C: 하나뿐이었다). **둘 다 «발행 직전에 발견»되는 종류라 그때는 이미 급하다.**
  - 🔴 **이것으로 `STATUS` 를 내리지 않는다** — §4 «STATUS 의 뜻은 작업을 끝까지 수행했는가»다. 인증이 만료됐다고 vault 감사가 성립하지 않는 것은 아니다. 사실만 남기고 판정은 리포트가 한다.
  - 조회만 한다(A등급). **로그인·드라이브 실행은 사람이 한다** — 스크립트는 명령을 알려 줄 뿐 실행하지 않는다.
  - **점검은 스킬 동기화보다 «앞»이다 (2026-08-28 개정).** 종전엔 에이전트 데이터 수집 자리에 있어 한 단계 늦었다 — 8/27 만료된 `gh` 토큰이 `deploy-skill` 의 `git fetch` 를 죽였고, 래퍼는 `STATUS: FAIL skill-sync` 로 멈췄으며, **그 원인을 말해 줄 검사는 그 뒤에 있어서 돌지 못했다.** 진단이 자기가 진단할 단계 뒤에 있으면 안 된다.
- **미기동은 다음 날 아침 리포트에 오른다 (2026-08-28 신설).** 8/27 에 세 작업이 같은 인증 만료로 `skill-sync` 에서 죽었는데 **자기 로그에만 남아 다음 날까지 아무도 몰랐다.** `run_audit.py` 는 «STATUS 줄이 없는» 회차만 봤고 «STATUS: FAIL 인» 회차는 보지 않았다. 이제 `RUNS_FAILED=<작업>@<날짜>:<사유>` 로 싣고 `RUNS_VERDICT=RED` 를 낸다.
  - 세 겹으로 갈랐다 — ① `deploy-skill` 이 `GIT_TERMINAL_PROMPT=0` 으로 **바로 실패**하고(종전엔 `/dev/tty` 를 더듬다 엉뚱한 오류를 냈다) ② 인증 꼴이면 재시도 없이 «credentials» 로 이름 붙이고, 그 밖의 실패는 5초 간격 3회 재시도하며 ③ 래퍼가 `STATUS: FAIL skill-sync-auth` 로 적어 다음 날 리포트에 사유가 그대로 실린다.
  - 🔴 **`STATUS: OK (부분: …)` 는 여기 오지 않는다** — §4 가 부분 완주를 실패가 아니라고 못박았다. `run_audit._selftest()` 의 네 케이스 중 하나가 그것을 지킨다(역검증: 판정 함수를 가짜로 바꾸면 자체 검사가 걸린다).
- **content-scout 소스 목록(1-1단계)은 래퍼가 조회한다 (2026-08-19 신설).** 스케줄 allowlist 에 `yt-dlp` 가 없어 이 단계는 구조적으로 불가능했고 리포트에 흔적 없이 건너뛰어졌다. ops-auditor 와 같은 «래퍼 선실행» — `scripts\source_watch.py` 가 `config.md` 소스 표를 실행 시점에 파싱해 YouTube 는 yt-dlp 로 조회하고 `logs\scout-data\sources_<날짜>.txt` 에 쓴다. Threads·Instagram 은 yt-dlp 미지원이라 `UNSUPPORTED` 로 남겨 에이전트가 WebFetch 하거나 «미확인»으로 적는다. **0건·«신작 없음»으로 뭉개지 않는다.**

### 등록된 스케줄 (현황판)

| 작업 | 에이전트 | 주기 | 상태 |
|---|---|---|---|
| morning-vault-health | ops-auditor | 평일 12:30 | 가동 |
| tomangchi-scout | content-scout | 매일 08:00 | 가동 |
| job-scout | job-scout | 매일 08:30 | 가동 |
| skill-drift-audit | (에이전트 없음 — 결정적 비교) | 매일 12:30 | 가동 |
| hermes-event-watch | 헤르메스 `sagun` (감지는 `event_watch.py`) | 매일 07:40 | 시범 8/26~28 |
| workshop-backup | (에이전트 없음 — 결정적 압축·대조) | 주 1회 일요일 13:00 + **편 발행 직후** | **등록 대기 — 관리자 셸 필요**(`docs\schedule-task-registration.md` §2-1) |

- **정본은 실값이고 이 표는 조회 결과다** (2026-08-25). `Get-ScheduledTask -TaskPath '\JJ\'` 의 트리거가 정본이며, 이 표가 실값과 다르면 표가 틀린 것이다 — 8/22 재등록으로 vault·drift 가 12:30 으로 옮겨진 뒤 이 표는 사흘간 07:30/07:00 을 가리키고 있었고, 8/23 ops-auditor 가 그것을 읽고 «미실행»으로 오독했다. 재등록·트리거 변경 뒤에는 `docs\schedule-task-registration.md` 절차대로 실값을 재조회해 이 표를 맞춘다.

- **skill-drift-audit 은 에이전트를 쓰지 않는다.** 두 폴더를 비교하고 사본의 자립 검증을 돌리는 결정적 작업이라 정답이 있다 — §0이 추측을 금하는 자리에 모델을 넣을 이유가 없다. `scripts\skill-drift-audit.ps1` 이 `skill_drift_audit.py` 를 직접 부른다.
  - **이 작업은 한시적이다.** 브랜드 스크립트 정본이 하나로 합쳐지면(`docs\plan-brand-assets-move.md`) 비교 대상이 사라져 **함께 폐기한다.** 감사가 필요 없어지는 것이 그 이전 작업의 목적이다.

## 5. 리포트 형식

공통 헤더: 부서/작업명/날짜, 실행 시각·소요, 등급, 결론 요약 3줄 이내.
- 결론 먼저, 근거는 뒤에.
- B등급 제안은 "제안 → 근거 → JJ가 할 일" 3단 구성.
- 문제 심각도: 🔴 즉시 / 🟡 이번 주 / ⚪ 참고.

## 6. 금지 사항

- vault, C:\토망치, 블로그 레포 등 출장지 직접 수정
- 외부 발송 (이메일, DM, SNS 게시, 댓글)
  - **예외는 Threads 텍스트 스레드 하나뿐이다 (2026-08-28 · §0 «발행은 매체별로 가른다»).** 인스타그램 게시·고정 댓글·DM·이메일은 **그대로 금지**다. 예외가 서려면 `publish_approval\<ep>.json` 3확인이 먼저이고, 그 장치는 아직 없다.
  - 🔴 **에이전트가 `publish_approval\` 을 만들거나·고치거나·서명하는 것은 금지다.** 자기 승인은 승인이 아니다.
- main 커밋, force push
- 시크릿/토큰을 리포트나 로그에 기록
- deny rules 우회 시도
- 확인되지 않은 정보를 사실처럼 보고
- **한글 포함 파일을 PS 5.1 리다이렉트(`>`, `>>`)로 생성·추가** — PS 5.1 기본 인코딩이 UTF-8이 아니라 파일이 깨진다. 한글 파일 쓰기는 **UTF-8(BOM 없음) 명시 필수**: `Set-Content -Encoding utf8` 또는 `[IO.File]::WriteAllText($p, $t, (New-Object Text.UTF8Encoding $false))`. Bash 툴 heredoc(`cat > f <<'EOF'`)과 Write/Edit 툴은 UTF-8 그대로 쓰므로 **인코딩에는** 안전하다.
  - **단, 백슬래시·정규식·따옴표·em dash 가 든 코드는 heredoc 으로 쓰지 않는다 — Write/Edit 툴로 쓴다 (2026-08-25 신설, 전역).** heredoc 은 내용을 셸이 한 번 더 해석한다. 근거: 8/25 파이썬 정규식의 `\1` 이 `\x01` 로 풀려 백레퍼런스가 깨졌고, **재시도 2회를 포함해 3회 헛돈 뒤** `cat -A` 로 바이트를 찍고서야 보였다. 8/23 에는 `I'm` 의 작은따옴표와 em dash 가 heredoc 과 cp949 콘솔을 같이 깨뜨렸다. 파일에 든 코드가 그대로 도착했는지는 **실행 결과가 아니라 바이트**로 확인한다.
  - 인코딩 점검은 `iconv` 로 하지 마라 — 이 환경에 없어서 **전 파일이 "디코딩 실패"로 오탐**된다. `py -c "open(f,'rb').read().decode('utf-8')"` 로 확인한다.
  - **콘솔에 한글이 깨져 보이는 것과 파일이 깨진 것은 다르다.** 터미널 코드페이지 문제일 수 있으니 파일을 직접 열어 확인한 뒤 판단한다.
