# 열린 PR 원장 — 처분 판정 (2026-08-27)

> **왜 있나.** PR 이 열린 채 잊히면 «머지될 것»으로 읽히다가, 그 사이 정본이 움직여 **조용히 낡거나 되돌리는 변경**이 된다. 이 원장은 전 레포를 훑은 결과와 건별 판정안을 남긴다. **판정 실행(머지·닫기)은 JJ 지목 승인.**
> 조회: `gh pr list -R <repo> --state open`. 대상 레포 5곳 — `jj-company` · `tomangchi-skill` · `content-ops` · `tomangchi-lab.github.io` · `OhjaejunO.github.io`.

## 전수 결과 (2026-08-27 조회)

**열린 PR 은 1건뿐이다.** 나머지 4개 레포는 0건 — 8/25~27 세션들이 만든 PR 은 전부 머지되거나(#72~#84 계열) 닫혔다(#68).

| 레포 | 열린 PR | 판정안 |
|---|---|---|
| `content-ops` | ~~#8~~ | ✅ **종결 2026-08-27** — 충돌 해소 후 머지(`dfe55d8`). 아래 §종결 기록 |
| `jj-company` | 0 | — |
| `tomangchi-skill` | 0 (이번 세션 #84 제외) | — |
| `tomangchi-lab.github.io` | 0 | — |
| `OhjaejunO.github.io` | 0 (#15 머지 완료) | — |

## content-ops #8 — 씬 개념 4종 (`deadline-overlap` · `free-open` · `shutter` · `backup`)

**내용**: ep15(마누스 무료 개방 ↔ 데이터 삭제 창 겹침)용 상황 씬 개념 4종을 `cards/scenes.py` 에 등재. PR 본문에 자체 역검증(키워드 가로채기 실측)이 붙어 있다.

**실측 (2026-08-27 · 로컬 시험 머지 후 확인)**
- 네 개념 **전부 현행 `scenes.py` 에 없다** — 다른 PR 이 대체하지 않았다. 여전히 additive 하고 충돌 없이 머지된다(자동 머지 성공).
- PR 이 스스로 적은 역검증이 그대로 재현된다: `겹침`→`deadline-overlap` · `무료 개방`→`free-open` · `셔터`→`shutter` · `백업`→`backup` · `마감 겹침`→`deadline`(기록된 가로채기) · 없는 개념→`None`.
- 🔴 **그런데 새 충돌이 하나 생겼다.** PR 작성(8/15) 이후 머지된 **`free-lane-more-items`(ep33, PR #19 · 8/26)** 의 첫 키워드가 **`무료 개방`** 이다. `_concept_key` 는 **부분 문자열 매칭 + 먼저 선언된 개념이 이긴다** — 시험 머지 상태에서 `무료 개방` 은 **`free-open` 으로 간다.** 즉 **#8 을 그대로 머지하면 ep33 계열 호출이 조용히 다른 씬을 받는다.**

**판정안 — 🟡 조건부 머지 (충돌 해소 후)**
1. `free-open` 의 키워드에서 **`무료 개방` 을 뺀다**(남는 것: `무료 풀림`·`개방`·`요금 면제`·`공짜`·`free-open`). 또는 `free-lane-more-items` 보다 **뒤에 선언**되도록 순서를 옮긴다. 둘 중 하나면 족하다.
2. 해소 뒤 **양쪽 호출을 다시 잰다** — `무료 개방`→`free-lane-more-items` · `무료 풀림`→`free-open` **둘 다** 확인(한쪽만 보면 반대 가로채기를 못 본다).
3. 그 상태로 머지.

**닫기(폐기)를 권하지 않는 이유**: 네 개념은 서비스와 무관한 **구조**(무료 개방 이벤트 / 창 겹침 / 점검 차단 / 이관 마감)라 재사용 가치가 남아 있고, ep15 는 이미 발행됐지만 개념 라이브러리는 편과 수명이 다르다.

**남은 위험 (적어 둔다)**: `_concept_key` 의 «먼저 선언이 이긴다» 는 **개념이 늘수록 조용한 가로채기가 늘어나는 구조**다. 지금은 PR 본문·주석이 사람에게 경고할 뿐 **기계 검사가 없다.** 개념 등재 시 «새 키워드가 기존 개념에 먼저 걸리는가»를 보는 자체 검사(양방향)를 `scenes.py` 에 두는 것이 근본이다 — 별건 제안.

## 2026-08-28 회차 — 전수 재조회와 처분

| 레포 | PR | 처분 |
|---|---|---|
| `jj-company` | **#81** 셸 가드 «구현 승인» 표기 + #8 종결 기록 | ✅ **머지** `6ac1399` (JJ 지목 승인) |
| `jj-company` | **#82** Threads 첨부 미디어 규칙 | ✅ **머지** `dd1c140` (JJ 지목 승인) |
| `jj-company` | #88 제어문자 등재 + 훑기 검사기 | 🟡 **열린 채 둔다** — 이번 회차 지목 대상이 아니다 |
| `content-ops` | #22 `penny-a-picture` 씬 등재 | ✅ **이미 머지돼 있었다** (`4cb01f4`) — 지시서에는 «머지» 로 적혀 있었으나 실물 확인 결과 8/27 에 이미 들어갔다 |
| `tomangchi-lab.github.io` | #22 ep38 킷 | ✅ **이미 머지돼 있었다** (`a9f6fb8`) |
| `tomangchi-lab.github.io` | **#24** ep38 취소선 항목 정정 | 🟡 **신규 · 머지 대기** |

🔴 **머지 후 실물 확인 (정관 §0)**: `git fetch` 뒤 `origin/main` 에서 두 파일을 직접 읽어 반영을 확인했다 —
`docs/design-bash-escape-guard.md` 3행의 «🟢 구현 승인됨» · `dist_transform.py` 의 `official_sources` 2개소.

🔴 **#82 는 머지 직후 «지금도 옳은가»가 한 번 흔들렸다.** 이 회차에 ep38 의 `shots/` 아래가
**전/후 합성 판**으로 바뀌면서, `official_sources()` 의 전제(«`shots/` 아래 = 공식 원본»)가 깨졌다.
실제로 브리프가 **우리 합성 판 3장을 «공식 원본» 후보로** 냈다. 같은 회차에서 고쳤다 —
편 선언 `ATTACH_OFFICIAL` 우선 + 판 실크기(1080x776) 파일은 «우리 합성 판»으로 표시.
역검증: ep38 3/3 표시 · ep35 0/4 표시(생 캡처는 안 걸린다).

**교훈**: 원장이 적은 «지금도 옳은가»는 **머지 시점만의 질문이 아니다.** 같은 회차의 다른 작업이
전제를 바꾸면 방금 머지한 것이 그 순간 낡는다.

## 2026-08-28 2회차 — 처분 완료

| 레포 | PR | 처분 |
|---|---|---|
| `jj-company` | **#88** 제어문자 등재 + 훑기 검사기 | ✅ **머지** `a1ee1af` |
| `jj-company` | **#89** 원본 재점검 + C-5·C-6 + 첨부 전제 정정 | ✅ **머지** `f7b25ff` |
| `tomangchi-lab.github.io` | **#24** ep38 취소선 정정 | ✅ **머지** `c3d6ed8` |

**순서를 봤다 (정관 §0 조건 4)**: #88 과 #89 가 **둘 다 `docs/infra-backlog.md` 를 만진다.**
#88 은 90행 부근(§우선순위 1), #89 는 파일 끝(§끝난 것 앞)이라 구간이 갈렸다.
**#88 을 먼저** 머지하고 #89 의 `mergeable` 을 다시 조회해(`UNKNOWN` → `MERGEABLE/CLEAN`) 이어 머지했다.
**실물 확인**: `origin/main` 에서 `### 1-2.` 와 `### 8.` 두 항목이 **같이** 들어간 것을 직접 읽었다.

## 운용 규칙 (제안)

- **PR 은 열어 둔 채 7일을 넘기지 않는다.** 넘기면 이 원장에 올리고 «머지 / 닫기 / 다시 연다(재작성)» 중 하나로 판정한다.
- 판정은 **머지 가능 여부가 아니라 «지금도 옳은가»** 로 한다 — MERGEABLE 은 충돌이 없다는 뜻이지 되돌리지 않는다는 뜻이 아니다(정관 §0 머지 조건 4).

## 종결 기록 — content-ops #8 (2026-08-27)

**충돌 해소 → 머지 `dfe55d8`.** JJ 조건부 승인대로 «키워드 제거 → 양방향 재측정 → 충돌 0 확인» 순으로 처리했고, 그 과정에서 **한 번 더 걸렸다.**

1. PR 브랜치에 현행 main 을 머지하니 `scenes.self_test()` 가 **그림자 4건**을 냈다 — `free-open` 이 ep33 `free-lane-more-items` 의 «무료 개방»·«free»·«free-lane» 을 가리고, 별개로 `narrow-door` 의 «preview» 가 ep34 `blueprint-unroll` 의 «architecture-preview» 를 가렸다.
2. 🔴 **넷째는 이 PR 과 무관한 기존 결함이었다** — main 단독 `self_test()` 로도 재현된다. **8/26 PR #19(ep32~34 개념) 이후 아무도 self_test 를 돌리지 않았다는 뜻**이다. 같이 고쳤다(죽은 키워드 제거).
3. 🔴 **«무료 개방» 만 지워서는 안 됐다** — 매칭이 **부분 문자열**이라 남은 «개방» 이 그대로 «무료 개방» 을 삼켰다(1차 수정 실측: 4건 → 1건). «개방» 까지 빼고서야 0건이 됐다. **키워드를 지울 때는 «그 말이 다른 부름말의 부분 문자열인가»까지 본다.**
4. 머지 후 **main 실물 재측정**: 충돌 **0건** · 역검증 픽스처 여전히 잡힘 · 양방향 확인(«무료 개방» → `free-lane-more-items` / «무료 풀림» → `free-open` / «겹침» → `deadline-overlap` / «셔터»·«백업» 정상).

**남은 제안(별건)**: 개념 등재 시 `self_test()` 를 **강제로 통과시키는 자리**가 없다 — PR 본문·주석이 사람에게 맡기고 있어 8/26 처럼 조용히 새 그림자가 생긴다. content-ops 에 커밋 훅이나 CI 한 줄로 붙이는 것이 근본이다.

🔴 **이 절을 쓰다가 같은 함정에 또 걸렸다 (2026-08-27, 이 세션 3회째).** 위 문단들을 `py -c "…"` **큰따옴표** 로 넘겼더니 그 안의 백틱을 셸이 명령으로 실행해 `scenes.self_test()`·`free-open` 같은 **식별자가 빈 자리로 커밋됐다**(커밋 `da56772`). 바로 다음 커밋에서 Write/Edit 도구로 복구했다. 8/27 하루에 세 번(메모리 갱신 → `gh auth login` 재실행 · 이 절) — `docs/design-bash-escape-guard.md` 가 **왜 조항이 아니라 장치여야 하는지**의 세 번째 실측이고, 그래서 그 설계안이 같은 날 «구현 승인» 을 받았다.
