# 열린 PR 원장 — 처분 판정 (2026-08-27)

> **왜 있나.** PR 이 열린 채 잊히면 «머지될 것»으로 읽히다가, 그 사이 정본이 움직여 **조용히 낡거나 되돌리는 변경**이 된다. 이 원장은 전 레포를 훑은 결과와 건별 판정안을 남긴다. **판정 실행(머지·닫기)은 JJ 지목 승인.**
> 조회: `gh pr list -R <repo> --state open`. 대상 레포 5곳 — `jj-company` · `tomangchi-skill` · `content-ops` · `tomangchi-lab.github.io` · `OhjaejunO.github.io`.

## 전수 결과 (2026-08-27 조회)

**열린 PR 은 1건뿐이다.** 나머지 4개 레포는 0건 — 8/25~27 세션들이 만든 PR 은 전부 머지되거나(#72~#84 계열) 닫혔다(#68).

| 레포 | 열린 PR | 판정안 |
|---|---|---|
| `content-ops` | ~~#8~~ | ✅ **종결 2026-08-27** — 충돌 해소 후 머지(`dfe55d8`). 아래 §종결 기록 |
| `jj-company` | 0 | — |
| `tomangchi-skill` | 0 (이번 세션 #84 제외) | — |
| `tomangchi-lab.github.io` | 0 | — |
| `OhjaejunO.github.io` | 0 (#15 머지 완료) | — |

## content-ops #8 — 씬 개념 4종 (`deadline-overlap` · `free-open` · `shutter` · `backup`)

**내용**: ep15(마누스 무료 개방 ↔ 데이터 삭제 창 겹침)용 상황 씬 개념 4종을 `cards/scenes.py` 에 등재. PR 본문에 자체 역검증(키워드 가로채기 실측)이 붙어 있다.

**실측 (2026-08-27 · 로컬 시험 머지 후 확인)**
- 네 개념 **전부 현행 `scenes.py` 에 없다** — 다른 PR 이 대체하지 않았다. 여전히 additive 하고 충돌 없이 머지된다(자동 머지 성공).
- PR 이 스스로 적은 역검증이 그대로 재현된다: `겹침`→`deadline-overlap` · `무료 개방`→`free-open` · `셔터`→`shutter` · `백업`→`backup` · `마감 겹침`→`deadline`(기록된 가로채기) · 없는 개념→`None`.
- 🔴 **그런데 새 충돌이 하나 생겼다.** PR 작성(8/15) 이후 머지된 **`free-lane-more-items`(ep33, PR #19 · 8/26)** 의 첫 키워드가 **`무료 개방`** 이다. `_concept_key` 는 **부분 문자열 매칭 + 먼저 선언된 개념이 이긴다** — 시험 머지 상태에서 `무료 개방` 은 **`free-open` 으로 간다.** 즉 **#8 을 그대로 머지하면 ep33 계열 호출이 조용히 다른 씬을 받는다.**

**판정안 — 🟡 조건부 머지 (충돌 해소 후)**
1. `free-open` 의 키워드에서 **`무료 개방` 을 뺀다**(남는 것: `무료 풀림`·`개방`·`요금 면제`·`공짜`·`free-open`). 또는 `free-lane-more-items` 보다 **뒤에 선언**되도록 순서를 옮긴다. 둘 중 하나면 족하다.
2. 해소 뒤 **양쪽 호출을 다시 잰다** — `무료 개방`→`free-lane-more-items` · `무료 풀림`→`free-open` **둘 다** 확인(한쪽만 보면 반대 가로채기를 못 본다).
3. 그 상태로 머지.

**닫기(폐기)를 권하지 않는 이유**: 네 개념은 서비스와 무관한 **구조**(무료 개방 이벤트 / 창 겹침 / 점검 차단 / 이관 마감)라 재사용 가치가 남아 있고, ep15 는 이미 발행됐지만 개념 라이브러리는 편과 수명이 다르다.

**남은 위험 (적어 둔다)**: `_concept_key` 의 «먼저 선언이 이긴다» 는 **개념이 늘수록 조용한 가로채기가 늘어나는 구조**다. 지금은 PR 본문·주석이 사람에게 경고할 뿐 **기계 검사가 없다.** 개념 등재 시 «새 키워드가 기존 개념에 먼저 걸리는가»를 보는 자체 검사(양방향)를 `scenes.py` 에 두는 것이 근본이다 — 별건 제안.

## 운용 규칙 (제안)

- **PR 은 열어 둔 채 7일을 넘기지 않는다.** 넘기면 이 원장에 올리고 «머지 / 닫기 / 다시 연다(재작성)» 중 하나로 판정한다.
- 판정은 **머지 가능 여부가 아니라 «지금도 옳은가»** 로 한다 — MERGEABLE 은 충돌이 없다는 뜻이지 되돌리지 않는다는 뜻이 아니다(정관 §0 머지 조건 4).

## 종결 기록 — content-ops #8 (2026-08-27)

**충돌 해소 → 머지 `dfe55d8`.** JJ 조건부 승인대로 «키워드 제거 → 양방향 재측정 → 충돌 0 확인» 순으로 처리했고, 그 과정에서 **한 번 더 걸렸다.**

1. PR 브랜치에 현행 main 을 머지하니  가 **그림자 4건**을 냈다 —  이 ep33  의 «무료 개방»·«free»·«free-lane» 을 가리고, 별개로  의 «preview» 가 ep34  의 «architecture-preview» 를 가렸다.
2. 🔴 **넷째는 이 PR 과 무관한 기존 결함이었다** — main 단독  로도 재현된다. **8/26 PR #19(ep32~34 개념) 이후 아무도 self_test 를 돌리지 않았다는 뜻**이다. 같이 고쳤다(죽은 키워드 제거).
3. 🔴 **«무료 개방» 만 지워서는 안 됐다** — 매칭이 **부분 문자열**이라 남은 «개방» 이 그대로 «무료 개방» 을 삼켰다(1차 수정 실측: 4건 → 1건). «개방» 까지 빼고서야 0건이 됐다. **키워드를 지울 때는 «그 말이 다른 부름말의 부분 문자열인가»까지 본다.**
4. 머지 후 **main 실물 재측정**: 충돌 **0건** · 역검증 픽스처 여전히 잡힘 · 양방향 확인(«무료 개방»→ / «무료 풀림»→ / «겹침»→ / «셔터»·«백업» 정상).

**남은 제안(별건)**: 개념 등재 시  를 **강제로 통과시키는 자리**가 없다 — PR 본문·주석이 사람에게 맡기고 있어 8/26 처럼 조용히 새 그림자가 생긴다. content-ops 에 커밋 훅이나 CI 한 줄로 붙이는 것이 근본이다.
