# 헤르메스 ① 크로스 모델 검수 — 양식 4 · 층: 감시 · 상태: 설계 (2026-08-26)

**JOB** 게이트(`verify.py`) 통과본을 **다른 모델**이 규격으로 재검수해 «놓친 것 목록» 을 낸다. 게이트 뒤의 2차 눈 — **게이트 대체 불가**(게이트 FAIL 은 이 자리에 오지 않는다).

**TRIGGER** 편 게이트 `STATUS: OK` 직후 사람 호출(SKILL §7 제작 순서 10번과 11번 사이). 스케줄 아님.

**INPUTS** 덱 실물(PNG/MP4) · `caption.txt`·`pinned_comment.txt`(발행 예정 공개 텍스트 — 사본 전달) · **공개 가능한 검수 체크리스트** `docs/workers/checklists/xreview.md`(JJ 승인·20줄 이내). **SKILL 원문·발행팩·검증로그·FACTS 는 투입 금지**(공통 원칙 — 무료 프로바이더 anonymously served). 이 워커는 레포를 읽지 않는다.
- 🔴 **UI 함정 — 봇 생성 시 모델 pin 필수.** Desktop 기본 모델이 Claude 계열이라 pin 없이 만들면 작성 모델과 같은 계열이 검수해 **크로스 모델이 성립하지 않는다.** 프로필 설정에 pin 된 모델명을 리포트 머리에 찍는다(안 찍히면 FAIL).
- **gumsu 실증 (2026-08-26)** — 비교 방향 반전(수치 동일·의미 반전)을 잡았다. guard 3 은 수치 집합만 보므로 이것이 사각이다. 시범 관찰 항목: «guard 3 통과 ∧ 헤르메스 🔴» 건수·유형.
- 🔴 **메모리 off 가 성립 조건.** 검수는 기억이 아니라 규격으로 해야 «같은 실수를 같은 이유로» 잡는다. 전 편 기억이 있으면 «지난번엔 통과했다» 가 판정에 섞인다. 설치 실측 ③에서 off 가 실제로 값을 안 남기는지 확인되지 않으면 이 자리는 착수하지 않는다.

**PROCESS**
1. 산출물과 규격 발췌를 받는다(레포 접근 없음).
2. 카드별로 «규격 위반 의심 · 근거 절 · 실물 위치(카드 번호·줄)» 를 낸다. 의심이지 판정이 아니다.
3. 캡션·고정 댓글·킷 본문에 v3.47 ③(안 한 일 열거)·v3.52 셀프 언급·예고 잔존을 본다(게이트 `[5-4]`·`[7]` 가 어휘로 못 잡는 의미 판정 — 여기가 제 자리다).
4. `reports/<날짜>_xreview_epNN.md` 에 목록 + 마지막 줄 `STATUS:`.

**OUTPUT** 리포트 1건. 정본 쓰기 없음.

**RULES — 금지 자리** 게이트 판정을 뒤집지 않는다(«게이트가 틀렸다» 는 learnings 후보로만) · 조문 제안 금지 · 발행 금지 · 산출물 수정 금지(제안만). 지적 상한 **10건** — 넘기면 «중요 순 10 + 나머지 건수» 로 자른다(읽는 비용).

**GATE** 리포트 형식(정관 §5) + 지적마다 근거 절 번호가 있는가(없는 지적은 «근거 없음» 으로 걸러 세지 않는다). 역검증: 규격 발췌에 없는 절 번호를 단 지적 1건을 심어 걸리는지.

**성공지표** keep rate = JJ 가 채택해 수정한 지적 ÷ 낸 지적 ≥ **50%** (사흘 시범, 편 2건 이상). 부수 지표: 채택 지적 중 «게이트가 못 잡는 종류»(의미 판정) 비율.

### 표본 원장 — 어느 회차를 세는가 (2026-08-28 신설)

🔴 **체크리스트가 바뀌면 그 전 회차는 표본이 아니다.** keep rate 는 «규격이 옳을 때 봇이 얼마나 맞히는가» 를 재는 값인데, 규격 자체가 틀렸던 회차의 오탐까지 세면 봇의 성적이 아니라 **우리 문서의 성적**을 재게 된다. 반대로 그 회차를 그냥 두면 개정의 효과도 보이지 않는다.

가르는 선은 **2026-08-26 10:46 참조형 개정**(`378d549` — 체크리스트를 조문 참조형으로 바꾸고 `xreview_refs.md` 발췌를 동봉)이다. 그 개정이 고친 것 셋:

| 항목 | 개정 전 (좁아서 오탐) | 개정 후 (정본대로) |
|---|---|---|
| C2 | «캡션·고정 댓글은 존댓말(**합쇼체**)» | 합쇼체·해요체 **모두** 존댓말 (§4.1 `[3]` 이 둘 다 통과시킨다) |
| C3 | «출처 언급 금지» 만 | **소재 고유명사**·두드려봄 실측 서술·조회 시점은 위반 아님 |
| C10 | «제작 과정 서사 금지» 만 | **채널 정체성 서술**(«돌려봤습니다»)은 과정 서사가 아니다 |

| 회차 | 시각 | 체크리스트 | 표본 | 결과 |
|---|---|---|---|---|
| ep31 | 08-26 10:31 | 개정 **전** | ❌ 제외 | 지적 6건 · 채택 3 (기각 3건이 전부 위 표의 C2·C3·C10 오탐) |
| ep32 | 08-26 20:37 | 개정 후 | ✅ | 형식 통과 · 지적 0건 |
| ep33 | 08-26 21:25 | 개정 후 | ⚠️ 산출 없음 | 출력 형식 위반 — 지적 0건 파싱 |
| ep34 | 08-26 22:49 | 개정 후 | ⚠️ 산출 없음 | 출력 형식 위반 — 지적 0건 파싱 |

**따라서 2026-08-28 현재 개정 후 유효 표본은 ep32 한 건이고 지적이 0건이라 keep rate 는 «분모 0 — 산출 불가» 다.** 시범 판정(8/29)을 이 값으로 내릴 수 없다. 형식 강제(`xreview.py` `MAX_ATTEMPTS`)를 넣은 뒤 **편 2건 이상에서 지적이 실제로 나와야** 비율이 성립한다.

- 앞으로는 리포트가 스스로 표본 경계를 말한다 — `xreview.py` 가 체크리스트·발췌의 **sha 앞 8자리와 수정 시각**을 리포트 머리에 찍는다(정관 §0 «실물이 정본»). 기억으로 표본을 가르지 않는다.
- 🔴 **2026-08-28 재측정 불가 — 프로바이더 장애.** `opencode-free` 의 `nemotron-3.5-lightning-free` 가 `HTTP 400 … Upstream request failed: [404]` 로 응답한다. gumsu·sagun 두 프로필에서 3회 시도 모두 같은 결과. 명세의 FAIL CONDITION 그대로 **«조용한 전환 금지»** 이므로 다른 모델로 갈아타지 않았고, 재측정은 프로바이더 복구 또는 플랜 B(Ollama 로컬) **JJ 결정** 뒤로 미룬다.

**FAIL CONDITION** 체크리스트를 못 받았거나 리포트를 못 쓴 경우 · 리포트 머리에 pin 모델명이 없는 경우 · **프로바이더 실패·변경 시 조용한 전환 금지 — 중단·보고**(플랜 B Ollama 는 JJ 결정). 지적 0건은 FAIL 이 아니다(«0건» 을 적는다).

**6질문 답** ① 산출물 사본 + 규격 발췌(사본) ② 리포트 파일은 유일 작성자, 편 폴더는 안 건드림 ③ SKILL §4.1·§5·§5.8-1·§6.1·v3.47·v3.52 발췌 ④ **메모리 · 타 편 · 검증로그(정답지를 보면 검수가 아니다)** ⑤ 지적 유형이 반복되면 → R6(게이트에 어휘 검사 추가) 또는 R7 ⑥ 같은 유형 3회 채택 → epcheck 항목 신설 제안(learnings 경유).

**하네스 판정** 봇 메모리 = **부채(off 필수)**. **채점** 반복 2 · 시간 1 · 검증 1(사람이 채택 판정) · 실패 비용 2 · 오염 2 = **8/10**.
