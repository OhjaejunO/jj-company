# 다음 인프라 정비 — 목록

> **이 파일이 «다음 인프라 정비 목록»의 정본이다 (2026-08-18 신설).**
> 그 전까지는 목록이 문서로 없었다 — 정비 항목이 리포트·PR 본문·계획 문서에 흩어져
> 있었고, 그래서 **끝난 것과 안 끝난 것을 한눈에 볼 자리가 없었다.**
>
> **규칙 셋**
> 1. **항목마다 «근거»를 적는다.** 근거 없는 줄은 만들지 않는다 — 왜 해야 하는지가
>    없으면 다음 사람이 판단을 처음부터 다시 한다.
> 2. **선행 조건이 있으면 먼저 적는다.** 순서가 틀리면 헛일을 한다.
> 3. **끝난 항목은 지우지 말고 ✅ 로 남긴다.** 지우면 «왜 그렇게 했는지»가 사라진다.

## 🔴 우선순위 1 — 측정이 틀린 것부터

**정리하기 전에 재는 것을 고친다.** 잘못 잰 수치로 정리에 들어가면 멀쩡한 것을 고친다.

### 12. 🔴 gumsu 프로필이 «쓰지 않는 도구»의 스키마를 매 호출 지고 간다 — **JJ 수작업** (2026-08-28 승격)

🔴 **2026-08-28 우선순위 1 상단으로 승격.** 같은 날 `xreview` 개정에서 **«입력 축소» 항목을 철회**했기
때문이다 — 킷 상한은 총 입력의 **1% 수준**이라 손잡이가 아니었고, 실측 경계도 통과 ep32 **26,407** 대
위반 ep33 **26,680** 으로 **273 토큰**이었다. 우리 쪽에서 줄일 것을 다 뺐으므로 **남은 손잡이는 이것뿐**이다.

🔴 **JJ 수작업 항목이다.** 프로필은 **레포 밖 정본**이라 에이전트가 고치지 않는다(정관 §1.5·§2).
스크립트로 자동화할 자리가 아니고, 명령을 알려 줄 뿐이다.

- **가장 큰 모순**: `gumsu` 의 SOUL 첫 줄이 **「도구를 쓰지 않는다」** 인데
  `terminal`·`file`·`code_execution`·`browser`·`computer_use` 를 포함해 **14개 툴셋이 켜져 있고**,
  그 스키마 **58,260바이트**가 **매 호출** 적재된다. 검수만 하는 봇에 파일·터미널·컴퓨터 조작
  스키마가 붙어 있을 이유가 없다 — 크기 문제이기 이전에 **범위 문제**다.

`xreview` 형식 위반 2건(8/26 ep33·ep34)의 조건으로 입력 크기를 봤는데, **우리가 줄일 수 있는 몫이 작다**는 것이 실측으로 나왔다.

| 몫 | 크기 | 누가 쥐고 있나 |
|---|---|---|
| hermes system prompt | 22,746자 | 프로필(우리 밖) |
| skills index | 7,247자 | 프로필 |
| **도구 스키마 JSON** | **58,260바이트** | **프로필 — `hermes tools`** |
| xreview 프롬프트(체크리스트+발췌+본문) | 7,217~11,093자 | 우리 |

조회: `hermes -p gumsu prompt-size --json`. **`gumsu` 는 `terminal`·`file`·`code_execution`·`browser`·`computer_use` 를 포함해 14개 툴셋이 켜져 있다** — SOUL 첫 줄이 «도구를 쓰지 않는다」 라고 적어 둔 봇인데도.

- 🔴 **킷 상한은 2026-08-28 에 철회했다** — ep34 에서 11,093 → 10,713자를 줄였을 뿐이라(총 입력 약 1%)
  «입력은 조여 놨다» 로 읽히면서 진짜 원인을 가린다. 근거는 `tomangchi-skill` `xreview.py` 머리 주석.
- 🔴 **하지 않았다 — 프로필은 레포 밖 정본이고 판정은 JJ 다.** `hermes -p gumsu tools disable <툴셋>` 또는 호출마다 `-t` 로 좁히는 두 길이 있는데, **`-t` 의 효과는 확인하지 못했다** — `prompt-size` 가 회차 인자를 반영하지 않고(같은 값 3회), 실측하려면 실호출이 필요한데 프로바이더가 8/28 현재 404 다.
- 착수 조건: 프로바이더 복구 → `-t` 유무로 `--usage-file` 의 `input_tokens` 를 비교 → 차이가 크면 프로필에서 영구 비활성.

### 1. `vault_audit.py` 별칭(alias) 미해석 — 감사 수치가 7배 과대

| | 스크립트 값 | 실측 | 배율 |
|---|---|---|---|
| 깨진 위키링크 | **685** | **97** (고유 55) | 약 **7배** |
| 고아 노트 | **50** | **10** | 5배 |

- **원인**: vault 194개 노트 중 **112개가 `aliases:` 를 선언**하고 링크가 대개 alias 로 걸리는데, 스크립트의 `resolve_link_target()` 이 파일명만 본다. 파일명 기준 693건 중 **596건이 별칭으로 정상 해석되는 링크**다.
- **조치**: `resolve_link_target()` 에 alias 딕셔너리 추가.
- **근거**: `reports\2026-08-18_morning-vault-health.md` §1·§2, 08-17 리포트 §7-3-1.
- 🔴 **아래 3번(링크·고아 정리)의 선행 조건이다.**

### 1-1. 스케줄 네 작업 `WakeToRun` — **8/21 적용 → 8/22 재등록 시 소실 → 8/25 재적용 완료, 절전 회차 실측 대기** (2026-08-21 등재)

🔴 **정정 (2026-08-25 판정 세션).** 아래 «적용 완료» 는 8/21 시점엔 사실이었으나 **지금은 아니다.**

| 시점 | 사실 | 근거 |
|---|---|---|
| 8/21 | `WakeToRun=True` 4/4 적용 | 이 절 아래 실측표 |
| **8/22 16:42~16:52** | 작업 4개 **재등록** — `C:\Windows\System32\Tasks\JJ\*` mtime. 이때 vault·drift 트리거가 12:30 으로 바뀌고 **`WakeToRun` 은 다시 `False`** 가 됐다(재등록 세션 기록은 확인 불가) | `Export-ScheduledTask` 8/25 실측: 4/4 `WakeToRun=False` |
| 8/23 | 12:30 drift 회차를 절전(09:25~21:21)으로 놓치고 21:29 catch-up — 깨어나지 못한 실증 | `skill-drift-audit_20260823.log` · Kernel-Power 42/107 |
| **8/25 22:31** | **재적용 완료** — 일반 셸의 `Set-ScheduledTask` 는 `0x80070005` 로 거부(S4U 작업은 승격 필요), UAC 승격 셸로 4/4 적용. **일반 셸 재조회 실값: `WakeToRun=True` 4/4** · StartWhenAvailable=True · 트리거 불변(12:30/12:30/08:00/08:30) · 작업 파일 mtime 22:31:15~16 | 이 세션 실측 (`Get-ScheduledTask -TaskPath '\JJ\'`) |
| 다음 | **첫 실측은 «절전 중 예정 시각» 회차다.** 판정은 Operational 로그 id=114 부재 + 래퍼 로그 첫 줄. 기계가 깨어 있던 회차는 실측이 아니다 | `docs\schedule-task-registration.md` §3 |

- **재등록이 설정을 되돌린다** — 그래서 `docs\schedule-task-registration.md` 에 «재등록 후 WakeToRun·트리거 실값 재확인» 단계를 넣었다. 아래 실측표는 8/21 기록으로 보존한다.
- 8/22 08:00 «실측 대기» 표는 채워졌다: 8/22 08:00·08:30 회차는 절전으로 안 돌았고(12:08 기상 후 12:14 catch-up), 정시 기동은 8/23 job 08:30 과 12:30 회차(8/24·8/25) 뿐이었다 — 전부 기계가 깨어 있던 회차라 **WakeToRun 실측은 아직 성립하지 않았다.** `reports\2026-08-25_schedule-verdict_0822-0825.md`.

✅ ~~**JJ 적용 완료 (2026-08-21).**~~ 스케줄러 설정이라 PR 로 못 고치는 항목이었고, JJ 가 직접 켰다. (8/22 재등록으로 소실 — 위 표)

**적용 실측 (2026-08-21, 이 세션에서 재확인)**

| 항목 | 값 |
|---|---|
| `morning-vault-health` / `tomangchi-scout` / `job-scout` / `skill-drift-audit` | `WakeToRun=True` · `StartWhenAvailable=True` · `LogonType=S4U` **4/4** |
| `RTCWAKE` (절전 모드 해제 타이머 허용) | `AC=1` |
| `UNATTENDSLEEP` (무인 절전 시간 제한) | `AC=1800` (120 → 1800) |

`DC=120` 은 그대로지만 **이 기계는 데스크톱(배터리 없음)이라 DC 경로를 타지 않는다.**

🔴 **아직 «고쳐졌다»가 아니다 — 실측이 남았다 (§4 2026-08-18 조항).** 설정이 켜진 것과 실제로 정시에 도는 것은 다르다. **다음 예정 회차가 첫 실측이다.**

| 시각 | 작업 | 볼 것 |
|---|---|---|
| 8/22 07:00 | skill-drift-audit | 로그 첫 줄 타임스탬프가 **07:00 대**인가 |
| 8/22 08:00 | tomangchi-scout | 08:00 대 + stagger 4분 뒤 실작업이 **살아서** 이어지는가 |
| 8/22 08:30 | job-scout | 08:30 대 + stagger 6분 뒤 동상 |
| 8/24(월) 07:30 | morning-vault-health | 주간 트리거라 하루 건너뛴다 |

- **판정 기준은 이벤트 `id=114` 의 부재다.** 그 이벤트가 없으면 정시 기동이고, 있으면 또 놓친 것이다.
- ⚠️ **PC 가 그 전에 깨어 있으면 이 실측은 성립하지 않는다** — 절전 상태에서 기상하는지를 보는 것이므로, 깨어 있던 회차는 «검증됨»으로 적지 않는다.
- stagger 4·6분이 `UNATTENDSLEEP=1800` 안에 들어가는지가 두 번째 관전 포인트다. 가장 긴 실측 소요는 tomangchi-scout 16.6분 + stagger 4분 = 약 21분.

**이하는 적용 전 진단 기록이다 (보존).**

- **증상**: 2026-08-21 네 작업이 예정 시각(07:00 / 07:30 / 08:00 / 08:30)에 하나도 안 돌고 **11:42:34 에 일괄 기동**했다. 약 4시간 지연.
- **원인**: `03:39:39` 절전 진입 → `11:36:36` 복귀(System 로그 Kernel-Power id=42 / Power-Troubleshooter id=1). 네 작업 모두 `WakeToRun=False` 라 **기상하지 않는다.** 이벤트 `id=114` 4건이 «예정대로 시작할 수 없음 → StartWhenAvailable 로 지금 시작»을 명시한다.
- **S4U 전환(08-20)으로는 안 풀린다.** S4U 는 «로그온 안 돼 있어도 실행»을 풀었고, «절전 중 기상»은 별개다. 둘을 같은 것으로 보면 매일 지연을 S4U 탓으로 오진한다.
- **전원 옵션은 (진단 시점에도) 이미 허용돼 있었다** — `RTCWAKE`(절전 모드 해제 타이머 허용) **AC=1 / DC=1** 실측. 작업 쪽 `WakeToRun` 만 켜면 된다.

🔴 **`WakeToRun` 만 켜면 오히려 세 작업이 죽는다 — 무인 절전 시간 제한(UNATTENDSLEEP)을 같이 올려야 한다.**

기상 타이머로 깨어난 뒤에는 이 값이 적용된다. **실측: `AC=120s` / `DC=120s`** (`powercfg /qh SCHEME_CURRENT SUB_SLEEP 7bc4a2f9-...`). 그런데 래퍼는 시작하자마자 stagger 로 잠든다:

| 작업 | 예정 | stagger | 2분 안에 실작업 시작? |
|---|---|---|---|
| skill-drift-audit | 07:00 | 0분 | ✅ 즉시 |
| morning-vault-health | 07:30 | **2분** | ⚠️ 경계선 |
| tomangchi-scout | 08:00 | **4분** | ❌ |
| job-scout | 08:30 | **6분** | ❌ |

**`WakeToRun` 만 켜면 깨어나서 stagger 로 자다가 다시 잠든다.** 지금(잠들어 안 돌음)보다 나은 것도 없고, 증상은 더 읽기 어렵다 — «깨어나긴 했는데 리포트가 없다».

- **가장 긴 실측 소요**: tomangchi-scout 16.6분(11:46:35→12:03:11) + stagger 4분 = 약 **21분**. 여유를 두면 **1800s(30분)** 가 적당하다.
- 대안으로 stagger 를 줄이는 것도 가능하나, **stagger 는 놓친 실행 일괄 기동(StartWhenAvailable) 경로를 막는 장치라 지우면 2026-08-19 사고가 돌아온다.** 전원 값을 올리는 쪽이 맞다.
- **근거**: `logs\scheduled\*_20260821.log` 4종, `Microsoft-Windows-TaskScheduler/Operational` id=114/129/100/201, System 로그 Kernel-Power.

### 1-2. 🔴 역슬래시 이스케이프가 **실제 제어 바이트로 박힌다** — 게이트 검사 2건이 헛돌고 있다 (2026-08-28 등재)

- **무슨 일**: `scripts\auth_check.py` 를 셸이 한 번 더 해석하면 `\a` 가 **BEL(0x07)** 이 되어 파일에 박힌다.
  화면에는 안 보이므로 `grep` 출력은 «scriptsuth_check.py» 로 뭉쳐 보이고, **파일에서 그 문자열을 찾으면
  안 나온다.** 이름으로 찾는 검사는 이걸 영원히 못 잡는다 — **바이트로 봐야 한다.**
  §6 「백슬래시가 든 코드는 heredoc 으로 쓰지 않는다」가 가리키는 그 결함이다.
- **이번에 고친 것 (PR #87 · 머지됨)**: `CLAUDE.md` 3건 · `scripts\morning-vault-health.ps1` 1건.
  뒤엣것은 **문서 오타가 아니라 기능 고장**이었다 — 래퍼가 `scripts<BEL>uth_check.py` 를 찾고 있어
  #85 로 넣은 인증 점검이 **매 회차 `UNAVAILABLE` 을 기록할** 상태였다. 지금 `Test-Path` True 확인.
- 🔴 **남은 것 (자회사 정본 — 본사가 고치지 않는다, §1.5)**: `tomangchi-skill` `skills\tomangchi\epcheck.py`
  에 같은 사고가 **4곳** 있다. `\b`(단어 경계)가 **0x08 백스페이스**로 박혔다.
  - `1035` 캡션 «ep 번호 비노출» · `1208` 고정 댓글 «ep 번호 비노출» — 패턴이 `<BS>ep\s?\d+` 라
    실제 글에는 백스페이스가 없으니 **한 번도 걸린 적이 없다. 늘 통과였다.**
  - `1069` «출처 나열·URL 없음» — `https?://` 갈래는 살아 있지만 **맨 도메인 갈래**(`foo.com` 꼴)가
    양쪽에 백스페이스를 요구해 **안 걸린다.**
  - 실측(2026-08-28): ep35~38 네 편의 캡션·고정 댓글을 **의도한 정규식**으로 다시 돌려 보니
    ep 번호 0건 · 도메인/URL 0건 — **산출물은 무사하다.** 헛돈 것은 검사 쪽이다.
  - **조치는 토망치랩 트랙에서 JJ가 판단한다.** 본사 에이전트는 자회사 정본을 읽되 고치지 않는다.
- **구조로 닫는 안 (§0 4층 ①·③)**: ① 정본 파일에 **제어문자가 있으면 커밋을 거부**하는 pre-commit 검사.
  검사기는 이미 있다 — 이번 회차에 쓴 훑기(역검증 5케이스: BEL 박힌 꼴은 잡고 온전한 역슬래시·줄바꿈·
  탭·CRLF 는 안 잡는다). 로그(`_gate.txt`)·`venv` 처럼 ANSI 색코드가 정상인 자리는 제외 대상이다.
  ③ 만으로 두면 «검사를 돌리는 것을 기억해야» 하므로 훅으로 가는 것이 맞다.

## 🟡 우선순위 2 — vault 실체 정리

### 2. `BACKUP_PATH` 미설정 — 백업 상태를 실측할 수 없다 (JJ 지시 2026-08-18)

- **현재 상태**: `departments\ops\config.md` 에 `BACKUP_PATH: (미설정)`.
- **결과**: 아침 감사 점검 항목 5(백업 검증)가 **매일 «확인 불가»로 떨어진다.** 백업이 되고 있는지 안 되고 있는지를 **아무도 모르는 상태**가 계속된다.
- **조치**: 실제 백업 경로를 확인해 `BACKUP_PATH` 를 채운다. 백업이 실제로 없다면 **«없음»이라고 적는 것도 조치다** — 그래야 «확인 불가»(모름)와 «백업 없음»(앎)이 갈린다.
- **근거**: `reports\2026-08-18_morning-vault-health.md` 백업 검증 항목. 2026-08-18 모지바케 수정(PR #51) 이후 그 «확인 불가» **사유는 읽을 수 있게 됐지만, 사유가 해소된 것은 아니다.**

### 3. JJ-Brain 깨진 링크·고아 노트 정리 (JJ 지시 2026-08-18)

🔴 **1번을 먼저 한다.** 스크립트가 별칭을 해석하기 전에는 **무엇을 고쳐야 하는지가 7배 부풀려 보인다.**

실측 기준(08-17)으로 실제 대상은 아래다.

| 대상 | 수 | 성격 |
|---|---|---|
| 깨진 링크 | **97건 / 고유 55개** | 실제 대상 |
| 고아 노트 | **10개** | 실제 대상 |

**알려진 하위 항목 (원인이 이미 특정됨)**

- **`ai-native-*` 4종** — 노트는 있는데 `aliases:` 에 파일명 슬러그만 빠졌다. **26회 링크**된다. → `202605241906-`·`07-`·`08-`·`09-` 의 **기존 `aliases:` 블록에 슬러그 추가.** ⚠️ **블록을 새로 만들지 말 것** (08-17 리포트 §1 정정).
- **한글 제목 zettel 4개** — `202605141700`·`1900`·`2100`·`2200`, `aliases:` 미선언 → 고아. → alias 추가 + `02_Areas/UE5-Development/UE5-MOC.md` 등에서 링크 연결.
- **`00_Inbox/` 0바이트 6개** — 파일이 있어서 링크 검사는 전부 통과하는데 **내용이 비어 있다.** 그중 `data-driven-weapon-architecture.md` 는 vault 안에서 **26회 링크**된다. **열어봐야만 비어 있는 걸 안다** — §0 조용한 실패 계열. → 내용을 채우거나, 링크를 실체 노트로 돌리고 빈 파일을 지운다.

- **근거**: `reports\2026-08-18_morning-vault-health.md`, 08-17 리포트 §1·§2·§7.
- ⚠️ **vault 는 2026-07-05 이후 무변경이다.** 수치가 매일 같은 것은 정상이며, **정리하지 않는 한 스스로 줄지 않는다.**

## 🟡 우선순위 3 — 기록이 사람 기억에 있는 것 (2026-08-22 등재)

**둘 다 «규율로 지키던 것»이다.** 정관 §3 이 훅으로 옮긴 것과 같은 계열 — 조항만 두면 또 규율이다.

### 4. 발행 후 뒷정리가 사람 기억에 있다 — **하루에 세 번 샜다**

**처음엔 「행 추가 누락」으로 좁게 봤는데 전수해 보니 그게 아니었다.** 새는 구멍이 셋이고,
셋 다 뿌리가 같다 — **발행이라는 사건이 시스템으로 돌아오는 경로가 없다.**

| 구멍 | 실제 사례 (2026-08-22 발견) |
|---|---|
| ① **행 자체가 없다** | ep25·ep26 (발행까지 끝났는데 행 없음) · `reels_spoop` (**2026-08-22 행 신설 → 같은 날 폐기** — 행이 없어 **폐기 판단조차 못 하던** 건이다. 행을 만들자 «미게시 + 수치 9일 경과»가 드러나 그 자리에서 닫혔다) · 울트라패스트(제작 8/15 → 행 8/18) |
| ② **행은 있는데 상태가 안 바뀐다** | **ep21·ep22 — 실물은 발행인데 `제작완료` 로 남아 있었다.** 그 탓에 같은 날 대기함 보고가 **«미발행 완제품 2개»로 오판**했다 |
| ③ **content-ops DB 레코드가 없다** | ep15·ep16·ep17 (발행로그는 `발행` 2026-08-15 인데 DB 에 레코드 자체가 없었다) · ep25·ep26 |

**전수 결과 (2026-08-22, 발행로그 × DB × 실제 폴더 3축 대조)**

- 전 26편 중 **어긋남 7건**을 찾았고 **5건을 그 자리에서 닫았다**(ep15·16·17 DB 레코드 생성, ep21·22 발행로그 정정 + 폴더 이동).
- **남은 2건은 사람만 닫을 수 있었다** — ep21·ep22 는 발행일이 없어 DB 전이가 막혀 있었고,
  **JJ 회신(2026-08-21)으로 같은 날 닫았다.** 최종 상태는 **26편 3축 전부 일치**이며
  남은 것은 ep1~ep5 발행일 `미확인` 뿐이다(처음부터 그렇게 기록된 오래된 건 · 별건).
- 🔴 **위치 대조는 폴더명이 아니라 «위치» 칸을 읽는다.** 1차 대조에서 `epNN_` 패턴으로 폴더를 훑었더니
  ep6(`A콘텐츠_출력/v2`)·ep7(`B콘텐츠_출력`)·ep17(`01_발행완료/힉스필드_레이어`)이 **거짓 경보**로 걸렸다.
  셋 다 폴더명이 규칙 밖일 뿐 **이동 보류 사유가 로그에 적혀 있는 정상 상태**다.
  **예외 목록을 두는 것은 답이 아니다** — 갱신 규칙 4가 이미
  「경로 의존성 때문에 못 옮기는 편은 `위치` 칸에 실제 경로를 적는다」로 정해 뒀고,
  **실측 결과 26편 전부 «위치» 칸 경로가 실재했다.** 칸을 읽으면 예외가 필요 없다.
  거짓 경보를 남기면 경보 자체를 안 보게 되므로 이 차이가 중요하다.

**🔴 원인은 «기억»이 맞다.** 발행은 JJ 가 인스타에서 손으로 하고, **그 사실을 시스템에 알리는 것도 사람**이다.
뒷정리 세 가지(발행로그 상태·DB 전이·폴더 이동)가 **서로 다른 세 곳**에 있어서, 하나만 빠뜨려도
나머지가 조용히 어긋난 채로 남는다 — 정관 §0 「조용히 실패하는 코드」의 문서판이다.

**제안: 「발행 완료」 회신을 트리거로 도는 절차 하나로 묶는다**

- `scripts\publish-done.ps1 -Ep 21 -PublishedAt 2026-08-20` **한 번**이 셋을 다 한다 —
  ① 발행로그 상태·발행일 ② content-ops `published` 전이 ③ `02_제작중` → `01_발행완료` 이동.
- **발행일은 인자로 받고, 없으면 거부한다.** 추측해서 넣지 않는다(§0). 실제로 오늘 ep21·ep22 가
  이 이유로 막혔다 — 그리고 **막은 것이 옳다.**
- 🟢 **이 방향이 듣는다는 증거가 이미 있다.** content-ops `Episode.save()` 의
  「`published` 로 가려면 `published_at` 이 있어야 한다」 가드가 **오늘 유일하게 기계가 잡아낸 자리**다.
  나머지는 전부 사람이 눈으로 찾았다. **가드가 있는 곳만 새지 않았다.**
- **역검증을 같이 둔다** — ⓐ 행 없는 편 ⓑ 상태 안 바뀐 편 ⓒ DB 레코드 없는 편 **셋을 각각** 넣어
  전부 걸리는지 보고, 정상 편은 통과하는지 본다. **한 유형만 시험하면 나머지 둘은 헛돈다.**
- **읽기 전용 감사부터 떼어 내도 된다.** 위 3축 대조는 결정적이라 정답이 있다 —
  `skill-drift-audit` 처럼 **모델 없이** 돌리고 어긋남만 보고하게 하면, 자동 수정 없이도 ②③ 은 다시 안 샌다.
- **선행 조건 없음.** 감사만 먼저 넣는 것은 단독 착수 가능하다.
- **근거**: 2026-08-22 ep25·ep26 정리 → ep21·ep22 오판 → 전 26편 전수.

### 5. 「조문↔코드 갈림」 전수 — 표본 3방향이 모였다

**조문과 코드가 어긋나는 방식이 하나가 아니다.** 지금까지 나온 것을 방향별로 가른다 —
가르지 않으면 «문서를 고칠 일»과 «코드를 고칠 일»이 섞여 전수 자체가 안 끝난다.

| 방향 | 표본 | 상태 |
|---|---|---|
| ⓐ **조문만 있고 코드 없음** | `save_scene` · `find_scene` · `get_scene` — SKILL.md 가 씬 라이브러리 조회·저장을 이 이름으로 지시하는데(§씬 라이브러리·`props/index.md` 「조회는 `scenes.find_scene(주체, 개념)`」) **`tomangchi-skill`·라이브 스킬·`content-ops`·workshop 어디에도 없다** | 🔴 미해소 |
| ⓐ (선례) | `manychat_std.py` — 실재하지 않는 스크립트를 정본이 가리키고 있었다 | ✅ v3.36.1 로 해소 (「없는 것을 가리키는 문장은 정본에 두지 않는다」) |
| ⓑ **코드만 있고 조문 없음** | `cardcheck` 격자 국소 대비(`GRID (6,3)`·`MIN_LOCAL_CONTRAST` 3.0) — 구현돼 있는데 `grep 국소 SKILL.md` 0건이었다 | ✅ **해소** — `tomangchi-skill` PR #73 (v3.49) |
| ⓒ **조문과 코드가 다름** | 킷 슬라이드 팬 — **조문**(v3.34 §1532)은 「**380px/s 기준**으로 이동거리 ÷ 380 = 팬 초」인데 **코드**(`scrollcard.py`)는 `dur=13.0` 을 **인자로 받고** 속도는 `print` 로 **출력만** 한다. 역산을 강제하는 것이 **아무것도 없다** — ep23 이 9.0초를 물려받아 521px/s 로 흘러간 사고가 정확히 이 구조다 | 🔴 미해소 |

- ⚠️ **`PAN_SEC` 라는 심볼은 어느 레포에도 없다** — 실체는 위 표의 `dur` 인자다. 이름으로 찾으면 못 찾는다.
- **ⓐ 는 두 갈래로 갈린다**: 함수를 **만들** 것인가, 조문을 **현행 사실로 고칠** 것인가.
  씬 라이브러리는 **지금 손으로 관리되고 있고**(ep25·ep26 최종 씬 8종이 미등재 상태로 남았다)
  자동 등재가 없어 **같은 그림을 다시 생성해 크레딧이 새는** 경로가 열려 있다 — 정관 §0 이 기록한 그 사고다.
  따라서 **여기는 «조문을 고쳐서» 닫을 자리가 아니라 «코드를 만들어서» 닫을 자리로 보인다.** 판단은 JJ.
- 🔴 **실물 사례가 나왔다 (2026-08-22, ⓐ `find_scene` 부재).** 울트라패스트 편을 폐기하며
  «지피 씬 6종을 라이브러리에 등재하자»고 제안했고 **JJ 가 승인했는데, 실행하려고 대조하니
  이미 6종 전부 등재돼 있었다** — `scenes.json` 개념 6건, `index.md` 6행 완비.
  폐기 폴더의 `_1차백업/` 은 **회색 스튜디오로 나온 1차 폐기분**이었다(해시 5/6 불일치).
  - **조회 함수가 없어서 난 일이다.** `find_scene` 이 실재했다면 **한 번 조회로 끝났을** 판단을,
    사람이 **파일명과 해시를 손으로 대조**해서야 알았다.
  - **위험은 «중복 생성»만이 아니다.** 대조하지 않았으면 **중복 등재**를 했을 것이고,
    그러면 `find_scene` 이 **같은 그림을 두 개념으로** 돌려준다. 조회 수단의 부재가
    양방향으로 샌다.
  - 근거: `workshop/03_소재큐/_폐기기록.md` §정정.
- **전수 방법 제안**: SKILL.md 에서 백틱으로 감싼 **식별자·파일명을 전부 뽑아** 레포 4곳에 존재 여부를 대조한다.
  결정적 비교라 정답이 있다 — §0 대로 **모델을 넣지 않고** 스크립트로 돌린다(`skill-drift-audit` 과 같은 성격).
- **근거**: 2026-08-22 ep25·ep26 정리 중 발견.

## 다른 문서에 있는 정비 항목 (여기서 가리키기만 한다)

**내용을 복사하지 않는다** — 복사하면 두 곳이 갈린다.

| 항목 | 정본 | 상태 |
|---|---|---|
| 마케팅 교보재 — 레퍼런스 전수 분해·갭 분석 | `docs\marketing-playbook.md` | 🟡 **잠정판(2026-08-19)** — 실물 파일 0건, JJ 제공함 `G:\내 드라이브\레퍼런스_제공함` 대기. SKILL 개정 후보 9건(편마다 하나씩) |
| 브랜드 스크립트 정본 단일화 | `docs\plan-brand-assets-move.md` | 승인·착수 대기. **창 조건 재판정 필요** — 그 문서의 창 판정은 2026-08-16 기준이라 낡았다 |
| `inner._v3_bottom` 침범 가드 | 같은 문서 §순서 0 | 위 정비보다 **먼저** 한다. 창이 안 열리면 **이 항목만 떼어 단독 진행** |
| Manychat API 연동 | `docs\infra-manychat-api.md` | **보류 확정.** 재검토 조건 2개가 성립하기 전에는 다시 보지 않는다 |
| A등급 부서 저장소 쓰기 경계 | `docs\ops-grade-boundary.md` | ✅ **해소 (2026-08-18, PR #50·#51)** — 도구 부재로 전환 |
| 토망치랩 SKILL **PR #14** (번호 뱃지 §6.7) | `tomangchi-skill` PR #14 | 🟡 **다음 정비.** 아래 참조 |

### 🟡 토망치랩 SKILL PR #14 — 판본 재지정 포함 다음 정비 (2026-08-18 등재)

**결정 유지: 지금 머지하지 않는다.** `CONFLICTING/DIRTY` 이기 **이전에 판본 번호가 겹친다.**

- PR 이 잡은 **`v3.10` 은 이미 소비됐다** — main 의 §6.5 「비교 소재는 나란히 동시 재생이 원칙 (2026-08-16 v3.10 신설)」이 그 번호를 쓰고 있고, 헤더는 이미 **v3.13** 까지 왔다.
- **리베이스 시 다음 빈 번호로 다시 잡아야 한다.**
- 충돌면은 **판본 헤더의 `기발행분은 소급 수정하지 않는다.` 한 줄과 그 직전 삽입 지점**뿐이다. 본문(§6.1·§6.7)은 v3.13 이 건드리지 않았다.
- **같이 정리할 것**: 헤더 블록에 **`v3.10` 줄이 없다.** §6.5 가 v3.10 으로 신설됐는데 판본 목록은 v3.9 다음이 바로 v3.11 이다. v3.13 PR 에서는 정확히 #14 의 충돌 구역이라 손대지 않았다.
- worktree `_wt/skill-badge-number` 는 이 리베이스가 끝날 때까지 **유지**한다.
- **근거**: 2026-08-18 PR #20·#49 본문, 발행로그 「미해결 / 다음 액션」.

### 6. 조건을 적는 곳과 감시하는 곳이 다르다 — 공정 편입 검토 (2026-08-22 등재)

**「~되면 낸다」를 소재 문서에 적어도 아무도 안 본다.** 감시는 `departments\marketing\config.md`
의 감시표들이 하는데, **소재 문서에서 감시표로 가는 다리가 없다.**

**전수 결과 (2026-08-22 · `03_소재큐`·`03_보류`·`02_제작중` 전 문서)**

| 조건 | 감시처 등록 | 판정 |
|---|---|---|
| 히기 — 「Seedance 2.5 출시되면」 | 🔴 **없음** (`config.md`·`content-scout.md` 전수 0건) | **출시(8/8경)를 16일간 아무도 못 봤다.** 2026-08-22 폐기로 해소 |
| 울트라패스트 축① 가격 공개 | ✅ 「가격·정책 변경 감시」 표 | 🟢 **모범 사례** — 아침 스캔이 매일 실측한다(8/22 로그 §10·§11) |
| 울트라패스트 축② 일반 개방 | ✅ 「제품 출시 감시」 표 | 🟢 자동. 단 **사람 확인 몫은 「우선순위 낮음」으로 매 회차 미시도** |
| 「AI에 뭘 못하게 막아뒀나」 조건② deny rules 보강 | — (해당 없음) | ⚠️ **외부 감시 대상이 아니다** — JJ 본인이 할 일. 감시가 아니라 **기한·담당이 없어** 8일째 정지 |

- 🟢 **울트라패스트 보류팩이 답을 이미 보여 준다.** 그 문서에는 **「재개 조건 감시 — 두 축이 각각
  어디에 걸리나」** 절이 있어 조건마다 **감시표 이름·URL·자동 감지 여부**를 적어 뒀다.
  **히기에는 그 절이 없었다.** 차이는 규율이 아니라 **양식**이다.
- **검토 과제**: 소재 문서에 조건을 쓸 때 **감시처 칸을 필수**로 만든다.
  - ⓐ **양식 강제** — 소재큐 문서 템플릿에 「조건 / 감시처 / 자동 감지 여부」 3칸을 둔다.
    비어 있으면 **「감시 안 됨」이라고 명시**하게 한다(빈칸을 «없음»으로 읽지 않게).
  - ⓑ **기계 검사** — 소재 문서에서 조건 문형(`되면`·`공개되면`·`충족 시`)을 뽑아
    `config.md` 감시표와 대조하고, 없으면 경고. 결정적 비교라 **모델 없이** 돌아간다.
  - 🔴 **역검증**: 감시처 없는 조건을 넣어 실제로 걸리는지 / 등록된 조건은 통과하는지 **양쪽**.
- ⚠️ **조건에는 두 종류가 있다.** 위 표의 4번처럼 **외부 신호가 아니라 사람 할 일**인 조건은
  감시표에 넣을 자리가 없다. 이쪽은 **기한·담당**이 빠진 것이므로 다른 처방이 필요하다 —
  둘을 같은 칸에 넣으면 감시표가 할 일 목록으로 오염된다.
- **근거**: 2026-08-22 히기 폐기 처리 중 전수.

### 7. 스캔 제안 ↔ 기발행 대조가 구조적으로 안 돈다 (2026-08-22 등재)

🔴 **오늘 아침 스캔이 «Google AI Pro/Plus 학생 무료 오퍼»를 신규 제안했는데, 그것이 ep24 다** —
**전날(2026-08-21) 발행분이다.** 규칙이 없어서가 아니라 **대조에 쓰는 데이터가 비어 있다.**

**두 겹으로 뚫려 있다**

1. **데이터**: `content-scout.md` §278 이 「기존 발행 중복 — content-ops `episodes_topic` 대조」를
   요구하는데, **`episodes_topic` 연결이 ep20 에서 멈췄다.**
   실측 — **ep21·22·23·24·25·26 여섯 편 전부 topic 연결 `None`**, Gemini/학생 관련 topic **0건**.
   즉 스캔이 아무리 성실히 대조해도 **«중복 아님»이 나오는 것이 정상 동작**이다.
2. **양식**: `content-scout.md` §270 의 「기존 발행 중복」 칸이 **X 화제 표에만** 있고
   **「후보 전체 + 판정 사유」 주 표에는 칸 자체가 없다**(8/22 로그 §37 실측:
   `후보 | 축 | 1차 확인 | 판정 | 사유`). 주 후보가 중복 대조를 안 거치는 구조다.

- **먼저 고칠 것은 1번이다.** 칸을 만들어도 대조할 데이터가 없으면 `중복 미확인` 만 쌓인다.
- **topic 등재도 «발행 후 뒷정리»다** — 항목 4의 `publish-done` 절차에 **④ topic 등재**를
  같이 넣으면 한 자리에서 닫힌다. 별도 공정으로 만들면 또 사람 기억에 남는다.
- 🔴 **역검증**: 이미 발행한 소재를 후보로 넣어 **실제로 «중복»으로 걸리는지** 본다.
  ep24(Google 학생 오퍼)가 **그대로 시험 입력**이다 — 오늘 안 걸린 것이 확인됐으므로,
  고친 뒤 걸리면 그 자체가 증명이다.
- **근거**: 2026-08-22 스캔로그 대조 · content-ops DB 실측.

### 8. 킷 HTML 이 **두 곳에 있고 갈려도 아무도 안 본다** (2026-08-28 등재)

- **무슨 일**: 킷 페이지는 **편 폴더**(`workshop/02_제작중/epNN_*/<kit>.html`)와 **배포 레포**
  (`tomangchi-lab.github.io/kits/<kit>.html`) **두 곳**에 같은 이름으로 있다.
  게이트 `[8-1]`·`[8-2]`·`[8-3]` 은 전부 `os.path.join(편폴더, KIT["html"])` 만 본다 —
  **배포본은 게이트 사정권 밖**이다. 둘을 맞추는 코드도, 다른지 보는 검사도 없다.
- **왜 위험한가**: 방향이 **양쪽 다** 샌다.
  ① 배포본만 고치면 → 게이트는 편 폴더 옛 판을 보고 `[8-2]` 최신성 **OK** 를 찍는다. 캡처·킷 영상은
  옛 내용으로 남고, 발행되는 페이지와 카드가 말하는 내용이 갈린다.
  ② 편 폴더만 고치면 → 게이트는 통과하는데 **배포된 페이지는 안 바뀐다.** 킷 URL 을 받은 독자가 옛 판을 본다.
- **근거 실측 (2026-08-28 · ep38)**: 취소선 항목을 정정하며 **배포본만** 고쳤다. 게이트를 돌리니
  `[8-2] 킷 캡처 mtime ≥ HTML mtime` 이 **OK** 로 나왔다 — 편 폴더 HTML 은 00:42, 캡처는 00:56,
  배포본은 09:18. 검사는 «자기가 아는 파일» 기준으로 정직하게 통과했고, **갈렸다는 사실은 아무도 안 봤다.**
  `diff` 를 손으로 떠서야 보였다.
- **문안 후보 (§0 4층 순서대로)**:
  ① **구조** — 편 폴더를 유일한 정본으로 두고 배포는 `deploy-kit` 한 줄이 복사한다(사본을 손으로 안 만든다).
  ② 구조를 못 바꾸면 **생성 단계** — `kitchain.py shoot` 이 배포본과 다르면 멈춘다.
  ③ 그것도 아니면 **검사** — 게이트 `[8-4]` 신설: 편 폴더 HTML 과 `kits/` 사본의 바이트가 같은가.
  ④ **못 잡는 자리** — 배포 레포가 다른 기계에서 바뀌면 로컬 검사로는 못 본다. 그건 적어 둔다.
- **지금 상태**: ep38 은 두 파일을 손으로 맞추고 `kitchain.py . all` 로 캡처·영상을 다시 구웠다
  (킷 영상 12.7초 → 12.8초, 편 선언도 갱신).
- **전수 확인 (2026-08-28)**: 발행 대기 네 편의 두 사본을 `diff` 로 대조 — **ep35·36·37·38 전부 동일.**
  즉 **이번이 첫 갈림이었고 같은 세션에서 닫혔다.** 그래도 항목은 남긴다 — 갈리는 것을 막는 장치가
  여전히 없고, 다음에는 발행 직전에 갈릴 수 있다(정관 §0 «조용히 실패하는 코드»).

### 9. 🔴 유통 워커가 **주장 0건짜리 지시서**를 정상인 척 냈다 (2026-08-28 등재 · 같은 날 고침)

- **무슨 일**: `dist_transform._module_literals` 는 빌더를 실행하지 않고 `ast.literal_eval` 로
  최상위 대입만 뽑는다. 빌더가 `"shot": F.VIDEO_SRC` 처럼 **`_facts.py` 값을 가리키면**
  `CARDS` 통째로 평가에 실패하는데, 종전 코드는 그 실패를 **조용히 삼키고** `load_ep` 이
  `decl.get("CARDS") or {}` 로 받았다. 결과는 **주장 0건 · 첨부 후보 0건짜리 «작업 지시서»** 이고,
  문서 형식은 멀쩡해서 **읽는 사람이 «이 편은 원래 쓸 게 없나 보다»로 읽는다.**
- **얼마나 오래**: ep36 은 **영상 카드가 있는 편이라 처음부터 이 상태**였다. 8/27 에 만든
  `reports6-08-27_dist_ep36.md` 계열이 그 빈 지시서를 재료로 삼았다.
- **고친 것 (이번 회차)**: ① `_facts.py` 를 먼저 읽어 `F.*` 참조를 값으로 치환한다(`_Subst`).
  ② 그래도 `CARDS` 가 비면 **`RuntimeError` 로 죽인다** — «카드 0건인 편»은 존재하지 않으므로
  그건 읽기 실패다. 정관 §0 «조용히 실패하는 코드».
- **역검증 (양쪽)**: 네 편 다 정상 판독(ep35 주장 30 · ep36 **0 → 24** · ep37 25 · ep38 29) ·
  일부러 읽을 수 없게 만든 `CARDS` 는 예외로 죽는다.
- **남은 것**: 치환 대상이 지금은 `F.`(=`_facts`)와 **같은 파일 위쪽의 이름** 둘이다.
- 🔴 **바로 다음 회차에 또 났고, 이번엔 시끄럽게 죽었다 (2026-08-28 3차).** ep35 가
  `"key": "… — %s" % AAI_LABEL` 로 카드 값을 **식으로** 조립하자 `CARDS` 를 못 읽었다.
  ① 같은 파일 이름 치환을 더했고 ② 그래도 `%` 연산은 못 푸니 **편 쪽에서 리터럴로 되돌렸다**
  (선언과 라벨이 갈리지 않게 편 `REQUIRED` 가 둘을 대조한다).
  **장치가 제 몫을 했다** — 조용히 «주장 0건» 을 냈으면 그대로 발행 재료가 됐을 자리다.
- **규칙으로**: **카드 선언 값은 리터럴로 적는다.** 유통 워커가 빌더를 실행하지 않고 읽기 때문이다.

### 10. 🔴 **번호 기반 파일명이라 카드가 하나 바뀔 때마다 드라이브에 옛 판이 남는다** (2026-08-28 등재)

- **무슨 일**: 덱 파일명이 `01_cover.png`·`02_ox.png` 처럼 **자리 번호**다. 카드를 하나 넣거나 빼면
  뒤가 전부 밀려 **이름이 통째로 바뀐다.** 드라이브는 «같은 이름»만 덮어쓰므로 **옛 번호 파일이 그대로 남고**,
  폰에서 덱을 열면 새 파일과 옛 파일이 **섞여 보인다.**
- **근거 실측 (2026-08-28)**: 4차 교정에서 ep35·ep36·ep38 의 덱이 움직이자 잔존 **13개**가 나왔다
  (ep35 4 · ep36 6 · ep38 3). `deliver.py` 의 검증이 잡아 `STATUS: FAIL deliver-verify` 로 세웠고,
  **삭제는 사람 자리**라 스크립트가 명령만 낸다. 같은 회차에 ep35 가 한 번 더 움직여(10 → 11장)
  **같은 일이 한 세션에 두 번** 났다.
- 🔴 **이번이 처음이 아니다.** SKILL §6.8 이 「씬 이름에 카드 번호를 넣지 않는다」를 못박은 근거 ⓐ 가
  바로 이것이다 — «드라이브에 옛 번호 파일이 남아 폰에서 덱에 섞였다». **씬 이름에서는 번호를 뺐는데
  덱 파일명에는 그대로 남아 있다.** 조항이 반쪽만 적용된 자리다.
- **선택지 (§0 4층 순서대로)**:
  ① **구조** — 덱 파일명을 **내용 기반**으로 바꾸고(`cover.png`·`ox.png`·`artifact.mp4`) 순서는
     `DECK` 선언이 진다. 이름이 안 바뀌니 덮어쓰기로 끝나고 잔존이 **원리적으로** 안 생긴다.
     드라이브에서 사람이 순서를 보려면 전달 때 번호를 **접두어로 붙여 복사**하면 된다(그 번호는 매번 새로 붙는다).
  ② **생성 단계** — 전달 전에 대상 폴더를 비우고 넣는다. **되돌리기 어려운 삭제**라 C등급 경계에 닿는다.
  ③ **검사** — 지금 상태. `deliver.py` 가 잡아 주지만 **사람이 매번 지워야 한다.**
  ①이 맞다. ③은 «잡히니까 괜찮다»로 굳기 쉬운데, 이번처럼 한 세션에 두 번 나면 그 비용이 사람에게 간다.
- **지금 상태**: 잔존 13개는 **JJ 가 지운다**(2026-08-28 판정). ep35 는 그 뒤 한 번 더 움직였으므로
  **제거 목록이 갱신됐다** — 아래 발행 전 절차 참조.

### 13. 인스타그램 Content Publishing API — **보류 확정 (2026-08-28 등재)**

**결론: 안 한다.** 자동화 이득보다 **덱 규격을 뜯는 비용**이 크고, 넷 중 둘은 비용이 아니라 **조문 위반**이다.
정관 §0 «발행은 매체별로 가른다» 가 이 항목을 가리킨다.

**전제 4건 (JJ 조사 2026-08-28 · 우리 쪽 실측은 이 세션)**

| # | API 가 요구하는 것 | 우리 현행 (실측 2026-08-28) | 판정 |
|---|---|---|---|
| ① | 캐러셀 **상한 10장** | ep35 **11장**(png 9 + mp4 2) · ep36 7 · ep37 8 · ep38 9 | 🟡 **비용** — ep35 급 편은 카드를 빼야 한다 |
| ② | **JPEG 강제** | 정지 카드 전부 `.png` | 🔴 **조문 충돌** — 정관 §0 「그대로 인용 = 원본 대비 **화소 손실 0**」(2026-08-28). JPEG 는 손실 압축이라 공식 원본을 실은 카드가 **정의상 위반**이 된다 |
| ③ | **무음 영상 불가** | 덱 mp4 전부 오디오 스트림 **0개**(`ffprobe` 실측 — ep35 `08_artifact` · ep38 `06_edit`·`08_kit`) | 🟡 **비용** — 전 영상 카드에 무음 트랙을 넣어야 한다 |
| ④ | 카드마다 **공개 HTTPS URL** | 덱은 발행 순간까지 비공개(편 폴더·드라이브) | 🔴 **원칙 충돌** — **발행 전 산출물을 먼저 공개**해야 API 를 부를 수 있다. 예약·검수 중인 판이 URL 로 새어 나간다 |

- **②④ 가 수지를 뒤집는다.** ①③ 만이면 규격을 고쳐 갈 수 있는데, ② 는 이번에 세운 조문을 **매 편 어기게** 만들고
  ④ 는 「발행 전에는 안 보인다」는 전제를 깬다. **조문을 지키면서 이 API 를 쓰는 경로가 없다.**
- **재개 조건**: ② 가 풀리거나(무손실 포맷 허용) ④ 가 풀리면(서명 URL·업로드 세션) 다시 잰다.
  ①③ 만 남으면 그때는 «규격 변경 비용 대 사람 15분» 의 계산이고, 그 계산은 지금 하지 않는다.
- **이 항목은 «안 하기로 한 판정»이라 착수 순서에 넣지 않는다.** 지우지도 않는다 —
  안 적으면 다음 회차가 같은 조사를 처음부터 다시 한다(정관 §0).

### 14. 편수·게시물 수가 **세 곳에서 갈린다** — 셈법 조사 (2026-08-28 등재)

같은 값을 세 곳이 다르게 들고 있다. **어느 하나가 «맞다»고 정하기 전에 셈법부터 본다** —
정관 §0 «실물을 조회할 수 있는 것은 실물이 정본이고 문서는 조회 결과다».

| 자리 | 값 | 조회 방법 |
|---|---|---|
| **발행로그** (정본) | **31편 / 32건** → ep35~38 반영 후 **35편 / 36건** | 표의 ep 행 34개 중 상태가 정확히 `발행` 인 행 32개 · 고유 편 31 (ep1 과 ep1-릴스 가 한 편) |
| 포트폴리오 (대외) | 32편 / 33건 (2026-08-27 기입) | `portfolio-refresh-state` 메모리 · `OhjaejunO.github.io` PR #15 |
| content-ops DB | 발행 28 · 취소 2 (총 30행, 마지막 `number` 34) | `episodes_episode` 직접 조회 |

**JJ 확정 (2026-08-28)**: **발행로그가 정본**이고 갱신 후 값은 **35편 / 36건**이다.
취소·폐기 편(**ep8 발행취소 · ep19 폐기**)은 발행으로 세지 않는다.

**조사 항목 둘**

1. 🔴 **DB 가 4건 적은 원인.** 발행로그 32건 대 DB 발행 28건. 마지막 `number` 는 34 라
   **번호는 따라왔는데 행이 빈다** — 어느 편이 DB 에 안 들어갔는지 번호 대조로 특정한다.
   ep35~38 은 아직 양쪽 다 없으므로 이 차이와 무관하다.
   같이 볼 것: DB 의 `published_at` 이 `15:00:00`·`03:00:00` 처럼 **시각이 일정하다** —
   실제 발행 시각이 아니라 관행값으로 보인다. 그렇다면 **DB 는 «발행일»의 근거가 아니다.**
2. 🔴 **포트폴리오 셈법.** 한 칸 높은 값(32/33)이 나오려면 **`발행취소` 행이 «발행» 으로 세어져야** 한다 —
   `발행` 을 부분 문자열로 맞추면 `발행취소`(ep8)가 걸려 정확히 33행·32편이 된다.
   **추정이다** — 그때 쓴 셈 코드를 열어 확인하지 않았다. 확인하고 적는다.

- 🔴 **포트폴리오는 대외 문서라 수정이 필요하다 — 그러나 이번 회차에서 고치지 않는다(JJ 지시).**
  별건으로 처리한다. 고칠 때는 **웹 4쪽·원페이지·PDF·전사 맵**이 같이 걸리므로
  `portfolio-refresh-state` 메모리의 갱신 절차를 그대로 따른다.
**조사 항목 ③ (2026-08-28 추가) — content-ops DB 를 무엇에 쓸 것인가**

값을 맞추기 전에 **쓸모부터 정해야 한다.** 지금 이 DB 로 답할 수 있는 물음이 «편이 몇 개인가» 하나뿐인데,
그 물음의 정본은 **발행로그**다. 정본이 따로 있는 값을 한 벌 더 들고 있으면 **갈리기만 한다** —
실제로 지금 4건 갈려 있다.

- **URL 열이 없다.** `episodes_episode` 는 `id`·`number`·`title`·`category`·`status`·`published_at`·
  `cancel_reason`·`created_at` 뿐이다. 게시물 주소를 못 담으므로 **«그 게시물이 무엇이었나»를 못 답한다.**
- **`published_at` 이 관행값이었다.** 기존 행이 `15:00:00`·`03:00:00` 처럼 일정해 **실제 발행 시각이 아니다.**
  (ep35~38 은 2026-08-28 에 JJ 구술 실값으로 넣었다 — 관행값을 쓰지 않았다.)
  그래서 **«언제 나갔나»도 과거 행으로는 못 답한다.**
- **그러면 남는 쓸모는 무엇인가** — 후보 셋을 놓고 JJ 가 고른다.
  ⓐ **폐기** (발행로그로 일원화) ⓑ **소재·중복 판정의 뒷단**(`Topic`·`Source`·`similarity.py` 가 이미 있다 —
  `scan_check` 가 쓰는 그 자리) ⓒ **유통 채널별 게시물 원장**(URL·채널 열을 늘려 발행로그의 게시물 층을 대신)
- 🔴 **ⓒ 를 고르면 열 추가 마이그레이션이 선행**이고, 그때 «정본이 둘» 문제를 어떻게 가를지 같이 정해야 한다.
  ⓑ 가 지금 실제로 도는 쓸모에 가깝다 — `Episode` 표는 그 곁가지일 수 있다.
- **먼저 답할 것**: 위 4건 차이가 «넣다 만 것»인지 «안 넣기로 한 것»인지. 후자면 차이는 결함이 아니라 **설계**이고,
  그렇다면 편수 집계를 DB 로 하는 것부터가 틀렸다.

- **왜 등재하나**: 세 값이 갈린 채로 두면 다음 사람이 **아무 값이나 골라 쓴다.**
  8/24 에 28 대 29 로 갈렸던 것과 같은 계열이고, 그때는 ep1 릴스 행 누락이 원인이었다 —
  **한 번 갈린 자리는 셈법을 적어 두지 않으면 또 갈린다.**

### 15. 편 빌더가 **상위 폴더를 상대로 계산해** 공용 모듈에 닿는다 — 발행 후 이동하면 끊긴다 (2026-08-28 등재)

**언제** ep35~38 을 발행 후 `02_제작중` → `01_발행완료` 로 옮기자(발행로그 규칙 4) 그 편들의
`build_own.py`·`build_plates.py` 가 공용 모듈을 못 찾게 됐다.

- **꼴**: `sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))` —
  «편 폴더의 상위»를 넣는다. 제작 중에는 그 상위가 `02_제작중` 이라 `_ownart.py`·`_plate.py` 가 거기 있고,
  **옮기고 나면 상위가 `01_발행완료` 라 그 폴더에는 없다.**
- **전수 실측 (2026-08-28)** — 이 꼴을 쓰는 파일은 **5개, ep35~38 넷뿐**이다.

| 편 | 파일 | 상위에서 찾는 것 |
|---|---|---|
| ep35 | `build_own.py` · `build_plates.py` | `_ownart` · `_plate` |
| ep36·ep37·ep38 | `build_own.py` | `_ownart` |

- ✅ **편마다 반복되는 고장이 아니다 — 이번 묶음에서 처음 생겼다.** ep34 이전 편에는 이 꼴이 **0건**이고
  `build_own.py` 도 편 폴더 안에서 자족했다. `_ownart.py`·`_plate.py` 를 **공용 모듈로 뽑은 것이 2026-08-28**
  이고, 그때 «편 폴더 위»라는 자리를 상대 경로로 가리킨 것이 원인이다.
  (`_facts` 는 편 폴더 안에 있어 해당 없다 — 같은 줄에 섞여 보이지만 다른 삽입 경로로 풀린다.)
- **지금 실해는 없다.** 발행로그 규칙 7 이 «발행된 편은 소급 수정하지 않는다» 라 빌더를 다시 돌릴 일이 없다.
  🔴 **그러나 «돌릴 일이 없다» 는 «고장이 아니다» 가 아니다** — 되돌아가 재조립해야 할 사고가 나면
  그때 처음 발견된다. 그 자리가 가장 나쁜 발견 시점이다.
- **문안 후보 셋** — ⓐ 공용 모듈을 **`00_브랜드에셋` 으로 올린다**(이미 절대 경로로 참조하는 자리라 이동에 안 흔들린다)
  ⓑ 상대 계산을 **절대 경로 상수**로 바꾼다(`02_제작중` 을 박는다 — 옛 편들이 그렇게 하고 있고 이동 후에도 산다)
  ⓒ 편 템플릿에 «편 폴더 밖 모듈은 절대 경로로만» 한 줄. **ⓐ 가 §0 4층의 ①(구조)** 이다 —
  가리키는 자리를 옮기면 검사도 규칙도 필요 없어진다.
- 🔗 C-20 과 이웃이다 — 그쪽은 «판 만드는 코드가 커밋 밖», 이쪽은 «그 코드가 자기 의존물을 못 찾는다».
  둘 다 **제작 코드의 보관 자리** 문제다.
- **JJ 판정**: 대기.

#### 사례 2 — **같은 결함이 발행 워커에서도 났다. 이쪽은 보안 경계였다** (2026-08-28 · `publish_threads.py`)

편 빌더에서 잡은 지 몇 시간 만에 **같은 꼴이 발행 워커에서 나왔다.** 그런데 무게가 다르다 —
빌더 쪽은 «다시 돌리면 임포트가 깨진다» 였고, 이쪽은 **«승인 폴더가 사본마다 달라진다»** 다.

- **무엇이었나**: `hq = os.path.dirname(os.path.dirname(__file__))` 로 HQ 를 잡고
  `publish_approval` 을 붙였다. 그래서 같은 코드가 **어디 놓이느냐에 따라 다른 폴더**를 봤다.

| 워커 사본 | 보던 곳 |
|---|---|
| `C:\Users\ojaej\jj-company\scripts\` (운영 서버) | `C:\Users\ojaej\jj-company\publish_approval` |
| `C:\Users\ojaej\orca\jj-company\scripts\` (작업장) | `C:\Users\ojaej\orca\jj-company\publish_approval` |

- 🔴 **왜 더 무거운가.** 그 폴더는 **잠금의 경계**다. 경계가 사본마다 다르면
  ① 프로브가 «에이전트가 못 쓴다» 를 실증해도 **어느 폴더에 대한 것인지** 흐려지고
  ② 배치가 옮겨 둔 승인을 워커가 **못 보는** 조합이 생기며
  ③ 반대로 **프로브가 지키지 않는 폴더**에 승인을 두고 도는 조합도 생긴다.
  ③ 이 가장 나쁘다 — 장치는 통과인데 지키는 대상이 다른 것이다.
- 🔴 **셋 중 하나만 상대 계산이었고, 하필 그 하나가 경계를 쥐고 있었다.**
  배치(`move-approval.bat` `DST`)와 래퍼(`publish-threads.ps1` `$Hq`)는 처음부터 절대경로였다.
  **여럿이 같은 값을 들고 있을 때, 표기 방식이 하나만 다르면 그 하나가 어긋난다.**
- **조치 (2026-08-28)**: `HQ` 를 모듈 상수로 박고 `APPROVAL_DIR`·`REPORTS_DIR` 을 거기서 뽑았다.
  덧붙여 **자체 검사가 세 파일을 열어 실제로 대조한다** — `move-approval.bat` 의 `set "HQ="` 와
  `publish-threads.ps1` 의 `$Hq` 를 읽어 워커 상수와 견준다. 선언만 맞추면 다음 사람이
  한 곳만 고치고 지나가므로, **어긋나면 워커가 뜨지 않게** 했다.
- **역검증 실측**: 작업장 worktree 사본에서 돌려도 `C:\Users\ojaej\jj-company\publish_approval` 을
  가리켰고(종전이면 `.wt\abspath\publish_approval` 이었다), `cwd` 를 `C:\Windows` 로 바꿔도 같았다.
  승인이 없으므로 **발행 없이 멈췄다**(`STATUS: OK (부분: 승인 파일 없음)` · publish 호출 0).
- **문안 후보 보강**: 15번 본문의 셋에 하나를 더한다 —
  **«여러 파일이 같은 경로를 들고 있으면 표기 방식을 통일하고, 자체 검사가 그 값들을 서로 대조한다.»**
  경로가 한 군데면 상대·절대가 취향이지만, **여러 군데면 그것은 정합성 문제**다.
- **JJ 판정**: 대기 (보강한 문안으로).


### 16. 🔴 산출물의 **생성 기계와 소비 기계가 다른데 전달 경로가 없다** (2026-08-29 등재 · 같은 날 고침)

**15번의 형제이되 유형이 다르다.** 15번은 «상대 경로가 사본 위치를 따라간다» 였다. 이건 한 겹
바깥이다 — 경로가 상대라서 **두 기계로 갈렸고**, 갈린 두 자리 사이에 **전달 수단이 아예 없었다.**

- **무엇이 있었나.** `dist_transform.py` 의 `repo_root()` 가 `__file__` 에서 레포 뿌리를 거슬러
  올라가 `reports\` 를 만들었다. 작업장(`orca\jj-company`)에서 돌리면 작업장에, 운영 서버
  (`jj-company`)에서 돌리면 운영 서버에 떨어졌다. 그런데 이 원고를 **소비하는**
  `publish_threads.py` 는 `HQ = C:\Users\ojaej\jj-company` 로 **못박혀 있어 운영 서버만 본다.**
- 🔴 **`reports\` 는 `.gitignore` 다.** 그래서 `git pull` 로도 안 넘어간다 — 두 자리 사이에
  **어떤 전달 경로도 없었다.** 15번은 «엉뚱한 곳을 가리킨다» 였지만 이건 **«가리킬 다리가 없다»** 다.
- **실측 (2026-08-29)**: ep35~38 유통 원고 **4건(브리프 포함 13파일)이 전부 작업장에만** 있었고
  운영 서버 `reports\` 에는 dist 원고가 **0건**이었다. 워커를 그대로 돌렸으면
  «원고를 못 찾았다 → `STATUS: FAIL manuscript-missing`» 으로 끝났을 것이다.
- **고친 방식 — 상수로 못박았다(§0 4층 ①).** `REPORTS_DIR` 을 절대경로 상수로 두고
  `repo_root()` 를 **지웠다**(남겨 두면 다음 사람이 다시 쓴다). 어디서 돌리든 한 자리에 떨어진다.
  - **«복사 단계를 생성 스킬에 박기» 를 고르지 않은 이유**: 그 단계는 사람이나 세션이 건너뛸 수
    있고, 건너뛰면 **조용히** 어긋난다 — 파일은 만들어졌고 게이트도 통과하므로 아무도 모른다.
    상수는 건너뛸 단계 자체를 없앤다. 층이 ③ 과 ① 로 갈린다.
  - 자리가 없으면 **만들지 않고 죽는다** — 자리가 없다는 것은 «운영 서버가 없다» 는 뜻이고,
    그때 조용히 만들면 워커가 영영 못 찾을 곳에 원고를 쓰게 된다.
- **남은 몫**: 같은 꼴의 «생성↔소비 기계가 다른» 산출물이 더 있는지 훑지 않았다.
  후보는 `logs\audit-data\`·`logs\scout-data\`·`shots\` 다. **전수 조사는 미착수.**

### 17. 사람이 직접 부르는 실행 경로에 **판본 확인 앞단이 없다** (2026-08-29 등재 · **같은 날 JJ 판정 → 1번 채택 · 고침**)

- **무엇이 있었나.** 스케줄 회차는 정관 §4 대로 시작할 때 `git pull` + `deploy-skill` 로
  운영 서버와 스킬 라이브를 최신에 맞춘다. 그런데 **사람이 손으로 부르는 경로에는 그 앞단이 없다.**
  `publish-threads.ps1` 은 lock → 프로브 → 워커로 바로 간다.
- **실측 (2026-08-29)**: 운영 서버가 `c010f43` 에 머물러 있었다. 그날 머지한 #110·#111·#112 가
  안 내려가 있었고, 그 상태로 발행 명령을 돌렸으면 **영수증·재기동 조정·중복 검사가 없는
  옛 워커**가 돌 뻔했다. 정확히 정관 §4 «고친 것이 언제부터 도는지» 가 가리키는 자리다.
- 🔴 **이 편(ep39)이 첫 실발행이라 하필 가장 비싼 회차에서 걸린다** — 되돌림 비용이 0이 아닌
  동작인데 옛 코드로 돌면 중복 발행 방지 장치가 통째로 없다.
- **선택지 둘.**
  1. **래퍼 시작부에 `git pull` 을 넣는다.** 스케줄 회차와 같아진다. 다만 발행 직전에 코드가
     바뀌는 것이라 «승인한 판본으로 도는가» 가 흐려질 수 있다.
  2. **판본만 확인하고 어긋나면 멈춘다.** `git fetch` 후 로컬 `main` 이 `origin/main` 뒤면
     `STATUS: FAIL stale-worker` 로 중단하고 사람에게 `git pull` 을 시킨다.
     `check-repo-guard.ps1` 이 이미 같은 판정을 한다 — 그것을 부르면 된다.
- **JJ 판정 (2026-08-29): 1번 채택.** 래퍼 시작부에서 `git pull` 을 돌려 **스케줄 경로와
  같게** 만든다. 2번(확인하고 사람에게 넘기기)은 매 회차 사람 손이 한 번 더 들고, 그 손이
  빠지는 날이 곧 옛 판본으로 도는 날이다 — 사람에게 맡기는 단계를 하나 더 만드는 것이
  이 항목이 생긴 이유와 같은 꼴이다.
- **구현**: lock 획득 직후·프로브 **앞**에 둔다. 실패하면 **옛 판본으로 진행하지 않고**
  `STATUS: FAIL stale-version` 으로 멈춘다. 재시도는 기존 `Invoke-GitPullRetry`(5초 간격 3회)를
  그대로 쓴다 — 절전 후 동시 기동의 ref 경쟁은 이미 그것이 흡수한다(19번 사고).
- 🔴 **이 pull 이 새로 고치는 것은 «워커» 이지 «래퍼» 가 아니다.** PowerShell 은 시작할 때
  `.ps1` 을 통째로 읽으므로, 래퍼 자신의 변경은 **다음 회차부터** 돈다(정관 §4 «고친 것이 언제부터
  도는지»). 파이썬 워커는 pull 뒤에 새 프로세스로 뜨므로 이번 회차부터 새 코드다.
- **역검증 (2026-08-29 실측)**: 임시 클론 둘을 만들어 **원격 주소 하나만** 다르게 했다.
  살아 있는 쪽은 `operations server now at 1016002` 를 찍고 지나갔고, 원격을 없는 자리로 돌린
  쪽은 `STATUS: FAIL stale-version` 으로 멈췄다 — **프로브에도 워커에도 닿지 않았다.**
  변조가 실제로 먹었는지(원격 주소 · 수동 `git pull` 종료코드 1)를 **먼저 확인하고** 판정했다(L-009).

### 18. 🔴 **사람이 손으로 넣은 정본이 커밋되지 않아 «넣었는데 없는» 상태가 된다** (2026-08-29 등재 · **같은 날 JJ 판정 → 후보 ① 채택 · 두 겹으로 고침**)

**16번과 같은 계열이다 — «전달 경로 부재».** 16번은 «생성 기계와 소비 기계가 달랐고 다리가
없었다» 였고, 이건 **«사람과 레포 사이에 다리가 없었다»** 다. 셋을 나란히 두면 같은 뿌리가 보인다:

| # | 무엇과 무엇 사이 | 왜 안 건너갔나 |
|---|---|---|
| 15 | 코드와 그 코드가 찾는 파일 | 상대 경로가 사본 위치를 따라갔다 |
| 16 | 생성 기계와 소비 기계 | 두 자리가 갈렸고 `reports\` 가 gitignore 라 다리가 없었다 |
| **18** | **사람과 레포** | **파일을 놓기만 하고 커밋하지 않았다 — 작업 트리에만 있었다** |
| **21** | **작업 트리와 어느 레포든** | **`workshop/` 이 통째로 gitignore 라 추적 대상이 아니다 — 놓을 자리가 없다** |

- **무엇이 있었나 (2026-08-29)**: JJ 가 제작 지시서 원문을
  `docs\directives\제작지시서_v1_2026-08-29.md` 에 넣었다고 알렸고 파일은 실제로 거기 있었다.
  그런데 **`git status` 에 `??` 로 뜨는 untracked** 였다. 결과로 ⓐ `origin/main` 에 없고
  ⓑ **운영 서버에 안 갔고**(거기 `docs/directives/` 엔 다른 두 파일뿐이었다)
  ⓒ `git clean` 한 번이면 사라지는 상태였다. **«정본» 이라고 부르는 파일이 정본 저장소 밖에 있었다.**
- 🔴 **그 앞 회차에는 파일이 아예 없었다.** 세션이 전수 검색(작업장·운영 서버·전 원격 브랜치·
  Downloads·Desktop·Documents·워크숍)으로 «없다» 를 확인하고 반영을 멈췄다. 원인은
  **`docs/directives/` 폴더가 그때 `main` 에 없었고 PR 브랜치에만 있었다는 것** — 사람이 저장하려던
  자리가 존재하지 않았다. 폴더가 머지된 뒤에야 넣을 수 있었고, 넣은 뒤에도 커밋이 안 됐다.
  **두 번 다 «넣었다» 와 «닿았다» 가 갈렸다.**
- **고친 방식**: 원문을 커밋하고 `docs/directives/README.md` 에 규칙을 넣었다 —
  «**원문은 커밋한다**. 작업 트리에만 있으면 `git clean` 한 번에 사라지고 운영 서버에도 안 간다».
  같이 넣은 것: JJ 개정 각주 꼴(원 조항을 덮어쓰지 않고 인용 블록으로 덧댄다).
- **남은 몫 — 이것은 ③(검사)이 아니라 ④(못 잡는다)다.** 사람이 파일을 놓는 행위를 코드가
  가로챌 수 없다. 지금 있는 것은 **다음 세션이 그 파일을 쓰려 할 때 `git ls-files` 로 확인하는
  절차**뿐이고, 그것은 규율이다. 구조로 닫으려면 후보가 둘이다:
  1. **`check-repo-guard.ps1` 에 한 줄** — 값싸지만 그 폴더에만 걸린다.
  2. **세션 착수 검사** — `check-repo-guard` 가 곧 그것이라(정관 §3) **1번과 같은 자리였다.**
- **JJ 판정 (2026-08-29): 후보 ① 채택 — 다만 «pre-commit» 쪽으로.** 그래서 **두 겹**이 됐다.
  - **겹 1 · `check-repo-guard.ps1`** (PR #119) — 정본 폴더 untracked 를 FAIL 로 찍는다.
    🔴 **이건 규율이다.** 정관 §3 이 «세션은 착수 시 돌린다» 고 적지만 **강제하는 장치가 없고**,
    실측으로 **2026-08-29 한 세션이 열 회차 넘게 돌면서 한 번도 안 돌렸다.**
  - **겹 2 · `scripts/githooks/pre-commit`** — **매 커밋마다 돈다.** 사람이 놓는 순간은 못 잡아도
    **다음 커밋에서 반드시 잡는다.** 겹 1 이 규율이라 이쪽이 실질이다.
  - 🔴 **훅은 «본 트리» 를 건너다본다.** 사람은 본 트리에 놓는데 훅은 worktree 에서 돌고,
    worktree 는 자기 체크아웃만 본다 — 로컬로 재면 **빈 목록**이 나온다(실측: 같은 순간 본 트리는
    표본을 보고 worktree 는 `[]`). 그대로 뒀으면 **검사가 영원히 통과**했을 것이다.
    `--git-common-dir` 의 부모가 본 트리다.
- **남은 위험 (§0 4층 ④ — 적어 둔다)**:
  ⓐ **놓고 아무도 커밋하지 않는 구간**은 훅이 못 본다. 그 구간은 겹 1 뿐이고 그것은 규율이다.
  ⓑ **`core.hooksPath` 가 없는 새 클론**에서는 훅이 안 돈다 — 지금 설정된 클론은 작업장·운영 서버
  둘이고, 목록과 설치법은 `scripts/githooks/README.md` 에 있다.
  ⓒ 대상은 **`docs/directives/` 뿐**이다. `reports\`·`logs\` 는 의도적 gitignore 라 넓히지 않았다.

### 19. 🔴 **승인 폴더 «쓰기 차단» 이 도구만 막고 스크립트는 못 막는다** (2026-08-29 등재 · 실측)

**정관 §0 «발행은 매체별로 가른다» 가 세운 3확인의 ②** — «승인 파일이 **에이전트 손으로
만들어지지 않았을 것**» — 은 «에이전트 권한에서 `publish_approval\` 을 빼고, 워커가 뜨기 전에
그 폴더에 실제로 한 번 써 보고 거부되는지 확인» 으로 재기로 돼 있다.

🔴 **실측(2026-08-29): 파이썬 파일 API 로는 그 폴더에 써진다.**

```
io.open(r"C:\Users\ojaej\jj-company\publish_approval\_probe.json", "w") → 성공
```

차단은 `settings.json` 의 `Edit(경로)` deny 이고 **그것은 «도구 호출» 을 막는다.** 에이전트가
Bash 로 파이썬 한 줄을 돌리면 그 경로는 열려 있다 — §4 가 이미 «`--allowed-tools` 는 게이트가
아니다» 로 같은 계열을 적어 둔 자리이고, 이번 것은 그 한 겹 바깥이다.

**그래서 지금 프로브가 증명하는 것은 «막혔다» 가 아니다.** 프로브를 그대로 두면 «봤다» 는
표시만 남고 실제로는 열려 있다 — 정관 §0 «감지 장치가 값을 담는지» 에 정면으로 걸린다.

**후보**
- ① **OS 권한으로 막는다** — 그 폴더의 ACL 에서 현재 사용자 쓰기를 빼고, JJ 가 쓸 때만
  승격한다. 도구·스크립트·셸을 가리지 않아 ① 층(구조)이다. 비용: JJ 의 이동 절차가 한 단계
  늘어난다(`move-approval.bat` 이 승격을 요구하게 된다).
- ② **다른 사용자·다른 기계에 둔다** — 승인 폴더를 에이전트가 아예 못 보는 자리에 두고
  워커는 읽기만 한다. 가장 확실하지만 운영 부담이 크다.
- ③ **서명을 파일 위치가 아니라 «키» 로 바꾼다** — JJ 만 가진 키로 서명하고 워커가 검증한다.
  폴더 권한과 무관해지지만 키 관리가 새로 생긴다.
- ④ **지금 상태를 «못 막는다» 로 적고 프로브 문구를 고친다** — 최소 조치. 발행이 아직
  0건이라 당장의 피해는 없지만, **조항이 요구한 ②는 성립하지 않는다**고 정직하게 남긴다.

🔴 **JJ 판정이 필요하다.** 이 항목이 닫히기 전에는 §0 «발행은 매체별로 가른다» 의 3확인 중
②가 **장치 없이 문장으로만 있다.** 발행을 여는 조건이 아직 서지 않았다는 뜻이다.

**지금 확인된 것 / 확인 못 한 것**
- 🟢 `publish_approval\` 은 **비어 있다** — 자기 서명이 일어난 흔적은 없다.
- 🔴 **폴더를 누가 만들었는지는 못 잰다** — 2026-08-29 11:04 에 생겼고 git 밖이라
  작성자가 남지 않는다(§0 4층 ④).

### 19번 — 승격 조건과 선결 조건 (2026-08-29 추가)

**승격 조건.** 실장이 끝나면 `permission_probe.py` 의 `PROBE_OS_LAYER` 를 **판정으로
올린다** — `open` 이면 그 회차는 `STATUS: FAIL probe-os-layer` 다. 지금 올리지 않는 이유는
**열려 있는 것이 사실**이라 전 회차가 죽기 때문이고, 그 사실 자체는 매 회차 출력에 남는다.

🔴 **선결 조건 — 스케줄 자동 발행을 열기 전에 19번이 닫혀 있어야 한다.** 사람이 손으로
부르는 지금은 «JJ 가 이동했는가» 를 사람이 안다. 스케줄이 스스로 발행을 시작하는 순간
그 앎이 사라지고, **승인 파일의 출처가 유일한 근거**가 된다. 그때 OS 층이 열려 있으면
근거가 근거가 아니다.

**판정 기록은 `docs\clause-backlog.md` C-29 에 있다** (권고 ⓑ · ⓐ·ⓒ 를 뺀 사유).

### 20. 🔴 **원고 층과 발행 층 사이에 «첨부» 필드가 없다 — 선언한 영상이 말없이 사라진다** (2026-08-30 등재 · 실전 사고)

**무슨 일**: ep39 첫 실전 발행(2026-08-30 01:38)에서 **텍스트 5포스트는 정상**으로 나갔고
**영상 2건(P1 템플릿·P4 쇼핑)은 하나도 안 붙었다.** 게이트·감사·리포트 전부 `STATUS: OK`.

**원인 — 「떨어진」 것이 아니라 실린 적이 없다.**
- `scripts\publish_threads.py:510` — `params = {"media_type": "TEXT", "text": body}`.
  `media_type` 이 **이 한 곳뿐이고 상수**다. `video_url`·`image_url`·`children` 이 **파일에 없다.**
- `parse_posts()` 는 `### P<n>` 다음 **첫 코드펜스만** 읽는다. 원고의 `**첨부** …` 줄은
  **펜스 밖**이라 파싱 대상이 아니다. 파일 전체에 「첨부」 **0회**.

**왜 조용했나 — 검사가 헛돈 게 아니라 그 축을 아무도 안 봤다.**
`distcheck [10-1]~[10-6]` 이 검증한 «실물»은 **디스크의 mp4**이지 «게시된 것»이 아니다.
`[10-5]` 의 「선언 형상 = 실물」조차 ffprobe 로 로컬 파일을 잰 것이다.
**선언 ↔ 실제 전송을 대조하는 자리가 어디에도 없다.**

**설계 문서가 이미 그 전제를 적어 두었다.** `docs\workers\publish-threads.md` 의
「왜 Threads 만인가」 표는 네 장벽(캐러셀 상한·JPEG 강제·무음 영상·**공개 HTTPS URL**)이
Threads 에선 **「해당 없음 — 첨부 0건」** 이라 적는다. 워커는 **첨부 0건 전제로 설계됐다.**
뒤에 원고 층이 첨부를 갖게 되면서 그 「해당 없음」이 **조용히 거짓이 됐다.**
같은 문서가 **고정 댓글은 「이 워커 밖이다」라고 명시**하는데 첨부에는 그런 줄이 없다 —
**생각한 배제는 적혔고 생각 못 한 배제는 안 적혔다.**

**선결 장벽**: Threads API 도 미디어를 **공개 URL**(`video_url`/`image_url`)로 받는다.
로컬 업로드 경로가 없다. 원고의 첨부 값은 `C:/Users/...` **로컬 경로**라 그대로는 못 쓴다.
**공개 URL 을 어디서 얻을지가 정해지기 전에는 어떤 구현도 성립하지 않는다.**

**후보**
- ① **막는다 (가장 싸고 지금 바로 참이 된다).** 워커가 원고에서 `**첨부**` 줄을 세어
  **1건이라도 있으면 발행을 거부**한다. 「못 붙인다」를 **거부로** 만들어 조용한 누락을 없앤다.
  정관 §0 4층 ①. 구현이 서기 전까지의 정답으로 보인다.
- ② **원고 층에서 뺀다.** Threads 원고에는 첨부 표를 만들지 않는다. 대신 `[10-4]` 가
  요구하는 「영상 출처:」 라벨도 같이 뺀다 — **라벨만 남으면 매달린 참조**가 된다(이번 사고).
- ③ **구현한다.** 공개 URL 확보(선공개 또는 X 원본 URL 통과 여부 실측) → `media_type=VIDEO`
  → **게시 후 실물 조회로 `media_type` 재확인**. 셋째 걸음이 없으면 같은 사고가 반복된다.
- ④ **사람이 붙인다고 문서에 못박는다.** 지금 상태를 «의도»로 만든다. 🔴 권하지 않는다 —
  워커의 존재 이유가 사람 손을 줄이는 것이고, 무엇보다 **아무 장치도 안 는다.**

🔴 **JJ 판정 대기.** ①은 다른 후보와 배타적이지 않다 — ③이 서기 전까지의 안전판으로 같이 갈 수 있다.

**곁가지 — 감사가 이 축을 영원히 못 본다.** `run_audit` 이 보는 것은 로그의 `STATUS:` 줄
하나뿐이다(`--tasks publish-threads` 실측: `RUNS_CHECKED=2 · RUNS_VERDICT=OK`).
첨부 결함은 구조적으로 이 경로에 안 올라온다 — **못 잡는다고 여기 적는다**(§0 4층 ④).

**실측 근거 전문**: `reports\2026-08-30_postmortem_ep39.md` (로컬 · 커밋 안 함).

### 21. 🔴 **`workshop/` 소스 653개(9.1MB)가 어느 레포에도 없다 — 기계 소실 = 영구 소실** (2026-08-30 등재 · **같은 날 ㉮ 착수 — 텍스트·`_official` 은 닫혔고 렌더물 1.74GB 만 남았다**)

**15·16·18 과 같은 «전달 경로 부재» 계열이고, 그중 가장 넓다.** 15는 «코드가 파일을 못 찾는다»,
16은 «두 기계 사이에 다리가 없다», 18은 «사람이 놓고 커밋을 안 했다» 였다. 이건 **놓을 자리
자체가 없다** — 사이트 레포 `.gitignore` 2행이 `workshop/` 이라 그 아래는 추적 대상이 아니다.

**실측 (2026-08-30).** `workshop/` 텍스트 소스를 **두 레포(`tomangchi-skill`·`jj-company`)의
`origin/main` 내용 해시**와 개행 정규화해 대조했다.

| 갈래 | 개수 | 크기 | 판정 |
|---|---|---|---|
| 두 레포에서 복구됨 | 34 | 0.2 MB | 🟢 |
| **어디에도 없음** | **653** | **9.1 MB** | 🔴 **기계와 함께 사라진다** |
| 렌더물(png·mp4 등) | 3250 | 3843 MB | ⚪ 다시 만들 수 있다 |
| `_official/` 원본 소재 | 21 | 49 MB | 🟡 재수집은 되나 **«그때 그 원본» 보장 없음** |

잃는 653개의 자리 — `01_발행완료` 467 · `02_제작중` 93 · `03_소재큐` 24 · `assets` 17 ·
`스캔로그` 16 · `99_지원제출` 11 · 그 밖. 확장자로는 `.py` 196 · `.txt` 160 · `.md` 156 ·
`.json` 109 · `.html` 32.

**무게가 큰 것 넷.**
- **편별 선언 45개**(`build_epNN.py`·`_facts.py`) — 편의 사실·크레딧·형상·게이트 선언이
  전부 여기 있다. 발행된 39편의 «무엇을 근거로 뭐라고 썼는가» 가 이 파일들이다.
- **공용이 아닌 제작 코드 87종 164개** — `_plate.py`(판 맞춤)·`_beforeafter.py`(전후 합성)·
  `_ownart.py`·각종 `_shoot_*.py`. ep38 §0 조사에서 «판 처리 수치를 만든 코드가 커밋된
  코드에 없다» 고 적었던 그 파일들이다.
- **`스캔로그/` 16개** — 🔴 **자회사 정본이다.** 정관 §2 가 content-scout 에게 **유일한
  출장지 쓰기 예외**를 준 폴더이고 SKILL §5.5-3-1 이 «그 폴더 정본» 이라 못박았다.
  정본이라고 적어 두고 **백업 경로가 없다.**
- **`발행로그.md`** — 채널 발행 원장.

🔴 **툴체인은 이 안건이 아니다 (2026-08-30 정정).** 처음 조회에서 `00_브랜드에셋` 7종을
«내용이 다름» 으로 셌는데 **전부 CRLF 차이**였다 — 클론 **워킹 트리**를 바이트로 비교한
탓이고, `skill-drift-audit` 은 `origin/main` 을 개행 정규화해 보므로 같은 날 🔴 0 을 냈다.
**감사가 맞고 내 첫 자가 틀렸다.** 툴체인 19/19 는 스킬 레포에서 그대로 복구된다.
정본 이중화 자체는 **15번과 `docs\plan-brand-assets-move.md`** 가 이미 다룬다.

---

**후보 (구현 없음 · JJ 판정 대기)**

**㉮ workshop 소스 전용 사설 레포 신설** — `.py`·편별 선언·`_official/` 만 추적하고
렌더물은 계속 제외.
- 🟢 **잃는 것을 정확히 덮는다.** 9.1MB + 49MB 라 무게도 작고, 이력이 남아 «언제 무엇이
  바뀌었나» 를 되짚을 수 있다. 편별 선언은 그 이력이 곧 편의 근거 기록이다.
- 🟢 **사설**이라 미발행 편·원본 소재가 밖으로 안 나간다.
- 🟡 **정관 §2 저촉 검토 필요.** 워크숍은 «읽기 전용(에이전트)» 자리다. 레포를 그 안에
  두면 `.git` 쓰기가 생기므로, **사람이 만들고 사람이 커밋**하는 자리로 두거나
  `스캔로그` 예외처럼 **조항에 명시**해야 한다. 조용히 열지 않는다.
- 🟡 레포가 넷이 된다 — 동기화 규율이 하나 는다(정관 §3 worktree 규칙도 같이 걸린다).

**㉯ 사이트 레포 `.gitignore` 를 «렌더물만 제외» 로 좁힘**
- 🔴 **사이트 레포는 PUBLIC 이다**(실측: `tomangchi-lab/tomangchi-lab.github.io` = PUBLIC).
  `02_제작중` 미발행 편과 `_official/` 원본이 **발행 전에 공개**된다 — 워커 정본이
  인스타 API 를 물린 바로 그 장벽(«발행 전 산출물 선공개»)을 우리 손으로 여는 셈이다.
- 🔴 편 폴더에 `caption.txt`·`발행팩.md`·승인 관련 문안이 섞여 있어 **무엇이 공개되는지
  한 줄씩 봐야** 한다. 검토 비용이 크고 한 번 push 하면 되돌림 비용이 높다(§0).
- 🟢 새 레포·새 규율이 안 는다는 것 하나.
- **권하지 않는다.**

**㉰ 주기 아카이브(zip → 드라이브)만**
- 🟢 가장 싸다. 텍스트 소스만 9.1MB 라 zip 하나면 끝이고, 전달 도구(`deliver.py`)가 이미 있다.
- 🔴 **이력이 없다.** «언제부터 이랬나» 를 못 되짚는다 — 편별 선언은 그 질문이 자주 붙는다.
- 🔴 **드라이브는 G: 까지만 검증된다** — 클라우드 반영은 Drive API 조회가 안 돼 **확인 불가**
  (기존 실측). 백업의 «도착» 을 증명할 수 없는 백업이다(§0 «감지 장치가 값을 담는지»).
- 🟡 주기를 걸면 스케줄 작업이 하나 는다. A등급이라 등록 자체는 걸림이 없다.

✅ **㉮ 착수 완료 (2026-08-30).** `OhjaejunO/tomangchi-workshop`(PRIVATE) 신설 · 초기 동기
**710개 · 58.19 MB** · 레포↔zip↔워크숍 **해시 3자 일치(불일치 0)**. 단방향은 훅이 아니라
**구조**로 닫았다 — 워크숍에 그 레포의 `.git` 이 없어 `pull`·`checkout` 이 닿을 경로가 없다
(§0 4층 ①). 절차·기계 간 규칙은 `docs\workshop-repo-sync.md`. ㉰(zip)는 **병행 유지**.

🔴 **남은 것은 «다시 못 만드는데 안 담긴 렌더물» 1.74 GB 다** — 옛 편 표지 시안·릴스 965MB ·
`_scenes/` 661MB · `_assets/` 62MB · 브랜드 이미지 53MB. **지금은 zip 백업에도 없어 어디에도
사본이 없다.** 담으면 레포가 1.8GB 가 되어 Git LFS 없이는 실용적이지 않다. 후보 넷(ⓐ LFS로
`_scenes/`만 · ⓑ 별도 레포 · ⓒ zip 범위 확대 · ⓓ 옛 시안은 포기)을 적어 두었고 **JJ 판정 대기.**

✅ **ⓒ·ⓓ 판정 반영 (2026-08-30 JJ).** zip 백업 범위를 넓혔고, 남길 것과 버릴 것을 갈랐다.

**ⓒ — 자산은 백업에 넣는다.** `_scenes/`(모델 생성) · `_assets/`·`assets/`(제공·수집) ·
`00_브랜드에셋` 이미지. 첫 회차 실측: **1,265개 · 966.34 MB · 24.6초**, 멤버 전수 해시 일치,
워크숍 불변 확인, 드라이브 사본 해시 일치. 🔴 한 벌이 ~1GB 라 **로컬 보관을 12개 → 4개**로
줄였다 — 이력은 이제 레포(`tomangchi-workshop`)가 지므로 zip 은 최근 것만 둔다.

**ⓓ — 옛 편 시안·아카이브는 «백업 제외 · 소실 감수».** 삭제하지 않는다. 워크숍에 그대로
두고, 기계가 죽으면 잃는 것으로 받아들인다. **506개 · 823.9 MB** (`_archive/`·`_retired/`·
`_폐기/`·`src/`(다시 받을 수 있는 원본)·`cover/v1·v2` 시안·프레임 덤프).

🔴 **선행 실측이 그 더미에서 «발행 채택본» 을 건져 냈다.** 「소실 감수」는 버려도 되는 것에만
쓸 수 있는 말인데, 더미 안에 **실제로 나간 산출물**이 섞여 있었다 — ep1 릴스
(`reel_tomangchi_1.mp4`)·힉스필드 릴스·채널 광고·릴스 표지 프레임 **10개 59.0 MB**. 덱 번호
체계 밖에서 발행되는 산출물이라 빌더가 다시 만들지 않는다. **규칙으로 건져 보존 범위에
넣었다.** 실측 없이 「옛 시안은 버린다」로 갔으면 **나간 것을 버릴 뻔했다.**

🔴 **자 하나를 도중에 고쳤다.** 첫 판은 «발행로그가 이름을 댔는가» 를 먼저 봐서
`08_kit.mp4`·`06_follow.png` 같은 **덱 산출물 45개**가 채택본으로 둔갑했다. «코드가 만든다» 를
**먼저** 거르도록 순서를 바꾸니 10개로 정리됐다 — 술어 순서가 판정을 바꾼 자리다.

**세 안은 배타적이지 않다.** ㉰ 를 오늘 안전판으로 걸고 ㉮ 를 본안으로 두는 조합이 가능하다 —
20번에서 ①(거부)과 ③(구현)을 같이 둔 것과 같은 꼴이다. 🔴 **JJ 판정 대기.**

---

### 22. 토망치랩 자산이 **세 소유자에 흩어져 있다** (2026-08-30 등재 · **같은 날 워크숍 한 건 이전 — 나머지 판정 대기**)

| 레포 | 소유자 | 공개 | 담는 것 |
|---|---|---|---|
| `tomangchi-skill` | `OhjaejunO` (개인) | PRIVATE | 스킬·게이트 정본 |
| `tomangchi-lab.github.io` | `tomangchi-lab` (org) | **PUBLIC** | 사이트·킷 |
| `tomangchi-workshop` | `OhjaejunO` (개인) | PRIVATE | 워크숍 소스 백업 (2026-08-30 신설 · **같은 날 `PU3-Lab` 에서 이전**) |

**왜 적어 두나.** 셋이 한 트랙(토망치랩)의 자산인데 소유자가 갈려 있다 — 권한·수명·양도가
세 곳에서 따로 논다. 지금 사고가 난 것은 아니고 **불편도 아직 없다.** 다만 «어디에 무엇이
있는가» 를 매번 기억으로 재구성하게 되는 자리라, 흩어졌다는 사실 자체를 기록한다.

- **후보**: ⓐ `tomangchi-lab` org 로 모은다(내용과 소유가 맞는다) ⓑ `OhjaejunO` 로 모은다
  ⓒ 그대로 둔다(각자 이유가 있다).
- **비용은 낮다** — GitHub transfer 로 옮겨지고 리다이렉트도 남는다. 서두를 이유가 없다.
- 🔴 **나머지 두 건은 JJ 판정 대기.**

#### ✅ 워크숍 레포 이전 (2026-08-30 JJ · 웹에서 실행)

**`PU3-Lab/tomangchi-workshop` → `OhjaejunO/tomangchi-workshop`.** 사유는 위 후보 선택이
아니라 **자산 경계**다 — `PU3-Lab` 은 팀 조직이고 그 안에 회사 자산이 있으면 안 된다.
즉 이 건은 ⓑ 를 택한 것이 아니라 **놓이면 안 되는 자리에서 뺀 것**이고, 남은 두 건의
판정(ⓐ/ⓑ/ⓒ)은 그대로 열려 있다.

🔴 **리다이렉트에 기대지 않고 문서·코드의 주소를 전부 갱신했다.** GitHub 리다이렉트는
`git clone`·`git push` 를 계속 통과시키므로 **틀린 주소가 적힌 채로도 아무 신호가 없다** —
정관 §0 «조용히 실패하는 코드» 의 주소판이다. 게다가 리다이렉트는 **옛 이름을 누가 다시
만들면 끊긴다.** 갱신한 자리 (2026-08-30 실측 · 7군데):

| 파일 | 자리 |
|---|---|
| `docs\workshop-repo-sync.md` | 머리 «레포» 줄 · 단방향 도해 · 온보딩 `git clone` 명령 |
| `docs\infra-backlog.md` | 21번 ㉮ 착수 기록 · 위 표 |
| `docs\plan-workshop-source-repo.md` | 경로 A 상태 줄 |
| `scripts\workshop_repo_sync.py` | 머리 도해 · **`REMOTE` 상수** |

`REMOTE` 상수가 이 중 유일하게 **도는 코드**다 — 나머지는 사람이 읽는 자리고, 이것은
리다이렉트가 사라지는 날 동기가 멈추는 자리다.

🔴 **그런데 «도는 자리» 를 한 칸 덜 셌다 (2026-08-30 PR 검토에서 실측).** `REMOTE` 는
`workshop_repo_sync.py:197-200` 에서 **스테이징 클론에 `.git` 이 없을 때만** `remote add`
에 쓰인다. 즉 **이미 만들어진 클론에는 영원히 반영되지 않는다** — 운영 기계의
`C:\Users\ojaej\orca\tomangchi-workshop` 은 갱신 뒤에도 `origin` 이
`PU3-Lab/tomangchi-workshop` 그대로였고, 리다이렉트 때문에 **push 가 계속 성공해 아무 신호도
없었다.** 그 회차에 `git remote set-url` 로 고치고 `git ls-remote` 로 `fabe3e4` 도달을
확인했다(로컬 설정이라 커밋 대상이 아니다).

**세는 법이 틀렸던 것이다** — 도는 자리는 「상수」가 아니라 **「상수 + 그 상수로 이미
만들어진 것」** 이다. 상수는 **생성 시점에만** 읽히므로, 생성이 끝난 뒤의 값은 **다른 곳에
복사되어 살아 있다.** C-35 ③ 이 «도는 코드를 먼저 센다» 고 적은 그 계산에 이 칸을 넣는다.

🔴 **바꾸지 않은 자리가 하나 있다.** `tomangchi-workshop` 레포 안
`99_지원제출\그룹바이\텍스트_교체본_v2.md` 의 `github.com/PU3-Lab/factory-space` 는
**다른 레포**다. 낱말 일치로 일괄 치환했으면 남의 주소를 틀리게 고칠 뻔했다 —
치환 대상은 «`PU3-Lab`» 이 아니라 «`PU3-Lab/tomangchi-workshop`» 이다.

**남은 것**: 이전으로 `PU3-Lab` org 에 회사 자산은 없어졌다. `tomangchi-lab` org 의
사이트 레포는 **PUBLIC 이고 내용과 소유가 맞아** 이 사유에 걸리지 않는다.

### 23. 🔴 **제작 코드가 기계 경계를 못 넘는다 — 집 기계 렌더 실측이 «미수행» 으로 끝났다** (2026-08-30 등재 · **이월 · 폐기 아님**)

**지금 상태 — 집 기계는 «작업장(제작 미개방)» 이다.** 레포·훅·문서 층은 섰고(`STATUS: OK`
검사 4건), **제작 층은 서지 못했다.** 온보딩 4번 과제 «렌더 동일성 실측» 은
🔴 **수행하지 못했다** — 「같다」도 「다르다」도 아니다. **재지 못한 것**이고, 그래서
«집 제작 완전 개방» 을 아무 문서에도 적지 않았다(§0 «확인 안 되면 확인 불가»).
근거 전문은 `docs\onboarding-report-2026-08-30.md` 4절.

**막은 것 넷** (2026-08-30 실측):

| # | 막은 것 | 성격 |
|---|---|---|
| ① | Python 실설치 없음 (`python.exe` 0바이트 스텁 · `py` 없음) | 설치하면 풀린다 |
| ② | `numpy`·`Pillow` 없음 | 설치하면 풀린다 |
| ③ | `ffmpeg`·`ffprobe` 없음 | 설치하면 풀린다 |
| ④ | 🔴 **제작 코드 100개 파일이 `C:\Users\ojaej\…` 를 상수로 박았다** | **설치로 안 풀린다** |

🔴 **④ 가 본체이고, 나머지 셋과 성격이 다르다.** ①~③ 은 그 기계의 준비 부족이라 깔면
끝나지만, ④ 는 **코드에 박힌 결함**이라 무엇을 깔아도 남는다. ep39 만 봐도
`build_ep39.py`·`build_cover_fit.py`·`build_own.py`·`build_plate_video.py`·`hashtable.py`·
`packfill.py` 여섯이 같은 값을 박고 있고, `00_브랜드에셋\xreview.py` 는
`C:\Users\ojaej\jj-company\…` 까지 본다.

🔴 **대조표를 만드는 도구 자신이 걸려 있다.** 4번 과제가 요구한 «레포 기록 해시와 대조» 는
`hashtable.py` 가 돌아야 성립하는데 그것이 운영 기계 경로를 본다. **재려는 자가 못 서는
구조**라 «측정 실패» 가 아니라 «측정 불가» 였다.

🔴 **15번의 해결책이 이 결함을 만들었다.** 15번은 «상위 폴더 상대 계산» 이 발행 후 이동에서
끊기는 것을 고치려 **절대경로로 바꿨다.** 한 기계 안에서는 옳다. 그런데 절대경로는
**기계 경계를 넘는 순간 틀린다** — 이식성을 대신 내준 것이고, 두 기계 체제에서 그 대가가
지금 청구됐다. **둘 다 만족하는 답은 «절대경로» 도 «상대경로» 도 아니다.**

**후보 (구현 전 · JJ 판정 대기)**
- ① **경로 헬퍼 하나로 모은다** — 자기 위치에서 위로 거슬러 «워크숍 뿌리 표식»(예:
  `발행로그.md`)을 찾아 절대경로를 **실행 시점에 만든다.** 상대 계산의 이동 취약성과
  절대 상수의 기계 종속을 **둘 다 피한다.** §0 4층 **①(구조)** 자리 — 되면 검사가 필요 없다.
- ② 환경변수 + 기본값(`workshop_repo_sync.py` 의 `WS`·`STAGE` 가 이미 이 꼴이다).
  ①보다 싸지만 **환경변수를 안 걸면 조용히 운영 기계 경로로 돈다** — 기본값이 함정이 된다.
- ③ 검사로 잡는다(제작 코드에 하드코딩 경로가 있으면 FAIL). 🔴 **권하지 않는다** —
  ①로 닫을 수 있는 것을 ③에 두는 것이 §0 4층이 «가장 비싼 선택» 이라 적은 그 자리다.

🔴 **이 건은 이월이지 폐기가 아니다.** «집에서 제작할 일이 없으니 없던 일로» 가 아니라,
**지금은 안 열고 여는 조건을 적어 둔다** — 조건 없이 미루면 §0 «다음에 정한다» 조항이
가리키는 그것(미룬 것이 아니라 버린 것)이 된다.

**재개 조건 — 아래 중 하나라도 서면 연다**
1. **집 기계에서 편을 제작해야 하는 날이 온다** (운영 기계 부재·이동·고장 포함).
2. 셋째 기계가 붙는다 — 그때는 ④ 가 **두 기계가 아니라 N 기계 문제**로 커진다.
3. 15번의 JJ 판정이 내려진다 — 15번과 이 항은 **같은 상수를 반대 방향에서 당기므로**
   따로 정하면 한쪽이 다른 쪽을 되돌린다. **판정은 같이 한다.**

**여는 순서** (최소 경로 · PNG 카드 1장 대조까지)
1. Python 실설치(Store 별칭 말고) → `pip install pillow numpy`
2. ④ 를 푼다 — 위 후보 중 채택안으로. 🔴 **100개 파일이라 한 회차 작업이다.**
3. `02_what.png` 를 조판해 `검증로그.md` 5-3 의 `a54201a4ce885775` 와 대조.
   영상 카드까지 열려면 `ffmpeg`·`ffprobe`, 편 게이트까지 열려면 `tomangchi-skill` 클론.

**✅ 폰트 축은 닫혔다 (2026-08-30 · 양쪽 실측 대조 완료).** 집 기계가 남긴 지문(리포트 4-4)에
**운영 기계의 같은 다섯 줄을 떠서 맞댔고, 5종 전부 sha256·바이트가 일치**한다 —
`NotoSansKR-VF` `018174E8CDD366AF`(10,415,532) · `malgun` `7A183CF1C6C56B96`(13,459,196) ·
`malgunbd` `E8CBC0B2AFCC14FB`(12,600,392) · `consola` `CF00B507B3286870`(453,088) ·
`seguiemj` `F07CBD7886F4A1A5`(12,450,664). 경로도 같은 `C:\Windows\Fonts\` 다.

**이것이 값어치가 있는 이유** — 렌더가 갈릴 때 원인 후보는 «폰트» 와 «라이브러리 판본» 둘인데,
**한쪽을 미리 지웠다.** 나중에 조판이 열려 결과가 다르게 나오면 **폰트는 용의선상에서 빠진
채로 시작한다.** ④ 와 무관하게 **지금 잴 수 있는 유일한 축**이었고, 그래서 지금 쟀다.

🔴 **남은 축은 라이브러리 판본이다 — 그것은 못 쟀다.** 집 기계에 Python 이 없어
`Pillow`·`numpy` 판본을 **조회할 수조차 없었다**(①·② 때문에 «없다» 가 아니라 «확인 불가»).
①·② 가 풀리는 날 같은 방식으로 양쪽을 맞댄다.

---

## ✅ 끝난 것 (지우지 않는다)

| 항목 | 처리 | 근거 |
|---|---|---|
| ops-auditor 가 리포트를 못 쓰던 상태 | `tools` 에 `Write` 추가 | PR #50. 8/18 11:29 리포트가 🔴 즉시로 달아 둔 항목이었다 |
| A등급 부서의 `Bash` 보유 | `Bash` 제거 + 래퍼 선실행 구조 | PR #50. 역검증: **도구 부재**(`tool_uses: 0`) |
| 감사 데이터 한글 모지바케 | `PYTHONIOENCODING=utf-8` | PR #51. 실측: `Binary file matches` → `BACKUP_STATUS=확인 불가 (BACKUP_PATH 미설정)` |
| 열린 PR 을 아무도 안 보던 상태 | 아침 감사 점검 항목 6 신설 | PR #50. 근거: PR #45 가 이틀간 방치 |
| 리포트 부재가 «최초 실행»으로 읽히던 문구 | 점검 항목 7 신설 + 문구 교정 | PR #51 |
| 운영 서버 신선도 무경보 | 점검 항목 8 신설 | PR #51. 근거: **14커밋 뒤처진 채** 스케줄 가동 |
| job-scout · content-scout 가 리포트를 못 쓰던 상태 | 둘 다 `tools` 에 `Write` 추가 | PR #60. 상위 세션 대필로 `STATUS: OK` 로 보이던 것 |
| skill-drift 리포트에 판정이 없던 것 | 본문 마지막 줄에 `STATUS` 추가 | PR #60. 역검증: FAIL · OK **양쪽** 경로 |

## 착수 순서 제안

    0. gumsu 툴셋 14개 정리              ← JJ 수작업. 우리 쪽 손잡이는 다 뺐다
    1. vault_audit.py 별칭 해석          ← 측정을 먼저 고친다
    2. BACKUP_PATH 확정                  ← 독립. 지금도 가능
    3. 링크·고아·inbox 정리              ← 1번 이후
    4. _v3_bottom 가드 → 브랜드 정본 단일화  ← 별도 트랙

**2번은 1번을 기다리지 않는다** — 서로 독립이고, 백업은 모르는 상태로 두는 비용이 가장 크다.

## ⚪ 관찰 대상 (지금 조치 없음)

- **content-scout 쓰기 제한 위반 1건 (2026-08-21)** — 스캔 중 `logs\_scout_query.py` 를 만들었다 **스스로 삭제**했다. 파일은 남아 있지 않지만 `reports\` · 스캔로그 밖은 쓰지 않는다는 정관 §2 · 프롬프트 «쓰기 제한» 조항 밖이었다.
  - **JJ 판단: 기록만 하고 관찰한다 (2026-08-21).** 재발하면 그때 조치한다.
  - **재발 판독법**: `logs\scheduled\tomangchi-scout_*.log` 의 `cc|` 줄에 임시 스크립트 생성 언급이 또 나오는지. 또는 `logs\` 에 `_scout_*` 잔존물.
  - **근거**: `logs\scheduled\tomangchi-scout_20260821.log` 12:02:21.
  - ⚠️ 단, 쓰기 경로 자체는 PR #60 으로 **달라졌다** — `Write` 가 생겼으므로 임시 스크립트로 우회할 동기가 줄어든다. **재발하면 동기가 다른 것이니 사유를 먼저 본다.**

## ⚪ 출처 확인 필요

- **`gamebuilders-day23` worktree** (`.wt/gamebuilders`, `d87443e`) — **손대지 않는다 (2026-08-18 확정).** factory-space 계열 다른 트랙 산출물로 보이나 **이 레포의 어느 작업에서 나왔는지 확인되지 않았다.** 등급 경계 정리(2026-08-18)에서 머지 완료 worktree 7종을 지울 때 **이것만 남겼다** — 판단 근거가 없는 것을 지우지 않는다. 출처가 확인되면 처분을 정한다.
