# 다음 인프라 정비 — 목록

> **이 파일이 «다음 인프라 정비 목록»의 정본이다 (2026-08-18 신설).**
> 그 전까지는 목록이 문서로 없었다 — 정비 항목이 리포트·PR 본문·계획 문서에 흩어져
> 있었고, 그래서 **끝난 것과 안 끝난 것을 한눈에 볼 자리가 없었다.**
>
> **규칙 셋**
> 1. **항목마다 «근거»를 적는다.** 근거 없는 줄은 만들지 않는다 — 왜 해야 하는지가
>    없으면 다음 사람이 판단을 처음부터 다시 한다.
> 2. **선행 조건이 있으면 먼저 적는다.** 순서가 틀리면 헛일을 한다.
> 3. **끝난 항목은 지우지 말고 ✅ 로 남긴다.** 지우면 «왜 그렇게 했는지»가 사라진다.

## 🔴 우선순위 1 — 측정이 틀린 것부터

**정리하기 전에 재는 것을 고친다.** 잘못 잰 수치로 정리에 들어가면 멀쩡한 것을 고친다.

### 1. `vault_audit.py` 별칭(alias) 미해석 — 감사 수치가 7배 과대

| | 스크립트 값 | 실측 | 배율 |
|---|---|---|---|
| 깨진 위키링크 | **685** | **97** (고유 55) | 약 **7배** |
| 고아 노트 | **50** | **10** | 5배 |

- **원인**: vault 194개 노트 중 **112개가 `aliases:` 를 선언**하고 링크가 대개 alias 로 걸리는데, 스크립트의 `resolve_link_target()` 이 파일명만 본다. 파일명 기준 693건 중 **596건이 별칭으로 정상 해석되는 링크**다.
- **조치**: `resolve_link_target()` 에 alias 딕셔너리 추가.
- **근거**: `reports\2026-08-18_morning-vault-health.md` §1·§2, 08-17 리포트 §7-3-1.
- 🔴 **아래 3번(링크·고아 정리)의 선행 조건이다.**

## 🟡 우선순위 2 — vault 실체 정리

### 2. `BACKUP_PATH` 미설정 — 백업 상태를 실측할 수 없다 (JJ 지시 2026-08-18)

- **현재 상태**: `departments\ops\config.md` 에 `BACKUP_PATH: (미설정)`.
- **결과**: 아침 감사 점검 항목 5(백업 검증)가 **매일 «확인 불가»로 떨어진다.** 백업이 되고 있는지 안 되고 있는지를 **아무도 모르는 상태**가 계속된다.
- **조치**: 실제 백업 경로를 확인해 `BACKUP_PATH` 를 채운다. 백업이 실제로 없다면 **«없음»이라고 적는 것도 조치다** — 그래야 «확인 불가»(모름)와 «백업 없음»(앎)이 갈린다.
- **근거**: `reports\2026-08-18_morning-vault-health.md` 백업 검증 항목. 2026-08-18 모지바케 수정(PR #51) 이후 그 «확인 불가» **사유는 읽을 수 있게 됐지만, 사유가 해소된 것은 아니다.**

### 3. JJ-Brain 깨진 링크·고아 노트 정리 (JJ 지시 2026-08-18)

🔴 **1번을 먼저 한다.** 스크립트가 별칭을 해석하기 전에는 **무엇을 고쳐야 하는지가 7배 부풀려 보인다.**

실측 기준(08-17)으로 실제 대상은 아래다.

| 대상 | 수 | 성격 |
|---|---|---|
| 깨진 링크 | **97건 / 고유 55개** | 실제 대상 |
| 고아 노트 | **10개** | 실제 대상 |

**알려진 하위 항목 (원인이 이미 특정됨)**

- **`ai-native-*` 4종** — 노트는 있는데 `aliases:` 에 파일명 슬러그만 빠졌다. **26회 링크**된다. → `202605241906-`·`07-`·`08-`·`09-` 의 **기존 `aliases:` 블록에 슬러그 추가.** ⚠️ **블록을 새로 만들지 말 것** (08-17 리포트 §1 정정).
- **한글 제목 zettel 4개** — `202605141700`·`1900`·`2100`·`2200`, `aliases:` 미선언 → 고아. → alias 추가 + `02_Areas/UE5-Development/UE5-MOC.md` 등에서 링크 연결.
- **`00_Inbox/` 0바이트 6개** — 파일이 있어서 링크 검사는 전부 통과하는데 **내용이 비어 있다.** 그중 `data-driven-weapon-architecture.md` 는 vault 안에서 **26회 링크**된다. **열어봐야만 비어 있는 걸 안다** — §0 조용한 실패 계열. → 내용을 채우거나, 링크를 실체 노트로 돌리고 빈 파일을 지운다.

- **근거**: `reports\2026-08-18_morning-vault-health.md`, 08-17 리포트 §1·§2·§7.
- ⚠️ **vault 는 2026-07-05 이후 무변경이다.** 수치가 매일 같은 것은 정상이며, **정리하지 않는 한 스스로 줄지 않는다.**

## 다른 문서에 있는 정비 항목 (여기서 가리키기만 한다)

**내용을 복사하지 않는다** — 복사하면 두 곳이 갈린다.

| 항목 | 정본 | 상태 |
|---|---|---|
| 브랜드 스크립트 정본 단일화 | `docs\plan-brand-assets-move.md` | 승인·착수 대기. **창 조건 재판정 필요** — 그 문서의 창 판정은 2026-08-16 기준이라 낡았다 |
| `inner._v3_bottom` 침범 가드 | 같은 문서 §순서 0 | 위 정비보다 **먼저** 한다. 창이 안 열리면 **이 항목만 떼어 단독 진행** |
| Manychat API 연동 | `docs\infra-manychat-api.md` | **보류 확정.** 재검토 조건 2개가 성립하기 전에는 다시 보지 않는다 |
| A등급 부서 저장소 쓰기 경계 | `docs\ops-grade-boundary.md` | ✅ **해소 (2026-08-18, PR #50·#51)** — 도구 부재로 전환 |

## ✅ 끝난 것 (지우지 않는다)

| 항목 | 처리 | 근거 |
|---|---|---|
| ops-auditor 가 리포트를 못 쓰던 상태 | `tools` 에 `Write` 추가 | PR #50. 8/18 11:29 리포트가 🔴 즉시로 달아 둔 항목이었다 |
| A등급 부서의 `Bash` 보유 | `Bash` 제거 + 래퍼 선실행 구조 | PR #50. 역검증: **도구 부재**(`tool_uses: 0`) |
| 감사 데이터 한글 모지바케 | `PYTHONIOENCODING=utf-8` | PR #51. 실측: `Binary file matches` → `BACKUP_STATUS=확인 불가 (BACKUP_PATH 미설정)` |
| 열린 PR 을 아무도 안 보던 상태 | 아침 감사 점검 항목 6 신설 | PR #50. 근거: PR #45 가 이틀간 방치 |
| 리포트 부재가 «최초 실행»으로 읽히던 문구 | 점검 항목 7 신설 + 문구 교정 | PR #51 |
| 운영 서버 신선도 무경보 | 점검 항목 8 신설 | PR #51. 근거: **14커밋 뒤처진 채** 스케줄 가동 |

## 착수 순서 제안

    1. vault_audit.py 별칭 해석          ← 측정을 먼저 고친다
    2. BACKUP_PATH 확정                  ← 독립. 지금도 가능
    3. 링크·고아·inbox 정리              ← 1번 이후
    4. _v3_bottom 가드 → 브랜드 정본 단일화  ← 별도 트랙

**2번은 1번을 기다리지 않는다** — 서로 독립이고, 백업은 모르는 상태로 두는 비용이 가장 크다.
