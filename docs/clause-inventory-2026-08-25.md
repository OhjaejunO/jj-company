# 조문 재고 분류표 — 2026-08-25 (JJ 1차 승인 대상)

> **성격**: 문안 작성 전 분류표. 채택분 문안은 승인 후 별도 커밋. 이 문서 자체는 PR 에 «근거 기록»으로 동봉한다.
> **2026-08-25 JJ 확정 (분류표 결정 전건)** — 범위: 이관까지 이 세션 · E1 락 ⓐ claim 파일 · E2 승계 ⓐ 공용화 · E3 fetch 근거+장치 · E4 편/게시물 층 분리 채택 · E5 §5.9 폐지 사유 «예고는 다음 편을 구속하며 축 전환 시 거짓이 된다 — ep31 축 전환(폭로→가이드) 실증» · A3 채택(«검수 노드는 작성 세션과 별도 컨텍스트에서 실행», 외부 원칙 도입·자체 실측 없음) · ep20 수치: 검증로그 실측 — 소스 파일 80.5s, 업로드 슬라이드 79.6s(조문 유지) · PR #68 close · SKILL 946행 같은 커밋 · preflight.py 이관 PR 포함.
> **수집 범위**: 메모리 `pending-clause-*` 21건 전수 · 2026-08-22~25 세션 산출물(검증로그 §22 · 파이프라인 의존그래프 감사 · PR #68/#70/#71 본문 · 스케줄 판정 보고 · content-scout 개정 diff 초안 · 세션 트랜스크립트 키워드 전수) · SKILL.md v3.49 현행 대조.
> **총 33건** — 채택 22 · 기각(중복·종결) 4 · JJ 결정 대기 5 · 확인 필요 2.
>
> 🔴 **범위 주의.** 이 세션의 작업 범위는 «정관·CLAUDE.md·docs·에이전트 정의»다. 그런데 재고의 **절반 이상(17건)이 자회사 정본(`tomangchi-skill` SKILL.md·코드)** 대상이다. 정관 §1.5 «본사 에이전트는 자회사 정본을 수정하지 않는다» 에 따라 이 표에서는 **분류만 하고 «이관» 으로 표시**했다. 문안·PR 은 tomangchi-skill 트랙에서 별도 세션이 올린다 — **이 세션이 그 트랙까지 열지는 JJ 결정 (§E-1)**.

표기 — 대상: **본사**(이 세션이 문안·PR) / **이관**(tomangchi-skill 트랙) · 층: [[pending-clause-defect-order]] 4층 ①기본값 ②생성 ③검사 ④사람

---

## A. 게이트 계열

| # | 건 | 분류 | 대상 | 근거 실측 1줄 | 비고 |
|---|---|---|---|---|---|
| A1 | **`[8-1]` 킷↔FACTS 이중 대조 · `[8-2]` 킷 캡처 mtime 역전** 등재 | **채택** — 구현 실물 참조 형식 | 이관 (verify.template.py + SKILL 게이트 절) | ep31: 킷 3회 재생성마다 수동 `cp`, `_kit_live.png` 가 세션 중 코드로 생성된 적 0회 · 위반 차단 4건 실측([E] 700.7→720.7 킷 차단 · [F] HTML touch → «457초 역전» 차단) | 구현: `ep31/verify.py:118·127` · `00_브랜드에셋/cardcheck.py:602 (KIT_MUST)`. 번호 `[8-1]/[8-2]` 는 제안 — JJ 체계 확정. **템플릿(`verify.template.py`)에는 아직 없다** → 등재 = 템플릿 편입 + 조문 한 줄 |
| A2 | **§22-1 계열 확장 (1순위)** — `FACTS` 대조를 캡션·고정댓글·발행팩·킷까지(«그 값이 등장하는데 옛 값이면 FAIL») + §22-2 재조회 후 재빌드 범위에 킷 영상, 영상 mtime < `SNAPSHOT` → FAIL | **채택** — 단 **구현 선행** | 이관 | ep27 재조회 2회 모두 `STATUS: OK` 상태에서 캡션 수치 5건·고정댓글 1건·발행팩 8건 낡음 + 킷 영상 1건 미갱신(드라이브 해시 대조가 잡음) · ep28 §7-1 선적용이 «캡션 조회시점 누락» 을 게이트 밖에서 잡음 · ep28 §11-6 1건 | `[8-2]` 가 §22-2 의 절반(mtime)을 이미 구현. **§22-1(캡션·발행팩 옛 값 검사)은 미구현** — «구현 실물 참조» 형식이므로 구현 PR 과 같이 올린다. 역검증: 옛 수치 1건 심어 걸리는지 / 새 수치만이면 통과 양쪽 |
| A3 | **게이트 컨텍스트 분리** | 🟡 **확인 필요** | 이관 | 로컬 기록(트랜스크립트·파일)에 이 표현이 **없다** — 웹챗 결정으로 추정. 가장 가까운 실측: ep31 guard 3 첫 실행 19건 불일치가 **전부 검사기 쪽**(① `<style>` 미제거로 CSS 값 유입 ② 정규식이 `25625`→`56` 조각 ③ `700.7초` 의 `7` 이 «7월» 문맥에 되살아남) | 추정 문안: «대조 텍스트에서 마크업·스타일 컨텍스트를 벗겨 «사실 문맥»만 읽는다, 수치 토큰은 경계까지 통째로». **JJ 가 원 지시를 한 줄로 확인해 주면 채택으로 전환** |
| A4 | α **조립 전 pre-flight 검사** (훅 고유명사·헤드라인 폭·판 최소 글자 높이) | **채택** — 구현 실물 참조 | 이관 | ep31 재작업 5회 중 2회(훅 고유명사 0 · 판 02 너무 작음)가 «만든 뒤» 잡힘 — `assert_cover_nouns` 는 있었는데 조립 전 부르는 자리가 없었다 · 위반 차단 [B]·[C] 실측 | 구현: `00_브랜드에셋/preflight.py`(신규). ⚠️ **skill 사본(`tomangchi-skill`)에 없다** — 내일 `skill-drift-audit` 이 울 것. 등재 시 사본 동기화 필수 |
| A5 | 검사가 틀린 것을 요구하면 **검사부터 고친다** (게이트 개정 → 문장 제거 순서) | **채택 — B4 에 흡수** | 본사 | ep28: `REQUIRED` 두 줄이 셀프 언급 문장을 강제 → 문장만 지우면 FAIL 나는 구조 | 별도 조문 아님. B4 4층 조문의 «③ 검사» 항 부속 문장 |

## B. 전역 작업 규칙 계열

| # | 건 | 분류 | 대상 | 근거 실측 1줄 | 비고 |
|---|---|---|---|---|---|
| B1 | **히어독 금지(전역)** — 정규식·백슬래시·따옴표·em dash 든 코드는 Bash heredoc 으로 쓰지 않는다, Write/Edit 도구 | **채택** | 본사 §6 | 8/25: 파이썬 히어독이 `\1`→`\x01` 로 풀려 백레퍼런스 파손, **재시도 2회 포함 3회 헛돎**, `cat -A` 로 바이트를 찍고서야 발견 · 8/23: `I'm`(작은따옴표)·em dash 가 heredoc 과 cp949 콘솔을 깨뜨림 | 현행 §6 은 «Bash heredoc 은 UTF-8 그대로 쓰므로 안전» — **한글 인코딩에 한정된 말이고 코드 내용에는 틀린 말**이 됐다. 조항을 «인코딩엔 안전 / 이스케이프 문자 든 코드엔 금지» 로 갈라 적는다 |
| B2 | **동시 세션 락** — 같은 레포·같은 목표로 두 세션이 도는 것을 장치로 막는다 | **채택** + JJ 결정(장치 형태) | 본사 §3 | 8/25 13:03: 본 트리에 **다른 세션의 `handoff_schema.py` 판본**(untracked, `MIN_URLS` 없음)이 있고 PR #70 판본과 갈림 · 같은 날 pre-flight 세션과 킷 체인 세션이 `verify.py` 를 각자 고치게 돼 «이번 세션은 pre-flight 절만» 으로 사람이 피함 · 8/15 content-ops 사고 | 현행 §3 worktree 조항은 **HEAD 공유**만 막고 «같은 목표 중복»은 못 막는다. 장치 두 안 — ⓐ 착수 시 `logs\claims\<주제>.claim`(세션 ID·시각·목표 한 줄, 종료 시 삭제, 있으면 중단·보고) ⓑ worktree 이름 = 주제 규약 + 착수 시 `git worktree list` 대조. **권고 ⓐ**(worktree 없이 도는 세션도 잡는다). 어느 쪽이든 층 ① |
| B3 | **승계 ⓐ/ⓑ** — ⓐ 킷 체인 공용화(`shoot_kit`·`build_kitcard` 를 `00_브랜드에셋` 공용으로) / ⓑ 승계 체크리스트(편 착수 시 전 편 스크립트 목록 대조) | 🔴 **JJ 결정 대기** | ⓐ 이관(코드) · ⓑ 이관(SKILL §7) | ep31: ep30 `shoot_kit.py` 를 안 가져오고 새로 쓰면서 출력명이 `kit_full.png` 로 갈림, `build_kitcard` 는 ep30 것을 복사해 `_kit_live.png` 를 읽음 → **승계 누락**(의도된 변경 아님) · 같은 유형: content-scout 정의(241행) vs 스키마 갈림 | **권고 ⓐ** — 층 ①(공용화하면 승계할 것이 없다). ⓑ는 층 ③ 규율이라 정관 §3 «조항만으로는 규율이다» 에 걸린다. ⓐ 채택 시 B7 제작 순서 목록이 «공용 스크립트 호출 순서» 로 자연히 성립 |
| B4 | **결함을 잡는 순서 4층** ① 기본값·구조 → ② 생성 단계 → ③ 검사 → ④ 사람 + «못 잡는다» 표기 | **채택** | 본사 §0 | 8/22 하루에 셋이 다른 층: 발행 후 뒷정리 ③ · PS1 인코딩 ③ · webshot `bar=False` **①(검사가 아예 불필요해짐)** · logo-ref-decode(참조를 얻는 유일 경로에 가드 = ①) · ep28 게이트 먼저 고치기 | §0 «감지 장치»·«조용히 실패» 옆. A5 흡수. 층별 선례는 메모리 파일의 표 그대로 |
| B5 | **«STATUS 는 감사 완수 여부» 일반화** — 발견 심각도가 아니다, `FAIL` 은 절차 미완·데이터 부재 | **채택** | 본사 §4 (ops-auditor.md:111 은 참조로 축약) | 8/25 ops-auditor 신규 세션이 합성 RED 발견에 `STATUS: FAIL` 을 찍음 · 8/24 scout 는 승인 거부 9건으로 절차를 건너뛰고도 `STATUS: OK`(반대 방향 오류) | 두 방향을 같이 적는다: «발견 🔴 ≠ FAIL» / «절차 누락 = OK 아님(부분 완수 표기)». 현재 에이전트 정의 1곳에만 있어 다른 두 스카우트에는 안 걸린다 |
| B6 | **git fetch 정본** — 조항은 있으나(§3) 재발 | **채택(근거 병기)** + JJ 결정(장치까지 갈지) | 본사 §3 | 8/24: 로컬 main 2커밋 뒤처져 **머지된 킷 PR 을 «미배포»로 여러 번 오보** + 그 과정의 `git stash` 가 당일 배포본을 옛 판으로 되돌림(라이브 전 발견) · 8/22 에도 3커밋 뒤처져 «ep26 킷 미배포» 오독 | **조항이 있었는데 두 번 났다** — 규율의 한계. 장치 안: `check-repo-guard.ps1` 이 «로컬 main 이 origin/main 뒤인가» 를 STATUS 로 찍고, 세션 착수 절차에 «fetch 후 시작» 을 둔다(층 ①로 이동). 권고: 장치까지 |
| B7 | **SKILL.md 제작 순서 단일 목록** | **채택** — 구현 실물 참조 | 이관 (SKILL §7 또는 §6 머리) | 파이프라인 감사 §2: §7·발행 체크리스트·§5.5 가 조각만 규정, **명목 vs 실측 대조가 구조적으로 불가능** · ep31 실측 순서표 있음 | 순서 안에 A4(pre-flight)·A1(guard) 위치를 박는다. B3 ⓐ 채택 시 «공용 스크립트 호출 순서» 로 적는다 |
| B8 | 스킬 사본 상대경로 — `ROOT` CWD 기준 또는 상대경로 거부 | **채택** (코드) | 이관 | 8/22: `webshot.py` 가 `~\.claude\skills\shots\…` 에 **조용히 쓰고 성공을 찍음**, `deliver.py` 는 SystemExit | 권고 ②(거부 — 조용한 실패를 요란한 실패로). ⚠️ 8/25 `fix-webshot-nobar`(#74) 는 주소 바 건이라 **이 건은 미해결**로 본다 — 확인 후 착수 |
| B9 | `logo_reference()` 디코드 가드 | **채택** (코드 + v3.28 한 줄) | 이관 | 8/22: 404 HTML 128,658 B 가 `.png` 로 저장, `exists()` 통과 — 정상 PNG(7,249 B)보다 17배 큼 | 층 ①. 역검증: 404 HTML `.png` 걸림 / 정상 PNG 통과 |
| B10 | 마크 왜곡 측정엔 기준물 + «자를 만들 수 있으면 만든다»(종이 정면) | **채택** | 이관 (§6.8 `logo` 행 · v3.28) | ep27 2회차(격자 있음) 1.28 왜곡 걸러냄 / 3회차(대리석) 네 방법이 0.607~1.098 로 전부 어긋남 · ep28 D 정면 종이 롤 +7.56°, 보정 후 0.5% | C3 과 같은 PR |

## C. 채널 조문 계열 (전부 자회사 정본 — 이관)

| # | 건 | 분류 | 자리 | 근거 실측 1줄 | 비고 |
|---|---|---|---|---|---|
| C1 | **표지 레퍼런스 4항** (주체를 화면에·숫자를 그림이·소재의 세계·다크) + **낱장 유통 기준(본문 카드도 마크)** + `cardcheck.reasons()` «밝고 평평하게» 문구 정정 | **채택** | §6 표지 절 | ep27 표지 4회 실패 후 JJ 레퍼런스 16장 분석 · 다크 하단 대비 206~229 vs 밝은 178.8 · ep28 허리 카드 합성 가능 사각 139×123px(=카드 65px, 표지 마크의 절반 이하) | C2②·C3·B10 과 같은 PR. 검사기 문구가 조문에 없는 제약을 만든 사례 — 코드 수정 동반 |
| C2 | ① 씬 문자열 대조는 **선언 자리를 잘라** 읽고 **표지도 대상** · ② **주체 크기는 프롬프트로 안 내려간다**(단독 아니면 상한 없음, 필요하면 코드 합성) | **채택** — ①은 **v3.48 과 중복 검사 필요** | §6.8 v3.45 옆 | ① 전체 이미지 검출 0건 vs 크롭 `14만` 검출(멀쩡한 씬 둘이 재생성될 뻔) · ② 지시 24% → 실물 40.1%·40.9%, 세 방식 전부 실패 | v3.48(2026-08-22 «씬 문자열 검사를 최종 카드로 + safe_area»)이 ①의 «표지도 대상» 을 이미 덮는지 문안 단계에서 대조 — 덮으면 ①은 «선언 자리 크롭» 한 줄만 남긴다 |
| C3 | **캐릭터 표지 유형** — 소재 주체는 마크(=«주체가 화면에 서 있다»), 캐릭터는 동작·관계만 · 예외 3조건 · 주체=캐릭터면 캐릭터가 대신 | **채택** | §6 «토망치 AI 소식 표지 기본 금지» 바로 아래 | ep28 1회차 A·B 국소 대비 1.85·1.32 탈락, D 10.67 · ep30 «공식 아님»이라 마크 불가 → 캐릭터가 주체 | 생성 실패(`status=failed`)로 «2장 뽑아 고른다»가 1장이었던 기록 포함 |
| C4 | **표지 훅 말미 물음표 허용** + §6 786줄 «의문형 §4.1 결정 대기» 표기 삭제(본문 헤드라인은 평서형 유지) | **채택** | §6 표지 절 (v3.46 옆 아님) | SKILL 「물음표」 0건, v3.46 은 씬 선언 문자열 전용(OCR) · ep28 첫 사례 · JJ 8/24 종결 | «결정 대기» 표기를 안 지우면 닫힌 것이 열린 채 남는다 |
| C5 | **헤드라인 해요체(ep28~)** + 게이트 `[3]` 헤드라인 검사 `since="ep28"` | **채택** | §4.1 v3.22 옆 | ep28 카드 6장 합쇼체→해요체 · 게이트 `[3]` 이 헤드라인을 «검사 없음» 으로 열어 둔 것이 원인(SKILL 292행) | 경계: 표지 반말 훅·캡션 존댓말 무변경. 역검증 3종(합쇼체 걸림/해요체 통과/표지 반말 안 걸림) |
| C6 | **본문 출처·셀프 언급 금지** + 카드=말/킷=원문 층 분리 | **채택** | §5.8-1 v3.20 아래 | ep28 3건 전수 제거 · ep30 킷 3절 머리 한 줄로 관계 명시 | 게이트 탐색 패턴 4종(«기준이에요»·«우리가»·«X에 올린»·«공식 발표») 추가 |
| C7 | **영상 전체 사용(구간 컷 금지)** + 소스 우선순위 ⓐ~ⓕ(산문 캡처 금지·공식 에셋·UI만·에셋 2회·없으면 카드 금지·실물 검수) | **채택** — 🔴 **충돌 1건** | §6 실물 위계 절, v3.24 아래 «별개 조항» 명시 | ep28 06 4.5초 컷 → JJ 폐기, 전체 32.33초 레터박스 · 캡처 320 CSS 3.38배 확대해도 «산문은 산문» · 9장→8장 | **SKILL 946행 «구간 컷은 8~18초 기본»(v3.25.1) 과 정면 충돌** — 같은 PR 에서 그 문장을 «풀버전 기본, 구간 컷 없음» 으로 개정해야 한다. 안 고치면 조문 둘이 반대 말을 한다 |
| C8 | 캐러셀 영상 60초 상한 실증 | ⚪ **기각(중복)** | — | **v3.25.1(2026-08-19) 이 이미 «ep20 AIMV 79.6s 풀버전 수용, 60s 상한은 구버전 오염» 으로 등재**(SKILL 70·946행) | 조문이 있었는데 실장이 «60초 상한» 을 전제했다 — 문제는 **조항 부재가 아니라 미조회**. 딸린 확인: 메모리는 **80.5초**, 조문은 **79.6초** — 같은 ep20 인데 수치가 다르다, JJ 확인 |
| C9 | **제3자 인용 맥락 검수** (귀속 확정 + 스레드 루트까지) | **채택** | §6 실물 위계, C7② 아래 | ep30 `@allforbigfire`: «ELI5 スタイル» 만 있어 귀속 불확정 + 루트가 정치 주장 스레드 → JJ 확정 받고도 중단·보고 | «지표로 고르지 않는다»·«후보 전부 막히면 카드를 접는다» 부속 |
| C10 | **어느 합계인지** — 임계 값은 상수 이름으로, 판정은 빌더 출력 | **채택** | §6.5 수치 절 | ep27: `personal_stars_total()` 169,950 으로 «초과 중단» → 실제 `TWO_STARS` 141,702 유지가 맞았음 | 이미 `REQUIRED` 로 걸려 있음 — 구현 실물 참조 |
| C11 | **v3.47 «기대할 자리» 기준선은 채널 공표가 정한다** + 판정 예시 4건 | **채택** | v3.47 절 | 8/22 전수: 「저희도 아직 안 해봤어요」 3건 ① vs 「자막 확인 안 함」 ③ | 예시표 그대로 수록. 발행 킷 3건 소급 없음 |
| C12 | **§5.9 «다음 편 예고» 폐지** | **채택** | §5.9 고정 댓글 6항 · v3.25 «예고 체인» | 8/24 3편 전수 예고 0건(ep30 1줄 제거) · 게이트는 예고를 요구하지 않음(캡션 금지·고정댓글 선택) | ⚠️ 원 지시(«긴급 프롬프트»)는 실장 세션에 **도달하지 않았고** 사후 확인으로 실행됐다. 폐지 사유 한 줄(왜 접었나)은 JJ 가 문안에 보태 주어야 근거 없는 줄이 안 된다 |
| C13 | **편수 / 게시물 수 층 분리** (발행로그 채널 열) | 🔴 **JJ 결정 대기** | 발행로그 규칙 | 현행 정의 «캐러셀+릴스=2건» 은 다른 산출물이라 성립, 채널 재게시는 같은 산출물 재유통 — 인스타·Threads·X 면 편수 3배 | 유통 확장(`04_운영/유통확장_설계안.md`) 확정과 한 몸. 확정 전엔 인스타 기준으로만 센다 |
| C14 | 크롭 폴백 조건 4건 | ⚪ **기각(종결)** | — | **v3.49 등재 완료** (SKILL 35·123·1111행) | 메모리 `pending-clause-crop-fallback` 삭제 대상 |
| C15 | 씬 프롬프트 실무 2건 (완전히 비어 있음·한 줄로) | ⚪ **기각(종결)** | — | **v3.49 등재 완료** (SKILL 1706행) | 메모리 `pending-clause-scene-prompt-v339` 삭제 대상 |

## D. 정합성(문서-실물) 계열

| # | 건 | 분류 | 대상 | 근거 실측 1줄 | 비고 |
|---|---|---|---|---|---|
| D1 | **«정본은 실값» 일반화** — 실물을 조회할 수 있는 항목은 실물이 정본, 문서는 조회 결과; 문서가 실물과 다르면 문서가 틀린 것 | **채택** | 본사 §0 (현황판 주석은 참조로 축약) | 현황판 07:30/07:00 이 실값 12:30 과 3일 어긋나 8/23 ops-auditor «미실행» 오독 · 백로그 «WakeToRun 적용 완료» vs 실물 4/4 False(8/25) · 킷 «미배포» 오독(8/24, B6) · content-scout 정의 vs 스키마(D2) | 네 사고가 한 뿌리. 조문에 «조회 절차가 있는 항목은 절차를 가리킨다»(`docs\schedule-task-registration.md` 등) 를 붙인다 |
| D2 | **content-scout 정의 개정 diff 적용** (241~247행 → `handoff_schema.py` 참조 + 리포트 본문 2항목) | **채택** | 본사 `.claude/agents/content-scout.md` | 7회차 중 1회차만 «## 제안» 헤더 · 대기함 #11~15 가 정의에 없는 필드 사용 · 이관 14블록 검사 1건 통과·미기입 49 | 초안 `departments/marketing/_content-scout_handoff_diff_draft_20260825.md`. **행 번호(241행) 현재 main 과 일치 확인.** 함께: 58행 참조 한 줄 · «X 화제» 경계 · 래퍼 사후 검사(`STATUS: FAIL handoff-schema`)는 별도 작업 |
| D3 | **보안 오탐 판정 기록 절차** | **채택** | 본사 §2 한 줄 + `docs\security-review-log.md` 신설 | 8/25 자동 보안 리뷰가 `permissions.deny` 12건을 allow 로 오독 → **두 세션이 각각 다시 판정**(13:24·13:35) — 기록 자리가 없어 같은 판정을 두 번 함 | 원장 열: 일자·알림 출처·판정(오탐/정탐)·근거 커밋·조치. 스케줄 판정의 «deny 미발생 = 걸릴 입력 없음» 도 같은 원장. 이후 같은 알림은 원장을 먼저 본다 |
| D4 | α **allowed-tools 는 게이트가 아니다** — `acceptEdits` 는 Edit/Write 를 목록과 무관하게 통과시킨다, 목록은 Bash 접두·Web 만 가른다 | **채택** | 본사 §4 | 스케줄 판정: 목록 밖 `Edit` 9·`Write` 16·`rm`·`date`·`grep`·`ls`·`wc` 실행 · `Edit(reports/**)` 프로브에서 Write 통과 | #71 이 메모리·래퍼 주석에 적었으나 **정관에는 없다**. vault-health default 모드 시범(8/26~28) 판정과 함께 확정 |
| D5 | 스케줄 재등록 후 실값 재확인 절차 | ⚪ **기각(완료)** | — | #71 `docs\schedule-task-registration.md` 신설·현황판 주석 | 종결 |

## E. JJ 결정 요청 (분류표 승인과 함께)

1. **범위** — 이관 17건(A1·A2·A3·A4·B3·B7~B10·C1~C7·C9~C12)을 **이 세션이 tomangchi-skill worktree 에서 이어서 올릴지**, 별도 세션으로 넘길지. 어느 쪽이든 정관 §1.5 에 따라 **본사 에이전트가 자회사 정본을 고치는 것**이므로 지목 승인이 필요하다. 권고: 이 세션이 절별 PR 3~4건으로 이어서(재고가 머리에 있을 때).
2. **B2 락 형태** — ⓐ claim 파일 / ⓑ worktree 이름 규약. 권고 ⓐ.
3. **B3 승계** — ⓐ 킷 체인 공용화 / ⓑ 체크리스트. 권고 ⓐ.
4. **B6** — 근거 병기만 / 장치(`check-repo-guard` 뒤처짐 검사)까지. 권고 장치까지.
5. **C13** — 유통 확장 확정 여부에 따라.
6. **A3·C12** — 원 지시 한 줄 확인(A3 문안 / C12 폐지 사유).
7. **C8 수치** — ep20 풀버전 79.6초(조문) vs 80.5초(메모리) 중 어느 것이 실측인가.

## F. 정합성 부수 발견 (조문 아님 — 보고만)

- 🔴 **PR #68(OPEN, `chore-waketorun-off`) 이 #71 과 반대 방향이다.** #68 은 «WakeToRun 폐기 + 절전→부팅 전제 정정», #71 은 «WakeToRun 8/25 재적용 완료» 를 백로그에 기록했다. #68 의 CLAUDE.md 수정(«02:12 절전 → 11:40 기상» 을 «부팅» 으로)은 지금도 유효할 수 있으나 WakeToRun 부분은 충돌 — **#68 처리 방향 결정 필요**(닫거나, 전제 정정만 남겨 리베이스).
- 🟡 `00_브랜드에셋/preflight.py` 가 `tomangchi-skill` 사본에 없다 — 다음 `skill-drift-audit` 이 🟡 로 뜬다(A4 등재 시 해소).
- ⚪ 메모리 정리: C14·C15 는 등재 완료라 `pending-clause-*` 2건 삭제 + MEMORY.md 색인 갱신(이 세션이 처리).

## G. 산출 PR (2026-08-25 문안 단계 완료 — 전부 생성만, 머지는 JJ 지목 승인)

| PR | 레포 · 브랜치 | 내용 | base / 순서 |
|---|---|---|---|
| **#72** | jj-company `docs-clause-inventory` | 본사 9건 + `claim.ps1` + guard 뒤처짐 검사 + content-scout 스키마 참조 + 보안 원장 + 이 분류표 | main |
| **#75** | tomangchi-skill `docs-gate-clauses` | v3.50 게이트 운용 3조 — `[5-3]`·`[8-1]~[8-3]`·pre-flight·사본 동기화 | main — **스택 1번** |
| **#76** | tomangchi-skill `docs-cover-clauses` | v3.51 표지·씬 6건 + `reasons()` 문구 | #75 — 2번 |
| **#77** | tomangchi-skill `docs-channel-clauses` | v3.52 채널 8건 + `[3]`·`[5-4]`·`[7]` + 946행 개정 | #76 — 3번 |
| **#78** | tomangchi-skill `feat-kit-chain-shared` | v3.53 제작 순서 목록·`kitchain.py`·상대경로 거부 | #77 — 4번 |
| **#18** | content-ops `fix-logo-ref-decode` | `logo_reference()` 디코드 가드 | main |
| #68 | jj-company | **close** — WakeToRun 재적용으로 전제 붕괴 | — |

머지 후 할 일과 부수 발견(ep27 킷 mtime 256초 역전 · ep31 공용 게이트 비호환 · `[4-2]` 코드 미반영 · 실행 정본 동기화 · 발행로그 머리 정정)은 메모리 `clause-prs-pending-merge` 와 각 PR 본문에 있다.
